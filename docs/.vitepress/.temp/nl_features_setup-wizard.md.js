import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Setup Wizard","description":"","frontmatter":{"title":"Setup Wizard","last_verified":"2026-05-19T00:00:00.000Z"},"headers":[],"relativePath":"nl/features/setup-wizard.md","filePath":"nl/features/setup-wizard.md"}');
const _sfc_main = { name: "nl/features/setup-wizard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="setup-wizard" tabindex="-1">Setup Wizard <a class="header-anchor" href="#setup-wizard" aria-label="Permalink to &quot;Setup Wizard&quot;">​</a></h1><p>Een magische setup in vijf stappen op <code>/setup</code> die je bedrijfsidentiteit vastlegt, optioneel je registratie ophaalt bij de KvK of een EU-register, een webadres kiest, merksuggesties genereert en alles in een klik toepast. De wizard is de voordeur voor nieuwe gebruikers en blijft daarna beschikbaar voor aanpassingen.</p><p>Als je hier bent voor de basiswandeling, begin dan bij <a href="/nl/getting-started/company-setup">Je bedrijf instellen</a>. Deze pagina is de diepe referentie: elke stap, elke optie, elk gevolg.</p><h2 id="wanneer-de-wizard-wordt-aangeboden" tabindex="-1">Wanneer de wizard wordt aangeboden <a class="header-anchor" href="#wanneer-de-wizard-wordt-aangeboden" aria-label="Permalink to &quot;Wanneer de wizard wordt aangeboden&quot;">​</a></h2><p>De wizard is op twee manieren bereikbaar:</p><ul><li><strong>Direct:</strong> ga op elk moment naar <code>/setup</code>.</li><li><strong>Dashboard-banner:</strong> zolang de onboarding niet voltooid is, staat er een sluitbare &quot;Setup afronden&quot;-banner bovenaan <code>/dashboard</code>. Deze toont een teller van openstaande velden (of een generiek &quot;Maak je profiel compleet&quot;-bericht als de teller nul is) en een knop terug naar de wizard. Een sluitknop (X-icoon) verbergt de banner per browser via localStorage, zodat deze over herlaadbeurten heen verborgen blijft totdat de wizard is voltooid.</li></ul><p>De wizard is niet-blokkerend: de oude gedwongen doorverwijzing naar <code>/setup</code> bij aanmelding (de <code>ONBOARDING_MANDATORY</code>-poort) is verwijderd. Nieuwe aanmeldingen komen direct op <code>/dashboard</code> en zien in plaats daarvan de sluitbare banner.</p><h2 id="pagina-opbouw" tabindex="-1">Pagina-opbouw <a class="header-anchor" href="#pagina-opbouw" aria-label="Permalink to &quot;Pagina-opbouw&quot;">​</a></h2><p>De wizard is een enkele pagina (<code>/setup</code>) met vijf stappen in een stepper. Elke stap schrijft de antwoorden bij elke wijziging naar een JSONB-kolom (<code>companies.onboarding_state</code>), dus de wizard is volledig hervatbaar: sluit de tab midden in een stap en het volgende bezoek brengt je naar dezelfde stap met dezelfde ingevulde antwoorden.</p><p>Drie knoppen staan onderaan elke stap:</p><ul><li><strong>Terug</strong> — ga naar de vorige stap (verborgen bij stap 1).</li><li><strong>Nu overslaan</strong> — vertrek zonder de onboarding als voltooid te markeren. De dashboard-banner blijft staan zodat de gebruiker kan terugkeren.</li><li><strong>Doorgaan</strong> / <strong>Voltooien</strong> — per stap beveiligd op de onderstaande antwoorden.</li></ul><p>Er is geen &quot;opslaan en afsluiten&quot; — opslaan gaat automatisch. <strong>Nu overslaan</strong> is een schone uitgang, geen weggooien.</p><h2 id="stap-1-—-bedrijf" tabindex="-1">Stap 1 — Bedrijf <a class="header-anchor" href="#stap-1-—-bedrijf" aria-label="Permalink to &quot;Stap 1 — Bedrijf&quot;">​</a></h2><p>Legt de twee antwoorden vast waarvan elke latere stap afhangt.</p><table tabindex="0"><thead><tr><th>Veld</th><th>Opgeslagen als</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Verplicht</th><th>Opmerkingen</th></tr></thead><tbody><tr><td>Bedrijfsnaam</td><td><code>answers.businessName</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td><td>Gebruikt als <code>display_name</code> en <code>company_name</code> bij Voltooien (de juridische naam uit de registerstap wint voor <code>company_name</code> als beide aanwezig zijn). Vult ook de subdomein-slug-suggestie in stap 3.</td></tr><tr><td>Wat je doet</td><td><code>answers.what</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td><td>Vrije tekst, 1–2 zinnen. Wordt samen met land en sector naar de suggestie-generator (Gemini) van de magische stap gestuurd.</td></tr></tbody></table><p><strong>Doorgaan is uitgeschakeld</strong> totdat beide velden niet-lege, bijgesneden waarden hebben.</p><h2 id="stap-2-—-register" tabindex="-1">Stap 2 — Register <a class="header-anchor" href="#stap-2-—-register" aria-label="Permalink to &quot;Stap 2 — Register&quot;">​</a></h2><p>Kies een land en zoek het bedrijf op in het officiele register van het land, vul de gegevens handmatig in, of sla de stap over.</p><h3 id="drie-wegen" tabindex="-1">Drie wegen <a class="header-anchor" href="#drie-wegen" aria-label="Permalink to &quot;Drie wegen&quot;">​</a></h3><ol><li><strong>Zoeken:</strong> typeahead op bedrijfsnaam, kies een match en laat de backend het Basisprofiel ophalen (EUR 0,02 per aanroep voor NL). Het zoekvak wordt alleen getoond als de betaalde KVK API (<code>KVK_API_KEY</code>) is ingesteld op de API-container. Als de sleutel ontbreekt, wordt zoeken overgeslagen en start de wizard standaard met handmatige invoer (weg 2).</li><li><strong>Handmatig:</strong> vul zelf de bedrijfsnaam, KVK-nummer (optioneel), adres, postcode en plaats in. De gegevens worden direct naar de bedrijfsrij geschreven via <code>PUT /company-settings/company</code> en <code>answers.kvk</code> krijgt <code>manual: true</code>. Handmatige invoer is de standaard wanneer KVK_API_KEY niet is ingesteld, en bestaat ook voor twee extra situaties zelfs wanneer zoeken beschikbaar is: (a) nieuwe bedrijven die nog niet in de gratis OpenKVK-dataset staan, en (b) bedrijven waarvan de handelsnaam niet overeenkomt met wat de gebruiker in de zoekopdracht typte.</li><li><strong>Overslaan</strong> — &quot;Geen KVK-inschrijving?&quot; slaat <code>answers.kvk = null</code> op. De wizard gaat verder; bedrijfsgegevens kunnen later in de instellingen worden ingevuld.</li></ol><p>Wisselen tussen zoeken en handmatig gaat met een klik (wanneer zoeken beschikbaar is): een &quot;Vul handmatig in&quot;-knop staat onder de zoekresultaten, en een &quot;Terug naar KVK-zoeken&quot;-link bovenaan het handmatige formulier. Wanneer <code>KVK_API_KEY</code> ontbreekt, is de schakel-naar-zoeken-knop volledig verborgen en begint en blijft de gebruiker op handmatige invoer.</p><h3 id="landopties" tabindex="-1">Landopties <a class="header-anchor" href="#landopties" aria-label="Permalink to &quot;Landopties&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Code</th><th>Getoond register</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Opzoeken actief</th></tr></thead><tbody><tr><td><code>NL</code></td><td>KvK</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>FR</code></td><td>SIRENE</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>GB</code></td><td>Companies House</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>DE</code></td><td>Handelsregister</td><td style="${ssrRenderStyle({ "text-align": "center" })}">nee — handmatige invoer</td></tr><tr><td><code>OTHER</code></td><td>(geen register)</td><td style="${ssrRenderStyle({ "text-align": "center" })}">nee — handmatige invoer</td></tr></tbody></table><p><code>NL</code> is de standaard. Land bepaalt meerdere dingen bij Voltooien: tijdzone (<code>Europe/Amsterdam</code> / <code>Europe/Berlin</code> / <code>Europe/Paris</code> / <code>Europe/London</code>), <code>pdf_language</code> (<code>nl</code> / <code>de</code> / <code>fr</code> / <code>en</code>), en het registerlabel in <code>footer_text</code> (<code>KvK 12345678</code>, <code>Handelsregister …</code>, <code>SIRENE …</code>, <code>CRN …</code>).</p><h3 id="zoekmodus" tabindex="-1">Zoekmodus <a class="header-anchor" href="#zoekmodus" aria-label="Permalink to &quot;Zoekmodus&quot;">​</a></h3><p>Voor ondersteunde landen zoekt de gebruiker op bedrijfsnaam. De backend roept de bijpassende publieke API aan en retourneert een van:</p><ul><li><strong>ok</strong> — <code>legalName</code>, <code>address</code>, <code>postalCode</code>, <code>city</code>, <code>sector</code> ingevuld en opgeslagen onder <code>answers.registry</code>.</li><li><strong>not-configured</strong> — provider nog niet aangesloten in deze omgeving; de gebruiker wordt gevraagd over te schakelen naar handmatige invoer.</li><li><strong>not-found</strong> — nummer niet gevonden; gebruiker kan opnieuw proberen of overschakelen naar handmatig.</li><li><strong>invalid</strong> — nummerformaat afgewezen.</li><li><strong>error</strong> — providerfout; handmatige invoer blijft een optie.</li></ul><h4 id="kvk-basisprofiel-alleen-nl" tabindex="-1">KvK Basisprofiel (alleen NL) <a class="header-anchor" href="#kvk-basisprofiel-alleen-nl" aria-label="Permalink to &quot;KvK Basisprofiel (alleen NL)&quot;">​</a></h4><p>Voor NL-werkruimtes is de KvK-opzoeking een tweestapsproces:</p><ol><li><strong>Typeahead</strong> — de gebruiker zoekt op bedrijfsnaam. Het <code>zoeken</code>-endpoint (gratis) retourneert overeenkomende vermeldingen. Dit is de autocomplete-stap die de bestaande <code>ok</code> / <code>not-found</code>-antwoorden aanstuurt. Als de zoekopdracht nul treffers oplevert, toont de UI een inline leeg-resultaatpaneel (titel, uitleg, en een &quot;vul handmatig in&quot;-knop die het handmatige formulier alvast vult met wat de gebruiker typte). Dit is gebruikelijk omdat de gratis OpenKVK-laag veel jonge bedrijven mist.</li><li><strong>Basisprofiel</strong> — zodra een match is gekozen, roept de wizard het KvK Basisprofiel-detailendpoint aan. Dit is een betaalde aanroep (EUR 0,02, 24u gecached per KVK-nummer). Het retourneert het volledige profiel: <code>legalName</code>, <code>statutaireNaam</code>, <code>tradeNames</code> (alle geregistreerde handelsnamen, gesorteerd), <code>rsin</code>, <code>legalForm</code>, <code>dateFounded</code>, bezoek- en postadressen, SBI-codes met primair-vlag, <code>employeeCount</code> en <code>indNonMailing</code> (geen-post-vlag). De wizard vult <code>business_page_hero_tagline</code> met de eerste handelsnaam en plaats (&quot;[handelsnaam] in [plaats]&quot;), en <code>description</code> met de primaire SBI-omschrijving (beperkt tot 280 tekens). Beide gebruiken COALESCE zodat handmatige aanpassingen in Instellingen behouden blijven.</li></ol><p>Wanneer een match wordt gekozen (zoekmodus), roept de frontend direct <code>PUT /company-settings/company</code> aan met de <code>name</code> van de match als <code>display_name</code>, het <code>kvkNumber</code> als <code>chamber</code>, <code>city</code>, en <code>country</code> hardcoded op <code>&quot;NL&quot;</code>. Dit schrijft de klantgerichte weergavenaam direct weg, zodat de dashboardbegroeting en de ge-seede site de juiste naam gebruiken vanaf het moment dat de gebruiker op de match klikt, in plaats van te wachten op de Voltooien-stap van de wizard.</p><p>Er zijn twee onafhankelijke functievlaggen voor NL KVK-opzoekingen:</p><ul><li><code>KVK_API_KEY</code> (env-var op de API-container): als deze ontbreekt, wordt het zoekvak helemaal niet getoond en start de wizard op die stap standaard met handmatige invoer. De gratis OpenKVK-laag alleen is te beperkt (~2% trefferkans, mist bijna elke nieuwe inschrijving). De vlag schakelt automatisch om zodra de sleutel is ingesteld.</li><li><code>KVK_BASISPROFIEL_ENABLED</code>: als deze uit staat (of als <code>KVK_API_KEY</code> ontbreekt), gebruikt de wizard alleen het gratis <code>zoeken</code>-resultaat (dezelfde <code>ok</code>-velden als hierboven). Als deze aan staat en <code>KVK_API_KEY</code> is aanwezig, verrijken de Basisprofielgegevens de <code>answers.registry</code>-payload en toont de Controle-stap elk veld dat naar de bedrijfsrij wordt geschreven.</li></ul><h3 id="handmatige-modus" tabindex="-1">Handmatige modus <a class="header-anchor" href="#handmatige-modus" aria-label="Permalink to &quot;Handmatige modus&quot;">​</a></h3><p>Gebruiker vult <code>chosen</code> (het registratienummer) in, en optioneel <code>legalName</code>, <code>address</code>, <code>sector</code>. Alle vier velden zijn optioneel in deze modus.</p><p>Bij opslaan roept de frontend <code>PUT /company-settings/company</code> aan met de handmatig ingevulde waarden plus <code>display_name</code> ingesteld op de hier getypte bedrijfsnaam en <code>country</code> hardcoded op <code>&quot;NL&quot;</code>. Dit schrijft zowel de klantgerichte naam als het land direct weg, in plaats van te wachten op de Voltooien-stap. De apply-logica van de Voltooien-stap leest nog steeds <code>answers.kvk</code> voor het legacy registerpad, maar het nieuwe 2-staps wizardpad leest <code>answers.kvk.legalName</code> en <code>answers.kvk.kvkNumber</code> direct, dus de inline PUT tijdens deze stap is de gezaghebbende schrijfactie.</p><h3 id="overslaan-modus" tabindex="-1">Overslaan-modus <a class="header-anchor" href="#overslaan-modus" aria-label="Permalink to &quot;Overslaan-modus&quot;">​</a></h3><p>Een &quot;Nog niet geregistreerd&quot;-toggle slaat <code>answers.registry = null</code> op. <strong>Doorgaan is altijd toegestaan op deze stap</strong>, ongeacht de modus — registergegevens zijn gemak, geen poort.</p><h2 id="stap-3-—-domein" tabindex="-1">Stap 3 — Domein <a class="header-anchor" href="#stap-3-—-domein" aria-label="Permalink to &quot;Stap 3 — Domein&quot;">​</a></h2><p>Kies het webadres dat je klanten zien op de openbare bedrijfspagina en je e-mailinbox. Drie routes, weergegeven als kaarten, dekken elk pad van gratis snelstart tot het kopen van een domein in de wizard.</p><h3 id="drie-keuze-kiezer" tabindex="-1">Drie-keuze-kiezer <a class="header-anchor" href="#drie-keuze-kiezer" aria-label="Permalink to &quot;Drie-keuze-kiezer&quot;">​</a></h3><p>Een rooster van drie kaarten biedt de keuze. Het selecteren van een route onthult de bijbehorende editor eronder; er is maar een route tegelijk actief.</p><p><strong>Subdomein (gratis):</strong> de gebruiker kiest een slug; een TLD-kiezer laat kiezen tussen <code>.mycompanydesk.nl</code> en <code>.mycompanydesk.com</code>. De slug is vooringevuld vanuit de KVK-juridische naam wanneer beschikbaar (kleine letters, accenten verwijderd, niet-ASCII-tekens verwijderd, afgekapt op 63 tekens), zodat de meeste gebruikers kunnen tikken-en-doorgaan zonder te typen. Beschikbaarheid wordt live gecontroleerd met een vertraging van 350 ms terwijl de gebruiker typt. Bij Voltooien wordt het subdomein via de Cloudflare API aangemaakt en is de website van het bedrijf direct bereikbaar.</p><p>Wanneer de wizard wordt uitgevoerd in de 2-staps (plan-gestuurde) flow, wordt de Domein-stap volledig overgeslagen. De Voltooien-stap maakt automatisch een werkruimte-subdomein aan op basis van de <code>display_name</code>-waarde: de slug wordt afgeleid van de weergavenaam (met opnieuw-proberen-bij-botsing-achtervoegsels tot 5 pogingen), en <code>activateSubdomain</code> registreert deze als de openbare site-URL. Best-effort: een botsing of fout wordt gelogd en blokkeert de wizard niet om te voltooien.</p><p><strong>Eigen domein:</strong> de gebruiker typt een domein dat hij al bezit. Een live validatie-regex controleert het formaat tijdens het typen (<code>[a-z0-9][a-z0-9-]*(\\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+</code>). Bij Voltooien voegt de wizard het domein toe aan de domeinlijst van de werkruimte (geen actie als het al was toegevoegd) en schakelt automatisch de inbox in: <code>info@&lt;domein&gt;</code> als de standaard mailbox plus <code>support@</code>, <code>sales@</code> en <code>noreply@</code> aliassen. Het 409-al-bestaand-pad van <code>apply.service</code> wordt netjes afgehandeld.</p><p>Als het domein nog niet naar de nameservers van MCD wijst, stuurt Voltooien door naar <code>/workspace/organization/company/address</code> zodat de gebruiker direct de DNS-instructies en een <strong>Verifieren</strong>-knop ziet. Anders gaat het naar het dashboard.</p><p><strong>Registreer een domein:</strong> integreert de live <code>DomainPurchaseCard</code> + <code>DomainClaimModal</code> uit de instellingenomgeving. De gebruiker kan een domein zoeken, beschikbaarheid en prijs controleren, en het kopen via OpenProvider of gratis claimen als Founding Member. Bij een succesvolle claim of aankoop is het domein al server-side aan de werkruimte gekoppeld via de <code>/api/domain-purchase</code>-flow, dus de wizard slaat het antwoord op als <code>mode=&#39;own&#39;</code> met de geregistreerde naam en <code>registered: true</code>; <code>apply.service</code> behandelt het als een no-op hertoevoeging. Een groene succesbanner toont de geregistreerde domeinnaam en laat de gebruiker doorgaan.</p><p>Als de gebruiker de Registreer-route opent maar geen aankoop voltooit, wordt de stap gemarkeerd als overgeslagen zodat de wizard verder kan. Ze kunnen later terugkeren via <code>Bedrijf › Je eigen .com-adres</code> wanneer ze er klaar voor zijn.</p><h3 id="terugschakelen-van-een-eigen-domein-naar-een-subdomein" tabindex="-1">Terugschakelen van een eigen domein naar een subdomein <a class="header-anchor" href="#terugschakelen-van-een-eigen-domein-naar-een-subdomein" aria-label="Permalink to &quot;Terugschakelen van een eigen domein naar een subdomein&quot;">​</a></h3><p>Als de werkruimte al een eigen websitedomein heeft, toont de wizard een waarschuwing voordat de gebruiker naar het subdomeinpad kan overschakelen — <code>activateSubdomain</code> weigert op bedrijven met nog een aangepast domein, en de fout zou anders pas bij Voltooien zichtbaar worden.</p><h2 id="stap-4-—-magie" tabindex="-1">Stap 4 — Magie <a class="header-anchor" href="#stap-4-—-magie" aria-label="Permalink to &quot;Stap 4 — Magie&quot;">​</a></h2><p>Genereert merksuggesties op basis van de tot nu toe vastgelegde antwoorden. Velden:</p><ul><li><strong>Merkkleur</strong> — hex. Als de gebruiker in deze stap een logo uploadt, wordt de dominante niet-witte kleur uit de raster gehaald (via <code>sharp</code>) en gebruikt; dat overschrijft de Gemini-suggestie, en een vlag (<code>brandColorFromLogo</code>) voorkomt dat een latere hergeneratie het overschrijft. Alleen-SVG-logo&#39;s en overwegend witte invoer vallen terug op het Gemini-palet.</li><li><strong>Tagline</strong> — korte hero-regel voor de openbare bedrijfspagina.</li><li><strong>Over-tekst</strong> — paragraaf voor de bedrijfspagina.</li><li><strong>Diensten</strong> — lijst van maximaal 8 dienstnamen. Elk wordt bij Voltooien in <code>company_services</code> ingevoegd, maar <strong>alleen als het bedrijf nog nul diensten heeft</strong> — de wizard overschrijft nooit een bestaande dienstenlijst.</li><li><strong>E-mailtoon</strong> — <code>formal</code> / <code>friendly</code> / <code>casual</code>. Vertaalt naar de e-mailstijl: <code>formal → classic</code>, <code>friendly</code> en <code>casual → modern</code>.</li><li><strong>E-mailsjablonen</strong> — gesorteerd op sjabloontype (<code>invoice_default</code>, <code>reminder_default</code>, enz.), elk met <code>subject</code> + <code>body</code>. Opgeslagen via de e-mailsjabloonrepository bij Voltooien.</li></ul><p>De gebruiker kan elke suggestie inline bewerken voordat hij doorgaat. <strong>Doorgaan is uitgeschakeld</strong> totdat er suggesties zijn (minstens een keer op <strong>Suggesties genereren</strong> klikken).</p><h3 id="logo-upload" tabindex="-1">Logo-upload <a class="header-anchor" href="#logo-upload" aria-label="Permalink to &quot;Logo-upload&quot;">​</a></h3><p>Klikken op de upload-tegel laat de gebruiker een afbeelding kiezen tot 4 MB. Het bestand wordt als base64 data-URI naar <code>/onboarding/upload-logo</code> gestuurd, die:</p><ol><li>Het opslaat via het canonieke bedrijfslogopad.</li><li>De dominante niet-witte kleur retourneert, die automatisch op de merkkleursuggestie wordt toegepast.</li><li><code>answers.logoUploaded = true</code> zet, zodat de Controle-tekst zich aanpast (&quot;we gebruiken je logo&quot; in plaats van &quot;we maken een initialenlogo van je bedrijfsnaam&quot;) en de toepasstap de initialengenerator overslaat.</li></ol><p>Als de gebruiker al een logo had, wordt dat getoond als bestaande voorvertoning (&quot;je hebt al een logo&quot;) in plaats van een lege upload-CTA.</p><h2 id="stap-5-—-controle" tabindex="-1">Stap 5 — Controle <a class="header-anchor" href="#stap-5-—-controle" aria-label="Permalink to &quot;Stap 5 — Controle&quot;">​</a></h2><p>Alleen-lezen diff van elk veld dat de wizard zou wijzigen. Twee secties:</p><ul><li><strong>Wijzigingen</strong> — <code>huidig → nieuw</code> rijen. Bevat een staal voor <code>brandColor</code>. Alleen velden die de wizard schreef en die afwijken van de huidige bedrijfsrij verschijnen hier.</li><li><strong>Al ingesteld</strong> — velden die de wizard vastlegde maar die al overeenkomen met de bedrijfsrij.</li></ul><p>Getoonde velden: bedrijfsnaam, land, registratienummer, juridische naam, adres, merkkleur, tagline, over-tekst, domeinoverzicht (<code>info@&lt;opgelost-domein&gt;</code> wordt getoond als een domein is ingesteld).</p><p>De <strong>Voltooien</strong>-knop in de footer roept <code>/onboarding/complete</code> aan. De huidige locale wordt doorgestuurd zodat standaardtekst (bijv. betalingsinstructies) gelokaliseerd is.</p><h2 id="wat-voltooien-daadwerkelijk-toepast" tabindex="-1">Wat Voltooien daadwerkelijk toepast <a class="header-anchor" href="#wat-voltooien-daadwerkelijk-toepast" aria-label="Permalink to &quot;Wat Voltooien daadwerkelijk toepast&quot;">​</a></h2><p><code>apply.service.js</code> doorloopt de antwoorden en schrijft ze in de echte bedrijfsrij. Twee semantieken, bewust gesplitst:</p><p><strong>Altijd overschrijven</strong> wanneer het wizard-antwoord een niet-lege string is en afwijkt van de huidige waarde:</p><ul><li><code>display_name</code>, <code>company_name</code></li><li><code>country</code> (valt terug op <code>&quot;NL&quot;</code> als <code>answers.country</code> niet is ingesteld , het 2-staps wizardpad vraagt geen land, dus NL is de impliciete standaard)</li><li><code>chamber</code> (via <code>answers.kvk.kvkNumber</code> indien aanwezig)</li></ul><p>Als de 2-staps wizard geen domein-antwoord heeft vastgelegd, maakt Finish automatisch een werkruimte-subdomein aan op basis van de <code>display_name</code>-waarde wanneer het abonnement subdomeinen toestaat en het bedrijf geen eigen domein heeft. De slug wordt afgeleid van <code>display_name</code> (kleine letters, ASCII, max 60 tekens, opnieuw geprobeerd met <code>-2</code>…<code>-5</code> achtervoegsel bij botsing).</p><ul><li><code>chamber</code>, <code>address</code>, <code>postal_code</code>, <code>city</code></li><li><code>brand_color</code>, <code>description</code>, <code>business_page_hero_tagline</code></li></ul><p><strong>Alleen lege velden vullen</strong> (nooit een bestaande handmatige keuze overschrijven, zodat herstarts gebruikers niet verrassen):</p><ul><li><code>second_accent_color</code> (afgeleid van merkkleur indien leeg)</li><li><code>email_style</code> (van e-mailtoon)</li><li><code>timezone</code> (landstandaard)</li><li><code>pdf_language</code> (landstandaard)</li><li><code>footer_text</code> (stijl <code>Handelsnaam - KvK 12345678</code>)</li><li><code>payment_options_enabled</code> (<code>bank_transfer</code>)</li><li><code>payment_default_method</code> (<code>bank_transfer</code>)</li><li><code>payment_instructions</code> (gelokaliseerde standaardtekst)</li><li><code>email_footer_show_website</code>, <code>email_footer_show_support_email</code>, <code>email_footer_show_business_page</code> (booleans)</li></ul><p>Neveneffecten buiten kolomschrijvingen:</p><ul><li><strong>Initialenlogo</strong> wordt gegenereerd uit bedrijfsnaam + merkkleur, maar alleen als <code>logo_path</code> en <code>logo_svg</code> beide leeg zijn. Volledig overgeslagen als de gebruiker een echt logo uploadde in de Magie-stap.</li><li><strong>Diensten</strong> — tot 8 invoegingen in <code>company_services</code>, alleen als het bedrijf er nog geen heeft.</li><li><strong>E-mailsjablonen</strong> — per type opgeslagen via de sjabloonrepository.</li><li><strong>Website-site-seed</strong> — bij de eerste Voltooien die nul pagina&#39;s in de werkruimte vindt, maakt <code>apply.service.js</code> een standaardsite met een concept-startpagina (<code>/</code>, template &quot;home&quot;, <code>is_home: true</code>) en vult de ontwerptokens met de merkkleur uit de wizard. Herhaalde uitvoeringen slaan het aanmaken over wanneer er al een pagina bestaat, zodat de wizard nooit handmatige aanpassingen overschrijft.</li><li><strong>Domein</strong> — <code>activateSubdomain</code> voor het subdomeinpad, of <code>addDomain</code> + <code>quickEnableInbox</code> (+ optionele persoonlijke mailbox) voor het eigen-domeinpad.</li></ul><p>Als domeintoewijzing mislukt, wordt de rest van Voltooien nog steeds toegepast — de fout verschijnt op het Voltooien-scherm met een specifieke foutcode (<code>subdomain_failed</code>, <code>domain_failed</code>, <code>inbox_enable_failed</code>, <code>personal_mailbox_failed</code>, <code>personal_mailbox_remove_failed</code>, <code>personal_mailbox_list_failed</code>) vertaald naar een gebruikersleesbare regel.</p><h2 id="voltooien-scherm" tabindex="-1">Voltooien-scherm <a class="header-anchor" href="#voltooien-scherm" aria-label="Permalink to &quot;Voltooien-scherm&quot;">​</a></h2><p>Een groen bevestigingspaneel dat 4,2 seconden (of 0,9 seconden wanneer er niets is toegewezen) wordt getoond voordat het doorverwijst. Het toont elk item dat de wizard heeft ingesteld in een overzichtskaartlijst:</p><ul><li><strong>Categorieen</strong> — de systeem-uitgavencategorieen die voor de werkruimte zijn aangemaakt (bijv. &quot;Kantoor, Reizen, Software&quot;).</li><li><strong>Diensten</strong> — aantal diensten toegevoegd aan de bedrijfspagina.</li><li><strong>Logo</strong> — bevestiging wanneer een initialenlogo is gegenereerd.</li><li><strong>E-mailsjablonen</strong> — aantal sjablonen opgeslagen in de merktone van de werkruimte.</li><li><strong>Website</strong> — bevestiging dat een standaardsite met startpagina, navigatie en merkkleur klaarstaat. Alleen zichtbaar wanneer de site net is aangemaakt.</li><li><strong>Domein</strong> — de kant-en-klare URL voor subdomeinen of het toegevoegde domein voor eigen-domeinconfiguraties.</li></ul><p>Twee knoppen verschijnen onder het overzicht:</p><ul><li><strong>Naar dashboard</strong> — brengt de gebruiker direct naar <code>/dashboard</code>, de auto-doorverwijstimer overslaand.</li><li><strong>Open de website-bouwer</strong> — alleen zichtbaar wanneer een site is aangemaakt. Brengt de gebruiker rechtstreeks naar <code>/website</code> zodat ze hun startpagina kunnen gaan bewerken.</li></ul><p>Daarna komt de gebruiker op <code>/dashboard</code> (standaardpad), <code>/website</code> (wanneer ze op de website-bouwer-CTA klikken), of <code>/workspace/organization/company/address</code> (wanneer een eigen domein in afwachting is van DNS-verificatie).</p><h2 id="de-wizard-opnieuw-doorlopen" tabindex="-1">De wizard opnieuw doorlopen <a class="header-anchor" href="#de-wizard-opnieuw-doorlopen" aria-label="Permalink to &quot;De wizard opnieuw doorlopen&quot;">​</a></h2><p>De wizard is volledig herhaalbaar. Terugkerende gebruikers landen op stap 1 met hun huidige antwoorden zichtbaar. Niets dwingt ze door elke stap — ze kunnen een veld op een stap bewerken en op Voltooien klikken.</p><p>De diff van de Controle-stap is het vangnet: het toont de gebruiker elke overschrijving die gaat gebeuren. Zinvolle-standaardvelden (tijdzone, pdf_language, betalingsstandaarden, footer-tekst) staan niet in de diff omdat Voltooien daar alleen lege velden vult — ze stilletjes omdraaien zou gebruikers verrassen die ze bewust hebben ingesteld.</p><p>Om individuele instellingen te herzien zonder de wizard, ga naar:</p><ul><li><code>/workspace/organization/company/about</code> — naam, registratie, adres, btw.</li><li><code>/workspace/organization/company/look</code> — merkkleur, logo.</li><li><code>/workspace/organization/company/website</code> — tagline, over-tekst, diensten.</li><li><code>/workspace/organization/company/address</code> — eigen domein + DNS.</li><li><code>/workspace/email</code> — inbox, mailboxen, sjablonen.</li></ul><p>Het volledige overzicht vind je in het <a href="/nl/settings/">overzicht van de instellingen</a>.</p><h2 id="randgevallen" tabindex="-1">Randgevallen <a class="header-anchor" href="#randgevallen" aria-label="Permalink to &quot;Randgevallen&quot;">​</a></h2><ul><li><strong>Een stap overslaan.</strong> Doorgaan is per stap beveiligd op de minimaal vereiste antwoorden. De Register-stap heeft geen poort; Domein vereist een gekozen pad met een niet-lege waarde, of een voltooide aankoop bij de Registreer-route, of de overgeslagen-vlag; Magie vereist dat Genereren is uitgevoerd; Bedrijf en Controle hebben hun eigen poorten.</li><li><strong>Sluiten midden in een stap.</strong> Elk antwoord wordt bij wijziging opgeslagen, dus het volgende bezoek gaat verder waar de gebruiker was gebleven. Stapindex wordt ook opgeslagen (<code>answers</code> en <code>currentStep</code> leven in dezelfde JSONB-kolom).</li><li><strong>Van gedachten veranderen op de Domein-stap.</strong> Overschakelen van <code>eigen</code> naar <code>subdomein</code> na het typen van een domein overschrijft <code>answers.domain</code> naar <code>null</code> totdat de gebruiker een slug kiest. Overschakelen naar de Registreer-route slaat een overgeslagen-antwoord op zodat een nieuwe aanmelding niet vastloopt als ze Registreer openen maar de aankoop uitstellen. Overschakelen naar een subdomein wanneer er al een aangepast domein is, toont een waarschuwing vooraf.</li><li><strong>Logo-extractie mislukt.</strong> Overwegend witte logo&#39;s en alleen-SVG-invoer die <code>sharp</code> niet kan rasteren retourneren <code>color: null</code>. De Gemini-merkkleursuggestie wordt dan gebruikt.</li><li><strong>Domein al toegevoegd bij eigen-domein Voltooien.</strong> Een 409 van <code>addDomain</code> valt terug op de bestaande rij zodat de inbox-inschakelstap nog steeds wordt uitgevoerd.</li><li><strong>Persoonlijke mailbox bestaat al.</strong> Een 409 van <code>createMailbox</code> wordt als succes behandeld.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("nl/features/setup-wizard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const setupWizard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  setupWizard as default
};
