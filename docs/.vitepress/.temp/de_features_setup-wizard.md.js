import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Einrichtungsassistent","description":"","frontmatter":{"title":"Einrichtungsassistent","last_verified":"2026-05-19T00:00:00.000Z"},"headers":[],"relativePath":"de/features/setup-wizard.md","filePath":"de/features/setup-wizard.md"}');
const _sfc_main = { name: "de/features/setup-wizard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="einrichtungsassistent" tabindex="-1">Einrichtungsassistent <a class="header-anchor" href="#einrichtungsassistent" aria-label="Permalink to &quot;Einrichtungsassistent&quot;">​</a></h1><p>Ein magisches Setup in fünf Schritten unter <code>/setup</code>, das Ihre Unternehmensidentitat erfasst, optional Ihre Registrierung vom Handelsregister oder einem EU-Register abruft, eine Webadresse auswahlt, Markenvorschlage generiert und alles mit einem Klick anwendet. Der Assistent ist die Eingangstur fur Erstbenutzer und bleibt danach fur Anpassungen verfugbar.</p><p>Wenn Sie hier die grundlegende Einfuhrung suchen, beginnen Sie bei <a href="/de/getting-started/company-setup">Unternehmen einrichten</a>. Diese Seite ist die ausfuhrliche Referenz: jeder Schritt, jede Option, jede Konsequenz.</p><h2 id="wann-der-assistent-angeboten-wird" tabindex="-1">Wann der Assistent angeboten wird <a class="header-anchor" href="#wann-der-assistent-angeboten-wird" aria-label="Permalink to &quot;Wann der Assistent angeboten wird&quot;">​</a></h2><p>Der Assistent ist auf zwei Wegen erreichbar:</p><ul><li><strong>Direkt:</strong> Navigieren Sie jederzeit zu <code>/setup</code>.</li><li><strong>Dashboard-Banner:</strong> Solange das Onboarding nicht abgeschlossen ist, erscheint ein schließbares &quot;Setup abschließen&quot;-Banner oben auf <code>/dashboard</code>. Es zeigt eine Zählung der ausstehenden Felder (oder einen generischen &quot;Profil vervollständigen&quot;-Text, wenn die Zählung null ist) und eine Schaltfläche zurück zum Assistenten. Eine Schließen-Schaltfläche (X-Symbol) blendet das Banner pro Browser über localStorage aus, sodass es über Seitenneuladungen hinweg ausgeblendet bleibt, bis der Assistent abgeschlossen ist.</li></ul><p>Der Assistent ist nicht blockierend: Die alte erzwungene Weiterleitung zu <code>/setup</code> bei der Anmeldung (die <code>ONBOARDING_MANDATORY</code>-Sperre) wurde entfernt. Neue Anmeldungen landen direkt auf <code>/dashboard</code> und sehen stattdessen das schließbare Banner.</p><h2 id="seitenaufbau" tabindex="-1">Seitenaufbau <a class="header-anchor" href="#seitenaufbau" aria-label="Permalink to &quot;Seitenaufbau&quot;">​</a></h2><p>Der Assistent ist eine einzelne Seite (<code>/setup</code>) mit fünf Schritten in einer Schrittleiste. Jeder Schritt schreibt seine Antworten bei jeder Änderung in eine JSONB-Spalte (<code>companies.onboarding_state</code>), sodass der Assistent vollstandig fortsetzbar ist: Schliessen Sie den Tab mitten im Schritt und der nachste Besuch bringt Sie zum selben Schritt mit denselben ausgefullten Antworten.</p><p>Drei Schaltflachen sitzen unten auf jedem Schritt:</p><ul><li><strong>Zuruck</strong> — zum vorherigen Schritt (bei Schritt 1 ausgeblendet).</li><li><strong>Jetzt uberspringen</strong> — verlasst den Assistenten, ohne das Onboarding als abgeschlossen zu markieren. Das Dashboard-Banner bleibt sichtbar, damit der Benutzer zuruckkehren kann.</li><li><strong>Weiter</strong> / <strong>Abschliessen</strong> — pro Schritt durch die unten stehenden Antworten gesteuert.</li></ul><p>Es gibt kein &quot;Speichern und Beenden&quot; — das Speichern erfolgt automatisch. <strong>Jetzt uberspringen</strong> ist ein sauberer Ausstieg, kein Verwerfen.</p><h2 id="schritt-1-—-unternehmen" tabindex="-1">Schritt 1 — Unternehmen <a class="header-anchor" href="#schritt-1-—-unternehmen" aria-label="Permalink to &quot;Schritt 1 — Unternehmen&quot;">​</a></h2><p>Erfasst die zwei Antworten, von denen jeder spatere Schritt abhangt.</p><table tabindex="0"><thead><tr><th>Feld</th><th>Gespeichert als</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Erforderlich</th><th>Hinweise</th></tr></thead><tbody><tr><td>Unternehmensname</td><td><code>answers.businessName</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td><td>Wird als <code>display_name</code> und <code>company_name</code> bei Abschluss verwendet (der rechtliche Name aus dem Registerschritt gewinnt fur <code>company_name</code>, wenn beide vorhanden sind). Befullt auch den Subdomain-Slug-Vorschlag in Schritt 3.</td></tr><tr><td>Was Sie tun</td><td><code>answers.what</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td><td>Freitext, 1–2 Satze. Wird zusammen mit Land und Branche an den Vorschlagsgenerator (Gemini) des magischen Schritts gesendet.</td></tr></tbody></table><p><strong>Weiter ist deaktiviert</strong>, bis beide Felder nicht-leere, bereinigte Werte haben.</p><h2 id="schritt-2-—-register" tabindex="-1">Schritt 2 — Register <a class="header-anchor" href="#schritt-2-—-register" aria-label="Permalink to &quot;Schritt 2 — Register&quot;">​</a></h2><p>Wählt ein Land und sucht das Unternehmen entweder im offiziellen Register des Landes, füllt die Angaben von Hand aus, oder überspringt den Schritt.</p><h3 id="drei-wege" tabindex="-1">Drei Wege <a class="header-anchor" href="#drei-wege" aria-label="Permalink to &quot;Drei Wege&quot;">​</a></h3><ol><li><strong>Suche:</strong> Typeahead nach Firmennamen, Treffer auswählen und das Basisprofil vom Backend abrufen lassen (EUR 0,02 pro Abruf für NL). Das Suchfeld wird nur angezeigt, wenn die kostenpflichtige KVK-API (<code>KVK_API_KEY</code>) auf dem API-Container konfiguriert ist. Fehlt der Schlüssel, wird die Suche übersprungen und der Assistent startet standardmäßig mit manueller Eingabe (Weg 2).</li><li><strong>Manuell:</strong> Firmennamen, Handelsregisternummer (optional), Adresse, Postleitzahl und Ort selbst eingeben. Die Daten werden direkt über <code>PUT /company-settings/company</code> in die Unternehmenszeile geschrieben und <code>answers.kvk</code> wird mit <code>manual: true</code> markiert. Die manuelle Eingabe ist der Standard, wenn KVK_API_KEY nicht gesetzt ist, und existiert auch für zwei zusätzliche Szenarien selbst wenn die Suche verfügbar ist: (a) neue Unternehmen, die noch nicht im kostenlosen OpenKVK-Datensatz enthalten sind, und (b) Unternehmen, deren Handelsname nicht mit der Sucheingabe des Benutzers übereinstimmt.</li><li><strong>Überspringen</strong> — &quot;Kein Handelsregistereintrag?&quot; speichert <code>answers.kvk = null</code>. Der Assistent fährt fort; Unternehmensdaten können später in den Einstellungen ergänzt werden.</li></ol><p>Der Wechsel zwischen Suche und manueller Eingabe erfolgt mit einem Klick (wenn die Suche verfügbar ist): Eine &quot;Manuell ausfüllen&quot;-Schaltfläche erscheint unter den Suchergebnissen, und ein &quot;Zurück zur KVK-Suche&quot;-Link sitzt oben im manuellen Formular. Wenn <code>KVK_API_KEY</code> fehlt, ist die Zurück-zur-Suche-Schaltfläche vollständig ausgeblendet und der Benutzer beginnt und bleibt auf manueller Eingabe.</p><h3 id="landoptionen" tabindex="-1">Landoptionen <a class="header-anchor" href="#landoptionen" aria-label="Permalink to &quot;Landoptionen&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Code</th><th>Angezeigtes Register</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Suche aktiv</th></tr></thead><tbody><tr><td><code>NL</code></td><td>KvK</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>FR</code></td><td>SIRENE</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>GB</code></td><td>Companies House</td><td style="${ssrRenderStyle({ "text-align": "center" })}">ja</td></tr><tr><td><code>DE</code></td><td>Handelsregister</td><td style="${ssrRenderStyle({ "text-align": "center" })}">nein — manuelle Eingabe</td></tr><tr><td><code>OTHER</code></td><td>(kein Register)</td><td style="${ssrRenderStyle({ "text-align": "center" })}">nein — manuelle Eingabe</td></tr></tbody></table><p><code>NL</code> ist die Voreinstellung. Das Land bestimmt mehrere Dinge beim Abschluss: Zeitzone (<code>Europe/Amsterdam</code> / <code>Europe/Berlin</code> / <code>Europe/Paris</code> / <code>Europe/London</code>), <code>pdf_language</code> (<code>nl</code> / <code>de</code> / <code>fr</code> / <code>en</code>) und das Registerlabel in <code>footer_text</code> (<code>KvK 12345678</code>, <code>Handelsregister …</code>, <code>SIRENE …</code>, <code>CRN …</code>).</p><h3 id="suchmodus" tabindex="-1">Suchmodus <a class="header-anchor" href="#suchmodus" aria-label="Permalink to &quot;Suchmodus&quot;">​</a></h3><p>Für unterstützte Länder sucht der Benutzer nach dem Firmennamen. Das Backend ruft die entsprechende öffentliche API auf und liefert einen der folgenden Rückgabewerte:</p><ul><li><strong>ok</strong> — <code>legalName</code>, <code>address</code>, <code>postalCode</code>, <code>city</code>, <code>sector</code> befullt und unter <code>answers.registry</code> gespeichert.</li><li><strong>not-configured</strong> — Anbieter in dieser Umgebung noch nicht angeschlossen; der Benutzer wird aufgefordert, zur manuellen Eingabe zu wechseln.</li><li><strong>not-found</strong> — Nummer nicht gefunden; Benutzer kann erneut versuchen oder manuell wechseln.</li><li><strong>invalid</strong> — Nummernformat abgelehnt.</li><li><strong>error</strong> — Anbieterfehler; manuelle Eingabe bleibt eine Option.</li></ul><h4 id="kvk-basisprofiel-nur-nl" tabindex="-1">KvK Basisprofiel (nur NL) <a class="header-anchor" href="#kvk-basisprofiel-nur-nl" aria-label="Permalink to &quot;KvK Basisprofiel (nur NL)&quot;">​</a></h4><p>Fur NL-Workspaces ist die KvK-Suche ein zweistufiger Prozess:</p><ol><li><strong>Typeahead</strong> — der Benutzer sucht nach Unternehmensnamen. Der <code>zoeken</code>-Endpunkt (kostenlos) gibt ubereinstimmende Eintrage zuruck. Dies ist der Autocomplete-Schritt, der die bestehenden <code>ok</code>- / <code>not-found</code>-Antworten antreibt. Wenn die Suche null Treffer liefert, zeigt die UI ein Inline-Panel fur leere Ergebnisse an (Titel, Erklarung und ein &quot;Manuell eintragen&quot;-Button, der das manuelle Formular mit dem bereits eingegebenen Text vorausfullt). Dies kommt haufig vor, da die kostenlose OpenKVK-Stufe viele junge Unternehmen nicht erfasst.</li><li><strong>Basisprofiel</strong> — sobald ein Treffer ausgewahlt ist, ruft der Assistent den KvK-Basisprofiel-Detailendpunkt auf. Dies ist ein kostenpflichtiger Aufruf (EUR 0,02, 24h zwischengespeichert pro KVK-Nummer). Er gibt das vollstandige Profil zuruck: <code>legalName</code>, <code>statutaireNaam</code> (satzungsmassiger Name), <code>tradeNames</code> (alle registrierten Handelsnamen, sortiert), <code>rsin</code>, <code>legalForm</code>, <code>dateFounded</code>, Besuchs- und Postadressen, SBI-Codes mit Primarflag, <code>employeeCount</code> und <code>indNonMailing</code> (Keine-Post-Flag). Der Assistent befullt <code>business_page_hero_tagline</code> mit dem ersten Handelsnamen und der Stadt (&quot;[Handelsname] in [Stadt]&quot;), und <code>description</code> mit der primaren SBI-Beschreibung (auf 280 Zeichen begrenzt). Beide verwenden COALESCE, sodass manuelle Bearbeitungen in den Einstellungen erhalten bleiben.</li></ol><p>Wenn ein Treffer ausgewahlt wird (Suchmodus), ruft das Frontend sofort <code>PUT /company-settings/company</code> mit dem <code>name</code> des Treffers als <code>display_name</code>, der <code>kvkNumber</code> als <code>chamber</code>, <code>city</code> und <code>country</code> hartcodiert auf <code>&quot;NL&quot;</code> auf. Dies schreibt den kundenorientierten Anzeigenamen sofort, sodass die Dashboard-Begrußung und die geseedete Site den richtigen Namen ab dem Moment verwenden, in dem der Benutzer auf den Treffer klickt, anstatt auf den Abschluss-Schritt des Assistenten zu warten.</p><p>Es gibt zwei unabhängige Feature-Flags für NL-KVK-Abfragen:</p><ul><li><code>KVK_API_KEY</code> (Env-Var auf dem API-Container): Wenn dieser fehlt, wird das Suchfeld gar nicht angezeigt und der Assistent startet in diesem Schritt standardmäßig mit manueller Eingabe. Die kostenlose OpenKVK-Stufe allein ist zu lückenhaft (~2% Trefferquote, verpasst fast jede Neuanmeldung). Das Flag schaltet automatisch um, sobald der Schlüssel gesetzt ist.</li><li><code>KVK_BASISPROFIEL_ENABLED</code>: Wenn deaktiviert (oder wenn <code>KVK_API_KEY</code> fehlt), verwendet der Assistent nur das kostenlose <code>zoeken</code>-Ergebnis (dieselben <code>ok</code>-Felder wie oben). Wenn aktiviert und <code>KVK_API_KEY</code> vorhanden ist, reichern die Basisprofiel-Daten die <code>answers.registry</code>-Payload an und der Überprüfungsschritt zeigt jedes Feld, das in die Unternehmenszeile geschrieben wird.</li></ul><h3 id="manueller-modus" tabindex="-1">Manueller Modus <a class="header-anchor" href="#manueller-modus" aria-label="Permalink to &quot;Manueller Modus&quot;">​</a></h3><p>Benutzer fullt <code>chosen</code> (seine Registrierungsnummer) aus, und optional <code>legalName</code>, <code>address</code>, <code>sector</code>. Alle vier Felder sind in diesem Modus optional.</p><p>Beim Speichern ruft das Frontend <code>PUT /company-settings/company</code> direkt mit den manuell eingegebenen Werten plus <code>display_name</code> gesetzt auf den hier eingegebenen Firmennamen und <code>country</code> hartcodiert auf <code>&quot;NL&quot;</code> auf. Dies schreibt sowohl den kundenorientierten Namen als auch das Land sofort, anstatt auf den Abschluss-Schritt zu warten. Die Apply-Logik des Abschluss-Schritts liest weiterhin <code>answers.kvk</code> fur den Legacy-Registerpfad, aber der neue 2-Schritt-Assistentenpfad liest <code>answers.kvk.legalName</code> und <code>answers.kvk.kvkNumber</code> direkt, sodass das inline PUT wahrend dieses Schritts der autoritative Schreibvorgang ist.</p><h3 id="uberspringen-modus" tabindex="-1">Überspringen-Modus <a class="header-anchor" href="#uberspringen-modus" aria-label="Permalink to &quot;Überspringen-Modus&quot;">​</a></h3><p>Ein &quot;Noch nicht registriert&quot;-Schalter speichert <code>answers.registry = null</code>. <strong>Weiter ist in diesem Schritt immer erlaubt</strong>, unabhangig vom Modus — Registerdaten sind Bequemlichkeit, keine Hürde.</p><h2 id="schritt-3-—-domain" tabindex="-1">Schritt 3 — Domain <a class="header-anchor" href="#schritt-3-—-domain" aria-label="Permalink to &quot;Schritt 3 — Domain&quot;">​</a></h2><p>Wahlen Sie die Webadresse, die Ihre Kunden auf der offentlichen Unternehmensseite und in Ihrem E-Mail-Posteingang sehen. Drei Wege, als Karten angezeigt, decken jeden Pfad ab, vom kostenlosen Schnellstart bis zum Kauf einer Domain direkt im Assistenten.</p><h3 id="drei-wege-auswahl" tabindex="-1">Drei-Wege-Auswahl <a class="header-anchor" href="#drei-wege-auswahl" aria-label="Permalink to &quot;Drei-Wege-Auswahl&quot;">​</a></h3><p>Ein Raster von drei Karten prasentiert die Wahl. Die Auswahl eines Weges zeigt den zugehorigen Editor darunter; es ist immer nur ein Weg gleichzeitig aktiv.</p><p><strong>Subdomain (kostenlos):</strong> der Benutzer wahlt einen Slug; eine TLD-Auswahl lasst zwischen <code>.mycompanydesk.nl</code> und <code>.mycompanydesk.com</code> wahlen. Der Slug ist aus dem rechtlichen KVK-Namen vorbefullt, wenn verfugbar (Kleinbuchstaben, Akzente entfernt, Nicht-ASCII-Zeichen entfernt, auf 63 Zeichen gekurzt), sodass die meisten Benutzer tippen-und-weitermachen konnen, ohne etwas einzugeben. Die Verfugbarkeit wird live mit einer Verzogerung von 350 ms gepruft, wahrend der Benutzer tippt. Beim Abschluss wird die Subdomain uber die Cloudflare-API bereitgestellt und die Unternehmenswebsite ist sofort erreichbar.</p><p>Wenn der Assistent im 2-Schritt (Plan-gesteuerten) Ablauf ausgefuhrt wird, wird der Domain-Schritt vollstandig weggelassen. Der Abschluss-Schritt stellt automatisch eine Workspace-Subdomain aus dem <code>display_name</code>-Wert bereit: Der Slug wird aus dem Anzeigenamen abgeleitet (mit Wiederholung-bei-Kollision-Suffixen bis zu 5 Versuche), und <code>activateSubdomain</code> registriert ihn als offentliche Site-URL. Best-Effort: Eine Kollision oder ein Fehler wird protokolliert und blockiert den Assistenten nicht am Abschluss.</p><p><strong>Eigene Domain:</strong> der Benutzer gibt eine Domain ein, die er bereits besitzt. Eine Live-Validierungs-Regex pruft das Format wahrend der Eingabe (<code>[a-z0-9][a-z0-9-]*(.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+</code>). Beim Abschluss fugt der Assistent die Domain zur Domainliste des Workspace hinzu (keine Aktion, wenn sie bereits hinzugefugt wurde) und aktiviert automatisch den Posteingang: <code>info@&lt;domain&gt;</code> als Standard-Postfach plus <code>support@</code>, <code>sales@</code> und <code>noreply@</code>-Aliase. Der 409-bereits-vorhanden-Pfad von <code>apply.service</code> wird sauber behandelt.</p><p>Wenn die Domain noch nicht auf die Nameserver von MCD zeigt, leitet der Abschluss zu <code>/workspace/organization/company/address</code> weiter, sodass der Benutzer sofort die DNS-Anweisungen und eine <strong>Uberprufen</strong>-Schaltflache sieht. Andernfalls geht es zum Dashboard.</p><p><strong>Domain registrieren:</strong> bettet die live <code>DomainPurchaseCard</code> + <code>DomainClaimModal</code> aus der Einstellungsoberflache ein. Der Benutzer kann eine Domain suchen, Verfugbarkeit und Preis prufen und sie entweder uber OpenProvider kaufen oder als Founding Member kostenlos beanspruchen. Bei erfolgreicher Beanspruchung oder Kauf ist die Domain bereits server-seitig uber den <code>/api/domain-purchase</code>-Workflow mit dem Workspace verbunden, sodass der Assistent die Antwort als <code>mode=&#39;own&#39;</code> mit dem registrierten Namen und <code>registered: true</code> speichert; <code>apply.service</code> behandelt dies als No-Op-Wiederhinzufugung. Ein grunes Erfolgsbanner zeigt den registrierten Domainnamen an und lasst den Benutzer fortfahren.</p><p>Wenn der Benutzer den Registrieren-Weg offnet, aber keinen Kauf abschliesst, wird der Schritt als ubersprungen markiert, damit der Assistent fortfahren kann. Der Benutzer kann spater uber <code>Unternehmen &gt; Ihre eigene .com-Adresse</code> zuruckkehren, wann immer er bereit ist.</p><h3 id="zuruckwechseln-von-einer-eigenen-domain-zu-einer-subdomain" tabindex="-1">Zuruckwechseln von einer eigenen Domain zu einer Subdomain <a class="header-anchor" href="#zuruckwechseln-von-einer-eigenen-domain-zu-einer-subdomain" aria-label="Permalink to &quot;Zuruckwechseln von einer eigenen Domain zu einer Subdomain&quot;">​</a></h3><p>Wenn der Workspace bereits eine eigene Website-Domain hat, zeigt der Assistent eine Warnung, bevor der Benutzer zum Subdomain-Pfad wechseln kann — <code>activateSubdomain</code> verweigert bei Unternehmen mit noch angehangter eigener Domain, und der Fehler wurde sonst erst beim Abschluss sichtbar.</p><h2 id="schritt-4-—-magie" tabindex="-1">Schritt 4 — Magie <a class="header-anchor" href="#schritt-4-—-magie" aria-label="Permalink to &quot;Schritt 4 — Magie&quot;">​</a></h2><p>Generiert Markenvorschlage aus den bisher erfassten Antworten. Felder:</p><ul><li><strong>Markenfarbe</strong> — Hex. Wenn der Benutzer in diesem Schritt ein Logo hochladt, wird die dominante nicht-weisse Farbe aus dem Raster extrahiert (via <code>sharp</code>) und verwendet; das uberschreibt den Gemini-Vorschlag, und ein Flag (<code>brandColorFromLogo</code>) verhindert, dass eine spatere Neugenerierung ihn uberschreibt. Nur-SVG-Logos und uberwiegend weisse Eingaben fallen auf die Gemini-Palette zuruck.</li><li><strong>Tagline</strong> — kurze Hero-Zeile fur die offentliche Unternehmensseite.</li><li><strong>Über-Text</strong> — Absatz fur die Unternehmensseite.</li><li><strong>Dienstleistungen</strong> — Liste von bis zu 8 Dienstleistungsnamen. Jede wird beim Abschluss in <code>company_services</code> eingefugt, aber <strong>nur wenn das Unternehmen noch keine Dienstleistungen hat</strong> — der Assistent uberschreibt niemals eine bestehende Dienstleistungsliste.</li><li><strong>E-Mail-Ton</strong> — <code>formal</code> / <code>friendly</code> / <code>casual</code>. Wird auf den E-Mail-Stil abgebildet: <code>formal → classic</code>, <code>friendly</code> und <code>casual → modern</code>.</li><li><strong>E-Mail-Vorlagen</strong> — nach Vorlagentyp (<code>invoice_default</code>, <code>reminder_default</code> usw.), jeweils mit <code>subject</code> + <code>body</code>. Beim Abschluss uber das E-Mail-Vorlagen-Repository gespeichert.</li></ul><p>Der Benutzer kann jeden Vorschlag vor dem Fortfahren inline bearbeiten. <strong>Weiter ist deaktiviert</strong>, bis Vorschlage existieren (mindestens einmal auf <strong>Vorschlage generieren</strong> klicken).</p><h3 id="logo-upload" tabindex="-1">Logo-Upload <a class="header-anchor" href="#logo-upload" aria-label="Permalink to &quot;Logo-Upload&quot;">​</a></h3><p>Durch Klicken auf die Upload-Kachel kann der Benutzer ein Bild bis zu 4 MB auswahlen. Die Datei wird als base64 data URI an <code>/onboarding/upload-logo</code> gesendet, die:</p><ol><li>Es uber den kanonischen Unternehmenslogo-Pfad speichert.</li><li>Die dominante nicht-weisse Farbe zuruckgibt, die automatisch auf den Markenfarbvorschlag angewendet wird.</li><li><code>answers.logoUploaded = true</code> setzt, damit sich der Überprüfungstext anpasst (&quot;wir verwenden Ihr Logo&quot; statt &quot;wir erstellen ein Initialenlogo aus Ihrem Unternehmensnamen&quot;) und der Anwendungsschritt den Initialengenerator uberspringt.</li></ol><p>Wenn der Benutzer bereits ein Logo hatte, wird es als bestehende Vorschau (&quot;Sie haben bereits ein Logo&quot;) statt einer leeren Upload-CTA angezeigt.</p><h2 id="schritt-5-—-uberprufung" tabindex="-1">Schritt 5 — Überprufung <a class="header-anchor" href="#schritt-5-—-uberprufung" aria-label="Permalink to &quot;Schritt 5 — Überprufung&quot;">​</a></h2><p>Schreibgeschutzter Diff jedes Feldes, das der Assistent andern wurde. Zwei Abschnitte:</p><ul><li><strong>Änderungen</strong> — <code>aktuell → neu</code> Zeilen. Enthalt ein Farbfeld fur <code>brandColor</code>. Nur Felder, die der Assistent geschrieben hat und die von der aktuellen Unternehmenszeile abweichen, erscheinen hier.</li><li><strong>Bereits gesetzt</strong> — Felder, die der Assistent erfasst hat, aber bereits mit der Unternehmenszeile ubereinstimmen.</li></ul><p>Angezeigte Felder: Unternehmensname, Land, Registrierungsnummer, rechtlicher Name, Adresse, Markenfarbe, Tagline, Über-Text, Domain-Übersicht (<code>info@&lt;aufgeloste-domain&gt;</code> wird als Vorschau angezeigt, wenn eine Domain gesetzt ist).</p><p>Die <strong>Abschliessen</strong>-Schaltflache in der Fusszeile ruft <code>/onboarding/complete</code> auf. Die aktuelle Sprache wird weitergeleitet, damit Standardtext (z. B. Zahlungsanweisungen) lokalisiert ist.</p><h2 id="was-abschliessen-tatsachlich-anwendet" tabindex="-1">Was Abschliessen tatsachlich anwendet <a class="header-anchor" href="#was-abschliessen-tatsachlich-anwendet" aria-label="Permalink to &quot;Was Abschliessen tatsachlich anwendet&quot;">​</a></h2><p><code>apply.service.js</code> durchlauft die Antworten und schreibt sie in die echte Unternehmenszeile. Zwei Semantiken, bewusst getrennt:</p><p><strong>Immer uberschreiben</strong>, wenn die Assistent-Antwort ein nicht-leerer String ist und vom aktuellen Wert abweicht:</p><ul><li><code>display_name</code>, <code>company_name</code></li><li><code>country</code> (fallt auf <code>&quot;NL&quot;</code> zuruck, wenn <code>answers.country</code> nicht gesetzt ist , der 2-Schritt-Assistent fragt kein Land ab, daher ist NL die implizite Voreinstellung)</li><li><code>chamber</code> (via <code>answers.kvk.kvkNumber</code>, wenn vorhanden)</li></ul><p>Wenn der 2-Schritt-Assistent keine Domain-Antwort erfasst hat, stellt Finish automatisch eine Workspace-Subdomain aus dem <code>display_name</code>-Wert bereit, wenn der Tarif Subdomains erlaubt und das Unternehmen keine eigene Domain hat. Der Slug wird aus <code>display_name</code> abgeleitet (Kleinbuchstaben, ASCII, max. 60 Zeichen, bei Kollision mit <code>-2</code>…<code>-5</code>-Suffix wiederholt).</p><ul><li><code>chamber</code>, <code>address</code>, <code>postal_code</code>, <code>city</code></li><li><code>brand_color</code>, <code>description</code>, <code>business_page_hero_tagline</code></li></ul><p><strong>Nur leere Felder fullen</strong> (niemals eine bestehende manuelle Wahl uberschreiben, damit erneute Durchlaufe Benutzer nicht uberraschen):</p><ul><li><code>second_accent_color</code> (von Markenfarbe abgeleitet, wenn leer)</li><li><code>email_style</code> (vom E-Mail-Ton)</li><li><code>timezone</code> (Landesstandard)</li><li><code>pdf_language</code> (Landesstandard)</li><li><code>footer_text</code> (Stil <code>Handelsname - Handelsregister 12345678</code>)</li><li><code>payment_options_enabled</code> (<code>bank_transfer</code>)</li><li><code>payment_default_method</code> (<code>bank_transfer</code>)</li><li><code>payment_instructions</code> (lokalisierter Standardtext)</li><li><code>email_footer_show_website</code>, <code>email_footer_show_support_email</code>, <code>email_footer_show_business_page</code> (Booleans)</li></ul><p>Nebeneffekte jenseits von Spaltenschreibvorgangen:</p><ul><li><strong>Initialenlogo</strong> wird aus Unternehmensname + Markenfarbe generiert, aber nur wenn <code>logo_path</code> und <code>logo_svg</code> beide leer sind. Vollstandig ubersprungen, wenn der Benutzer im Magie-Schritt ein echtes Logo hochgeladen hat.</li><li><strong>Dienstleistungen</strong> — bis zu 8 Einfugungen in <code>company_services</code>, nur wenn das Unternehmen noch keine hat.</li><li><strong>E-Mail-Vorlagen</strong> — pro Typ uber das Vorlagen-Repository gespeichert.</li><li><strong>Website-Site-Seed</strong> — beim ersten Abschluss, der null Seiten im Workspace findet, erstellt <code>apply.service.js</code> eine Standard-Site mit einer Entwurfs-Startseite (<code>/</code>, Vorlage &quot;home&quot;, <code>is_home: true</code>) und fullt die Design-Tokens mit der im Assistenten erfassten Markenfarbe. Wiederholte Ausfuhrungen uberspringen die Erstellung, wenn bereits eine Seite existiert, sodass der Assistent niemals manuelle Bearbeitungen uberschreibt.</li><li><strong>Domain</strong> — <code>activateSubdomain</code> fur den Subdomain-Pfad, oder <code>addDomain</code> + <code>quickEnableInbox</code> (+ optionales personliches Postfach) fur den eigenen-Domain-Pfad.</li></ul><p>Wenn die Domainbereitstellung fehlschlagt, wird der Rest des Abschlusses trotzdem angewendet — der Fehler erscheint auf dem Abschlussbildschirm mit einem spezifischen Fehlercode (<code>subdomain_failed</code>, <code>domain_failed</code>, <code>inbox_enable_failed</code>, <code>personal_mailbox_failed</code>, <code>personal_mailbox_remove_failed</code>, <code>personal_mailbox_list_failed</code>), ubersetzt in eine benutzerlesbare Zeile.</p><h2 id="abschlussbildschirm" tabindex="-1">Abschlussbildschirm <a class="header-anchor" href="#abschlussbildschirm" aria-label="Permalink to &quot;Abschlussbildschirm&quot;">​</a></h2><p>Ein grünes Bestatigungspanel, das 4,2 Sekunden (oder 0,9 Sekunden, wenn nichts bereitgestellt wurde) angezeigt wird, bevor es weiterleitet. Es listet jeden vom Assistenten eingerichteten Punkt in einer Übersichtskartenliste auf:</p><ul><li><strong>Kategorien</strong> — die fur den Workspace angelegten System-Ausgabenkategorien (z. B. &quot;Buro, Reisen, Software&quot;).</li><li><strong>Dienstleistungen</strong> — Anzahl der zur Unternehmensseite hinzugefugten Dienstleistungen.</li><li><strong>Logo</strong> — Bestatigung, wenn ein Initialenlogo generiert wurde.</li><li><strong>E-Mail-Vorlagen</strong> — Anzahl der in der Markenstimme des Workspace gespeicherten Vorlagen.</li><li><strong>Website</strong> — Bestatigung, dass eine Standard-Site mit Startseite, Navigation und Markenfarbe bereitsteht. Nur sichtbar, wenn die Site gerade neu angelegt wurde.</li><li><strong>Domain</strong> — die fertige URL fur Subdomains oder die hinzugefugte Domain fur eigene-Domain-Konfigurationen.</li></ul><p>Zwei Schaltflachen erscheinen unter der Übersicht:</p><ul><li><strong>Zum Dashboard</strong> — bringt den Benutzer sofort zu <code>/dashboard</code>, den automatischen Weiterleitungstimer uberspringend.</li><li><strong>Website-Baukasten offnen</strong> — nur sichtbar, wenn eine Site angelegt wurde. Bringt den Benutzer direkt zu <code>/website</code>, damit er mit der Bearbeitung seiner Startseite beginnen kann.</li></ul><p>Danach landet der Benutzer auf <code>/dashboard</code> (Standardpfad), <code>/website</code> (wenn er auf die Website-Baukasten-CTA geklickt hat) oder <code>/workspace/organization/company/address</code> (wenn eine eigene Domain auf DNS-Überprufung wartet).</p><h2 id="erneutes-durchlaufen-des-assistenten" tabindex="-1">Erneutes Durchlaufen des Assistenten <a class="header-anchor" href="#erneutes-durchlaufen-des-assistenten" aria-label="Permalink to &quot;Erneutes Durchlaufen des Assistenten&quot;">​</a></h2><p>Der Assistent ist vollstandig wiederholbar. Ruckkehrende Benutzer landen auf Schritt 1 mit sichtbaren aktuellen Antworten. Nichts zwingt sie durch jeden Schritt — sie konnen ein Feld in einem Schritt bearbeiten und auf Abschliessen klicken.</p><p>Der Diff des Überprüfungsschritts ist das Sicherheitsnetz: Er zeigt dem Benutzer jede bevorstehende Überschreibung. Sinnvolle Standardfelder (Zeitzone, pdf_language, Zahlungsstandards, Footer-Text) sind nicht im Diff enthalten, da Abschliessen dort nur leere Felder fullt — sie stillschweigend zu andern wurde Benutzer uberraschen, die sie bewusst gesetzt haben.</p><p>Um einzelne Einstellungen ohne den Assistenten zu uberprufen, gehen Sie zu:</p><ul><li><code>/workspace/organization/company/about</code> — Name, Registrierung, Adresse, USt.</li><li><code>/workspace/organization/company/look</code> — Markenfarbe, Logo.</li><li><code>/workspace/organization/company/website</code> — Tagline, Über-Text, Dienstleistungen.</li><li><code>/workspace/organization/company/address</code> — eigene Domain + DNS.</li><li><code>/workspace/email</code> — Posteingang, Postfacher, Vorlagen.</li></ul><p>Die vollständige Übersicht finden Sie in der <a href="/de/settings/">Übersicht der Einstellungen</a>.</p><h2 id="randfalle" tabindex="-1">Randfalle <a class="header-anchor" href="#randfalle" aria-label="Permalink to &quot;Randfalle&quot;">​</a></h2><ul><li><strong>Einen Schritt uberspringen.</strong> Weiter ist pro Schritt durch die mindestens erforderlichen Antworten gesteuert. Der Registerschritt hat keine Hürde; Domain erfordert einen gewahlten Pfad mit nicht-leerem Wert, oder einen abgeschlossenen Kauf im Registrieren-Weg, oder das Ubersprungen-Flag; Magie erfordert, dass Generieren ausgefuhrt wurde; Unternehmen und Überprufung haben ihre eigenen Hürden.</li><li><strong>Schliessen mitten im Schritt.</strong> Jede Antwort wird bei Änderung gespeichert, sodass der nachste Besuch dort fortgesetzt wird, wo der Benutzer aufgehort hat. Der Schrittindex wird ebenfalls gespeichert (<code>answers</code> und <code>currentStep</code> leben in derselben JSONB-Spalte).</li><li><strong>Meinungsanderung im Domain-Schritt.</strong> Wechsel von <code>eigene</code> zu <code>subdomain</code> nach Eingabe einer Domain uberschreibt <code>answers.domain</code> auf <code>null</code>, bis der Benutzer einen Slug wahlt. Wechsel zum Registrieren-Weg speichert eine Ubersprungen-Antwort, sodass eine neue Anmeldung nicht blockiert wird, wenn der Benutzer Registrieren offnet, aber den Kauf verschiebt. Wechsel zu einer Subdomain, wenn bereits eine eigene Domain angehangt ist, zeigt eine Vorabwarnung.</li><li><strong>Logo-Extraktion fehlgeschlagen.</strong> Überwiegend weisse Logos und Nur-SVG-Eingaben, die <code>sharp</code> nicht rastern kann, geben <code>color: null</code> zuruck. Der Gemini-Markenfarbvorschlag wird dann verwendet.</li><li><strong>Domain bereits bei eigene-Domain-Abschluss hinzugefugt.</strong> Ein 409 von <code>addDomain</code> fallt auf die bestehende Zeile zuruck, sodass der Posteingang-Aktivierungsschritt trotzdem ausgefuhrt wird.</li><li><strong>Personliches Postfach existiert bereits.</strong> Ein 409 von <code>createMailbox</code> wird als Erfolg behandelt.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("de/features/setup-wizard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const setupWizard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  setupWizard as default
};
