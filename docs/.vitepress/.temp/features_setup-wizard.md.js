import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Setup Wizard","description":"","frontmatter":{"title":"Setup Wizard","last_verified":"2026-05-19T00:00:00.000Z"},"headers":[],"relativePath":"features/setup-wizard.md","filePath":"features/setup-wizard.md"}');
const _sfc_main = { name: "features/setup-wizard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="setup-wizard" tabindex="-1">Setup Wizard <a class="header-anchor" href="#setup-wizard" aria-label="Permalink to &quot;Setup Wizard&quot;">​</a></h1><p>A five-step magical setup at <code>/setup</code> that captures your business identity, optionally pulls your registration from KvK or an EU registry, picks a web address, generates brand suggestions, and applies everything in one click. The wizard is the front door for first-time users and stays available later for tweaks.</p><p>If you came here looking for the basic walk-through, start at <a href="/getting-started/company-setup">Set Up Your Company</a>. This page is the deep reference: every step, every option, every consequence.</p><h2 id="when-the-wizard-is-offered" tabindex="-1">When the wizard is offered <a class="header-anchor" href="#when-the-wizard-is-offered" aria-label="Permalink to &quot;When the wizard is offered&quot;">​</a></h2><p>The wizard is reachable two ways:</p><ul><li><strong>Direct:</strong> navigate to <code>/setup</code> at any time.</li><li><strong>Dashboard banner:</strong> while onboarding is incomplete, a dismissable &quot;Finish setup&quot; banner sits at the top of <code>/dashboard</code>. It shows a count of pending fields (or a generic &quot;complete your profile&quot; message if the count is zero) and a button back into the wizard. A dismiss button (X icon) hides the banner per browser via localStorage, persisting across reloads until the wizard is completed.</li></ul><p>The wizard is non-blocking: the old forced redirect to <code>/setup</code> on signup (the <code>ONBOARDING_MANDATORY</code> gate) has been removed. New signups land on <code>/dashboard</code> directly and see the dismissable banner instead.</p><h2 id="page-layout" tabindex="-1">Page layout <a class="header-anchor" href="#page-layout" aria-label="Permalink to &quot;Page layout&quot;">​</a></h2><p>The wizard is a single page (<code>/setup</code>) with five steps in a stepper. Each step writes its answers to a JSONB column (<code>companies.onboarding_state</code>) on every change, so the wizard is fully resumable: close the tab mid-step and the next visit lands you on the same step with the same answers filled in.</p><p>Three buttons sit at the bottom on every step:</p><ul><li><strong>Back</strong> — go to the previous step (hidden on step 1).</li><li><strong>Skip for now</strong> — leaves without marking onboarding complete. The dashboard banner stays up so the user can come back.</li><li><strong>Continue</strong> / <strong>Finish</strong> — gated per step on the answers below.</li></ul><p>There is no &quot;save and exit&quot; — saving is automatic. <strong>Skip for now</strong> is a clean exit, not a discard.</p><h2 id="step-1-—-business" tabindex="-1">Step 1 — Business <a class="header-anchor" href="#step-1-—-business" aria-label="Permalink to &quot;Step 1 — Business&quot;">​</a></h2><p>Captures the two answers every later step depends on.</p><table tabindex="0"><thead><tr><th>Field</th><th>Stored as</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Required</th><th>Notes</th></tr></thead><tbody><tr><td>Business name</td><td><code>answers.businessName</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">yes</td><td>Used as <code>display_name</code> and <code>company_name</code> on Finish (legal name from the registry step wins for <code>company_name</code> if both are present). Also seeds the subdomain slug suggestion in step 3.</td></tr><tr><td>What you do</td><td><code>answers.what</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">yes</td><td>Free-text, 1–2 sentences. Fed to the magic step&#39;s suggestion generator (Gemini) along with country and sector.</td></tr></tbody></table><p><strong>Continue is disabled</strong> until both fields have non-empty trimmed values.</p><h2 id="step-2-—-registry" tabindex="-1">Step 2 — Registry <a class="header-anchor" href="#step-2-—-registry" aria-label="Permalink to &quot;Step 2 — Registry&quot;">​</a></h2><p>Picks a country and either looks up the business in the country&#39;s official registry, fills the details in manually, or skips the step entirely.</p><h3 id="three-paths" tabindex="-1">Three paths <a class="header-anchor" href="#three-paths" aria-label="Permalink to &quot;Three paths&quot;">​</a></h3><ol><li><strong>Search:</strong> typeahead by company name, pick a match, and let the backend pull the Basisprofiel (EUR 0.02/call for NL). The search box is only shown when the paid KVK API (<code>KVK_API_KEY</code>) is configured on the API container. When the key is missing, search is skipped and the wizard defaults to manual entry (path 2).</li><li><strong>Manual:</strong> fill in the company name, KVK number (optional), address, postal code, and city by hand. The data is saved directly to the company row via <code>PUT /company-settings/company</code> and <code>answers.kvk</code> is stamped with <code>manual: true</code>. Manual entry is the default when KVK_API_KEY is not set, and also exists for two additional scenarios even when search is available: (a) new companies not yet in the free OpenKVK dataset, and (b) companies whose trade name does not match what the user typed in search.</li><li><strong>Skip</strong> — &quot;Geen KVK-inschrijving?&quot; (or equivalent) stores <code>answers.kvk = null</code>. The wizard continues; company data can be filled later in settings.</li></ol><p>Switching between search and manual is a single click (when search is available): a &quot;Fill in manually&quot; button appears below the search results, and a &quot;Back to KVK search&quot; link sits at the top of the manual form. When <code>KVK_API_KEY</code> is absent, the switch-to-search button is hidden entirely and the user starts and stays on manual entry.</p><h3 id="country-options" tabindex="-1">Country options <a class="header-anchor" href="#country-options" aria-label="Permalink to &quot;Country options&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Code</th><th>Registry shown</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Lookup wired</th></tr></thead><tbody><tr><td><code>NL</code></td><td>KvK</td><td style="${ssrRenderStyle({ "text-align": "center" })}">yes</td></tr><tr><td><code>FR</code></td><td>SIRENE</td><td style="${ssrRenderStyle({ "text-align": "center" })}">yes</td></tr><tr><td><code>GB</code></td><td>Companies House</td><td style="${ssrRenderStyle({ "text-align": "center" })}">yes</td></tr><tr><td><code>DE</code></td><td>Handelsregister</td><td style="${ssrRenderStyle({ "text-align": "center" })}">no — manual entry</td></tr><tr><td><code>OTHER</code></td><td>(no registry)</td><td style="${ssrRenderStyle({ "text-align": "center" })}">no — manual entry</td></tr></tbody></table><p><code>NL</code> is the default. Country drives several things on Finish: timezone (<code>Europe/Amsterdam</code> / <code>Europe/Berlin</code> / <code>Europe/Paris</code> / <code>Europe/London</code>), <code>pdf_language</code> (<code>nl</code> / <code>de</code> / <code>fr</code> / <code>en</code>), and the registry label baked into <code>footer_text</code> (<code>KvK 12345678</code>, <code>Handelsregister …</code>, <code>SIRENE …</code>, <code>CRN …</code>).</p><h3 id="search-mode" tabindex="-1">Search mode <a class="header-anchor" href="#search-mode" aria-label="Permalink to &quot;Search mode&quot;">​</a></h3><p>For supported countries the user types a company name to search. The backend calls the matching public API and returns one of:</p><ul><li><strong>ok</strong> — <code>legalName</code>, <code>address</code>, <code>postalCode</code>, <code>city</code>, <code>sector</code> populated and stored under <code>answers.registry</code>.</li><li><strong>not-configured</strong> — provider not yet wired in this environment; the user is prompted to switch to manual entry.</li><li><strong>not-found</strong> — number didn&#39;t resolve; user can retry or switch to manual.</li><li><strong>invalid</strong> — number format rejected.</li><li><strong>error</strong> — provider error; manual entry remains an option.</li></ul><h4 id="kvk-basisprofiel-nl-only" tabindex="-1">KvK Basisprofiel (NL only) <a class="header-anchor" href="#kvk-basisprofiel-nl-only" aria-label="Permalink to &quot;KvK Basisprofiel (NL only)&quot;">​</a></h4><p>For NL workspaces, the KvK lookup is a two-step flow:</p><ol><li><strong>Typeahead</strong> — the user searches by company name. The <code>zoeken</code> endpoint (free) returns matching entries. This is the autocomplete step that powers the existing <code>ok</code> / <code>not-found</code> responses. When the search returns zero hits, the UI shows an inline empty-state panel (title, explanation, and a &quot;fill manually&quot; CTA that seeds the manual form with what the user already typed). This is common because OpenKVK&#39;s free tier misses many young businesses.</li><li><strong>Basisprofiel</strong> — once a match is picked, the wizard calls the KvK Basisprofiel detail endpoint. This is a paid call (EUR 0.02, 24h-cached per KVK number). It returns the full profile: <code>legalName</code>, <code>statutaireNaam</code> (statutory name), <code>tradeNames</code> (all registered trade names, ordered), <code>rsin</code>, <code>legalForm</code>, <code>dateFounded</code>, visiting and postal addresses, SBI codes with primary-flag, <code>employeeCount</code>, and <code>indNonMailing</code> (do-not-mail flag). The wizard auto-fills <code>business_page_hero_tagline</code> from the first trade name and city (&quot;[tradeName] in [city]&quot;), and <code>description</code> from the primary SBI description (capped at 280 characters). Both use COALESCE so any user edits in Settings are preserved.</li></ol><p>There are two independent feature gates for NL KVK lookups:</p><ul><li><code>KVK_API_KEY</code> (env var on the API container): when absent, the search box is not shown at all and the wizard defaults to manual entry on that step. OpenKVK&#39;s free tier alone is too sparse (~2% hit rate, misses nearly every fresh registration). The flag flips automatically once the key is set.</li><li><code>KVK_BASISPROFIEL_ENABLED</code>: when off (or when <code>KVK_API_KEY</code> is absent), the wizard uses the free <code>zoeken</code>-only result (the same <code>ok</code> fields listed above). When on and <code>KVK_API_KEY</code> is present, the Basisprofiel data enriches the <code>answers.registry</code> payload and the Review step shows every field that will be written to the company row.</li></ul><h3 id="manual-mode" tabindex="-1">Manual mode <a class="header-anchor" href="#manual-mode" aria-label="Permalink to &quot;Manual mode&quot;">​</a></h3><p>User fills <code>chosen</code> (their registration number), and optionally <code>legalName</code>, <code>address</code>, <code>sector</code>. All four fields are optional in this mode.</p><h3 id="skip-mode" tabindex="-1">Skip mode <a class="header-anchor" href="#skip-mode" aria-label="Permalink to &quot;Skip mode&quot;">​</a></h3><p>A &quot;Not registered yet&quot; toggle stores <code>answers.registry = null</code>. <strong>Continue is always allowed on this step</strong>, regardless of mode — registry data is convenience, not a gate.</p><h2 id="step-3-—-domain" tabindex="-1">Step 3 — Domain <a class="header-anchor" href="#step-3-—-domain" aria-label="Permalink to &quot;Step 3 — Domain&quot;">​</a></h2><p>Pick the web address your customers will see on the public business page and your email inbox. Three branches, displayed as cards, cover every path from free quick-start to buying a domain in-wizard.</p><h3 id="three-branch-picker" tabindex="-1">Three-branch picker <a class="header-anchor" href="#three-branch-picker" aria-label="Permalink to &quot;Three-branch picker&quot;">​</a></h3><p>A grid of three cards presents the choice. Selecting a branch reveals the matching editor below; only one branch is active at a time.</p><p><strong>Subdomain (free):</strong> the user picks a slug; a TLD selector lets them choose between <code>.mycompanydesk.nl</code> and <code>.mycompanydesk.com</code>. The slug is pre-filled from the KVK legal name when available (lowercased, accents stripped, non-ASCII characters removed, truncated at 63 characters), so most users can tap-and-continue without typing. Availability is checked live with a 350 ms debounce as the user types. On Finish the subdomain is provisioned via the Cloudflare API and the company&#39;s website becomes immediately reachable.</p><p>When the wizard is run in the 2-step (plan-gated) flow, the Domain step is omitted entirely. The Finish step auto-provisions a workspace subdomain from the <code>display_name</code> value: the slug is derived from the display name (with retry-on-collision suffixes up to 5 attempts), and <code>activateSubdomain</code> registers it as the public site URL. Best-effort: a collision or failure is logged and does not block the wizard from finishing.</p><p><strong>Own domain:</strong> the user types a domain they already own. A live validation regex checks the format as they type (<code>[a-z0-9][a-z0-9-]*(\\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+</code>). On Finish the wizard adds the domain to the workspace&#39;s domain list (no-op if it was already added) and auto-enables the inbox: <code>info@&lt;domain&gt;</code> as the default mailbox plus <code>support@</code>, <code>sales@</code>, and <code>noreply@</code> aliases. The 409-already-exists path from <code>apply.service</code> is handled gracefully.</p><p>If the domain isn&#39;t yet pointed at MCD&#39;s nameservers, Finish redirects to <code>/website?tab=domein</code> so the user immediately sees the DNS instructions and a <strong>Verify</strong> button. Otherwise it goes to the dashboard.</p><p><strong>Register a new domain:</strong> embeds the live <code>DomainPurchaseCard</code> + <code>DomainClaimModal</code> from the settings surface. The user can search for a domain, check availability and pricing, and either buy it through OpenProvider or claim it free as a Founding Member. On a successful claim or purchase, the domain is already attached to the workspace server-side via the <code>/api/domain-purchase</code> flow, so the wizard records the answer as <code>mode=&#39;own&#39;</code> with the registered name and <code>registered: true</code>; <code>apply.service</code> treats it as a no-op re-add. A green success banner shows the registered domain name and lets the user continue.</p><p>If the user opens the Register branch but does not complete a purchase, the step is marked as skipped so the wizard can continue. They can return later via <code>Company › Your own .com address</code> whenever ready.</p><h3 id="switching-from-a-custom-domain-back-to-a-subdomain" tabindex="-1">Switching from a custom domain back to a subdomain <a class="header-anchor" href="#switching-from-a-custom-domain-back-to-a-subdomain" aria-label="Permalink to &quot;Switching from a custom domain back to a subdomain&quot;">​</a></h3><p>If the workspace already has a custom website domain, the wizard surfaces a warning before letting the user switch to the subdomain path — <code>activateSubdomain</code> refuses on companies with a custom domain still attached, and the failure would otherwise only surface at Finish.</p><h2 id="step-4-—-magic" tabindex="-1">Step 4 — Magic <a class="header-anchor" href="#step-4-—-magic" aria-label="Permalink to &quot;Step 4 — Magic&quot;">​</a></h2><p>Generates brand suggestions from the answers captured so far. Fields:</p><ul><li><strong>Brand colour</strong> — hex. If the user uploads a logo on this step, the dominant non-white colour is extracted from the raster (via <code>sharp</code>) and used; that overrides the Gemini suggestion, and a flag (<code>brandColorFromLogo</code>) prevents a later regenerate from clobbering it. SVG-only logos and mostly-white inputs fall back to the Gemini palette.</li><li><strong>Tagline</strong> — short hero line for the public business page.</li><li><strong>About text</strong> — paragraph for the business page.</li><li><strong>Services</strong> — list of up to 8 service names. Each is inserted into <code>company_services</code> on Finish, but <strong>only if the company has zero services already</strong> — the wizard never overwrites an existing services list.</li><li><strong>Email tone</strong> — <code>formal</code> / <code>friendly</code> / <code>casual</code>. Maps to the email style: <code>formal → classic</code>, <code>friendly</code> and <code>casual → modern</code>.</li><li><strong>Email templates</strong> — keyed by template type (<code>invoice_default</code>, <code>reminder_default</code>, etc.), each with <code>subject</code> + <code>body</code>. Saved via the email template repository on Finish.</li></ul><p>The user can edit any suggestion inline before continuing. <strong>Continue is disabled</strong> until suggestions exist (clicking <strong>Generate suggestions</strong> at least once).</p><h3 id="logo-upload" tabindex="-1">Logo upload <a class="header-anchor" href="#logo-upload" aria-label="Permalink to &quot;Logo upload&quot;">​</a></h3><p>Clicking the upload tile lets the user pick an image up to 4 MB. The file is sent as a base64 data URI to <code>/onboarding/upload-logo</code>, which:</p><ol><li>Stores it via the canonical companies logo path.</li><li>Returns the dominant non-white colour, which is auto-applied to the brand-colour suggestion.</li><li>Sets <code>answers.logoUploaded = true</code> so the Review copy adjusts (&quot;we&#39;ll use your logo&quot; instead of &quot;we&#39;ll create an initials logo from your business name&quot;) and the apply step skips the initials generator.</li></ol><p>If the user already had a logo on file, it&#39;s shown as the existing preview (&quot;you already have a logo&quot;) instead of an empty upload CTA.</p><h2 id="step-5-—-review" tabindex="-1">Step 5 — Review <a class="header-anchor" href="#step-5-—-review" aria-label="Permalink to &quot;Step 5 — Review&quot;">​</a></h2><p>Read-only diff of every field the wizard would change. Two sections:</p><ul><li><strong>Changes</strong> — <code>current → next</code> rows. Includes a swatch for <code>brandColor</code>. Only fields the wizard wrote and that differ from the current company row appear here.</li><li><strong>Already set</strong> — fields the wizard captured but that already match the company row.</li></ul><p>Fields shown: business name, country, registry number, legal name, address, brand colour, tagline, about text, domain summary (<code>info@&lt;resolved-domain&gt;</code> is previewed if a domain is set).</p><p>The footer&#39;s <strong>Finish</strong> button calls <code>/onboarding/complete</code>. The current locale is forwarded so default copy (e.g. payment instructions) is localised.</p><h2 id="what-finish-actually-applies" tabindex="-1">What Finish actually applies <a class="header-anchor" href="#what-finish-actually-applies" aria-label="Permalink to &quot;What Finish actually applies&quot;">​</a></h2><p><code>apply.service.js</code> walks the answers and writes them into the real company row. Two semantics, deliberately split:</p><p><strong>Always overwrite</strong> when the wizard answer is a non-empty string and differs from the current value:</p><ul><li><code>display_name</code>, <code>company_name</code></li><li><code>country</code></li><li><code>chamber</code>, <code>address</code>, <code>postal_code</code>, <code>city</code></li><li><code>brand_color</code>, <code>description</code>, <code>business_page_hero_tagline</code></li></ul><p><strong>Fill blanks only</strong> (never override an existing manual choice, so re-runs don&#39;t surprise users):</p><ul><li><code>second_accent_color</code> (derived from brand colour if blank)</li><li><code>email_style</code> (from email tone)</li><li><code>timezone</code> (country default)</li><li><code>pdf_language</code> (country default)</li><li><code>footer_text</code> (<code>Trader name - KvK 12345678</code> style)</li><li><code>payment_options_enabled</code> (<code>bank_transfer</code>)</li><li><code>payment_default_method</code> (<code>bank_transfer</code>)</li><li><code>payment_instructions</code> (localised default copy)</li><li><code>email_footer_show_website</code>, <code>email_footer_show_support_email</code>, <code>email_footer_show_business_page</code> (booleans)</li></ul><p>Side effects beyond column writes:</p><ul><li><strong>Initials logo</strong> is generated from the business name + brand colour, but only if <code>logo_path</code> and <code>logo_svg</code> are both blank. Skipped entirely if the user uploaded a real logo on the Magic step.</li><li><strong>Services</strong> — up to 8 inserts into <code>company_services</code>, only if the company has none yet.</li><li><strong>Email templates</strong> — saved per type via the template repository.</li><li><strong>Website site seed</strong> — on the first Finish that finds zero pages in the workspace, <code>apply.service.js</code> creates a default site with a draft homepage (<code>/</code>, template &quot;home&quot;, <code>is_home: true</code>) and populates design tokens with the brand colour captured in the wizard. Re-runs skip creation when any page already exists, so the wizard never overwrites manual edits.</li><li><strong>Domain</strong> — <code>activateSubdomain</code> for the subdomain path, or <code>addDomain</code> + <code>quickEnableInbox</code> (+ optional personal mailbox) for the own-domain path.</li></ul><p>If domain provisioning fails, the rest of Finish still applies — the failure shows on the Finish splash with a specific error code (<code>subdomain_failed</code>, <code>domain_failed</code>, <code>inbox_enable_failed</code>, <code>personal_mailbox_failed</code>, <code>personal_mailbox_remove_failed</code>, <code>personal_mailbox_list_failed</code>) translated to a user-readable line.</p><h2 id="finish-splash" tabindex="-1">Finish splash <a class="header-anchor" href="#finish-splash" aria-label="Permalink to &quot;Finish splash&quot;">​</a></h2><p>A green confirmation panel that shows for 4.2 seconds (or 0.9 seconds when nothing was provisioned) before redirecting. It lists every item the wizard set up in a summary card list:</p><ul><li><strong>Categories</strong> - the system expense categories seeded for the workspace (e.g. &quot;Kantoor, Reizen, Software&quot;).</li><li><strong>Services</strong> - count of services added to the business page.</li><li><strong>Logo</strong> - confirmation when an initials logo was generated.</li><li><strong>Email templates</strong> - count of templates saved in the workspace brand tone.</li><li><strong>Website</strong> - confirmation that a default site with homepage, navigation and brand colour is ready. Only shown when the site was freshly seeded.</li><li><strong>Domain</strong> - the ready URL for subdomains or the added domain for own-domain setups.</li></ul><p>Two buttons appear below the summary:</p><ul><li><strong>Go to dashboard</strong> — takes the user to <code>/dashboard</code> immediately, skipping the auto-redirect timer.</li><li><strong>Open the website builder</strong> — visible only when a site was seeded. Takes the user straight to <code>/website</code> so they can start editing their homepage.</li></ul><p>Then the user lands on <code>/dashboard</code> (default path), <code>/website</code> (when they click the website builder CTA), or <code>/website?tab=domein</code> (when an own-domain is pending DNS verification).</p><h2 id="re-running-the-wizard" tabindex="-1">Re-running the wizard <a class="header-anchor" href="#re-running-the-wizard" aria-label="Permalink to &quot;Re-running the wizard&quot;">​</a></h2><p>The wizard is fully re-runnable. Returning users land on step 1 with their current answers visible. Nothing forces them through every step — they can edit one field on one step and click Finish.</p><p>The Review step&#39;s diff is the safety net: it shows the user every overwrite that&#39;s about to happen. Sensible-default fields (timezone, pdf_language, payment defaults, footer text) are not in the diff because Finish only fills blanks for those — silently flipping them would surprise users who set them deliberately.</p><p>To revisit individual settings without the wizard, go to:</p><ul><li><code>/workspace/organization/company/about</code> — name, registration, address, VAT.</li><li><code>/workspace/organization/company/look</code> — brand colour, logo.</li><li><code>/website</code> — tagline, about text, services (public business page).</li><li><code>/website?tab=domein</code> — custom domain + DNS.</li><li><code>/inbox</code> — inbox, mailboxes, templates.</li></ul><p>See <a href="/settings/">Settings overview</a> for the full map.</p><h2 id="edge-cases" tabindex="-1">Edge cases <a class="header-anchor" href="#edge-cases" aria-label="Permalink to &quot;Edge cases&quot;">​</a></h2><ul><li><strong>Skipping a step.</strong> Continue is gated per step on the minimum required answers. The Registry step has no gate; Domain requires a chosen path with a non-empty value, or a completed purchase for the Register branch, or the skipped flag; Magic requires Generate to have run; Business and Review have their own gates.</li><li><strong>Closing mid-step.</strong> Every answer is persisted on change, so the next visit resumes where the user left off. Step index is also persisted (<code>answers</code> and <code>currentStep</code> live in the same JSONB column).</li><li><strong>Changing your mind on the Domain step.</strong> Switching from <code>own</code> to <code>subdomain</code> after typing a domain rewrites <code>answers.domain</code> to <code>null</code> until the user picks a slug. Switching to the Register branch stores a skipped-answer so a fresh signup isn&#39;t dead-ended if they open Register but defer the purchase. Switching to a subdomain when a custom domain is already attached surfaces a pre-flight warning.</li><li><strong>Logo extraction fails.</strong> Mostly-white logos and SVG-only inputs that <code>sharp</code> can&#39;t rasterise return <code>color: null</code>. The Gemini brand-colour suggestion is used instead.</li><li><strong>Domain already added on own-domain Finish.</strong> A 409 from <code>addDomain</code> falls back to the existing row so the inbox-enable step still runs.</li><li><strong>Personal mailbox already exists.</strong> A 409 from <code>createMailbox</code> is treated as success.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("features/setup-wizard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const setupWizard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  setupWizard as default
};
