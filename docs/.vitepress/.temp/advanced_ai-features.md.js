import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"AI Features","description":"","frontmatter":{"title":"AI Features","last_verified":"2026-05-18T00:00:00.000Z"},"headers":[],"relativePath":"advanced/ai-features.md","filePath":"advanced/ai-features.md"}');
const _sfc_main = { name: "advanced/ai-features.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="ai-features" tabindex="-1">AI Features <a class="header-anchor" href="#ai-features" aria-label="Permalink to &quot;AI Features&quot;">​</a></h1><p>MyCompanyDesk uses AI in several places to speed up data entry, surface answers from your own books, and lower the cost of writing copy in four languages. This page lists every AI surface that ships in the product today and how it behaves.</p><p>Provider routing is intentional and changes per surface. The default chain is EU-only: Gemini (free quota) on Vertex AI <code>europe-west1</code> first, local Ollama as last resort. Ollama Cloud (US-hosted) is disabled by default because MyCompanyDesk does not have a signed Data Processing Agreement with Ollama Inc. It can be re-enabled for workloads that demonstrably carry no customer personal data, but the LLM router falls through to Vertex EU otherwise. Plan gating, where it applies, is enforced at the API layer through entitlements.</p><h2 id="contextual-guide-in-app-chatbot" tabindex="-1">Contextual guide (in-app chatbot) <a class="header-anchor" href="#contextual-guide-in-app-chatbot" aria-label="Permalink to &quot;Contextual guide (in-app chatbot)&quot;">​</a></h2><p>The help icon in the app shell opens a chat panel that knows which page you are on, what records you are looking at, and what your workspace data looks like. It is built as a tool-using agent: instead of guessing at numbers, it asks for them.</p><ul><li><strong>Model.</strong> Default chat model is Gemini on Vertex AI <code>europe-west1</code>, served over Cloudflare AI Gateway. When Ollama Cloud is explicitly enabled at the workspace level, <code>deepseek-v4-pro:cloud</code> takes over as the primary tier. Both paths fall back to the next tier on rate-limit or outage; the swap is transparent to the conversation.</li><li><strong>Streaming.</strong> Replies stream over <code>POST /api/contextual-guide/stream</code> (Server-Sent Events). The web client renders incoming tokens as a typewriter effect so first tokens appear in well under a second.</li><li><strong>FAQ short-circuit.</strong> Before the model is called, the query is matched against the workspace FAQ corpus using SymSpell-corrected keyword search and a vector cosine fallback (Workers AI <code>bge-m3</code> primary, Gemini embeddings fallback). A confident match returns the curated FAQ answer with no LLM round-trip -- fast and free.</li><li><strong>Page context.</strong> The current route, the visible entity, and a compact app-state summary are injected into the system prompt. The guide answers about what you are seeing, not in the abstract.</li><li><strong>Conversation memory.</strong> The last 6 turns are kept verbatim; older turns are summarised into a rolling memory.</li><li><strong>Pre-filter, no router.</strong> A small deterministic filter catches forbidden topics, casual messages, and empty input before the model is called. Beyond that there is <strong>no DATA/FAQ/GENERAL classifier in front of the agent</strong> -- the model sees the full tool catalog and picks tools itself, including a tool that searches the help knowledge base. One question can naturally use multiple tools (e.g. &quot;how do I file my Q2 VAT and what&#39;s my saldo&quot; calls <code>search_help</code> and <code>vat_aangifte_rubrieken</code> in the same loop).</li></ul><h3 id="eu-ai-act-disclosure-art-50" tabindex="-1">EU AI Act disclosure (art. 50) <a class="header-anchor" href="#eu-ai-act-disclosure-art-50" aria-label="Permalink to &quot;EU AI Act disclosure (art. 50)&quot;">​</a></h3><p>The EU AI Act (Regulation 2024/1689) classifies the contextual guide as a limited-risk AI system under article 50. Limited-risk systems must transparently disclose to end users that they are interacting with AI. Two disclosure mechanisms ship in the guide:</p><ul><li><strong>AI badge.</strong> A small &quot;AI&quot; pill sits next to the assistant name in the header. It is always visible while the guide is open, satisfying the continuous-disclosure requirement. A tooltip on the badge names the underlying provider (Google Gemini).</li><li><strong>Intro disclosure text.</strong> A short line appears under the welcome prompt in the empty chat state: &quot;You are talking to an AI assistant. Answers may contain errors; always verify financial or tax conclusions yourself.&quot;</li></ul><p>These disclosures also appear in Dutch, German, and French in their respective locale builds. The obligation takes effect in August 2026; the disclosures shipped ahead of the deadline.</p><h3 id="pro-visual-skin" tabindex="-1">Pro visual skin <a class="header-anchor" href="#pro-visual-skin" aria-label="Permalink to &quot;Pro visual skin&quot;">​</a></h3><p>Pro-tier workspaces get a premium assistant skin that replaces the generic styling with the Pro violet accent. When the billing plan is Pro, the assistant panel changes visually:</p><ul><li>The &quot;AI&quot; pill becomes a violet &quot;Pro&quot; pill, signalling that the assistant runs on the highest-tier model with full app context.</li><li>The panel border, avatar ring, online-dot, and send button shift to violet (<code>#a855f7</code>), matching the Pro ribbon in the app navigation.</li><li>The status line reads &quot;Your Pro assistant is ready&quot; instead of the generic &quot;Ready to help.&quot;</li></ul><p>The Pro skin is purely cosmetic. The underlying model selector, tool catalog, and EU AI Act disclosures remain identical across tiers. Backed by <code>TIER_CHAT_CONFIG</code> which already gives Pro a heavier model variant and unlimited app context.</p><h3 id="tool-catalog" tabindex="-1">Tool catalog <a class="header-anchor" href="#tool-catalog" aria-label="Permalink to &quot;Tool catalog&quot;">​</a></h3><p>When the question needs a number, page-help, or a VAT-specific aggregate, the model calls one of approximately 18 parameterised tools. Each handler is a hand-written, RLS-protected <code>SELECT</code> (or a wrapper around an existing aggregate service) -- the model picks the tool name and arguments, it cannot author SQL. Read-only by design. The same catalog is exposed to Gemini (function declarations) and to Ollama Cloud (OpenAI-compatible <code>tools</code> array), so a fallback between providers keeps capabilities identical.</p><p><strong>Data tools</strong> -- accept <code>period</code> of <code>this_month</code> / <code>last_month</code> / <code>this_quarter</code> / <code>this_year</code> / <code>last_year</code>:</p><table tabindex="0"><thead><tr><th>Tool</th><th>Returns</th></tr></thead><tbody><tr><td><code>revenue_summary</code></td><td>Revenue, expenses, profit for a period</td></tr><tr><td><code>top_customers</code></td><td>Top customers by revenue or outstanding</td></tr><tr><td><code>overdue_invoices</code></td><td>List of currently overdue invoices</td></tr><tr><td><code>vat_summary</code></td><td>VAT collected, paid, and net for a period</td></tr><tr><td><code>monthly_breakdown</code></td><td>Per-month totals across the year</td></tr><tr><td><code>expense_summary</code></td><td>Expense totals grouped by category</td></tr><tr><td><code>time_summary</code></td><td>Time-registration totals per project</td></tr><tr><td><code>invoice_list</code></td><td>Invoices filtered by status/date/customer</td></tr><tr><td><code>customer_aging</code></td><td>Receivables aging buckets per customer</td></tr><tr><td><code>tax_summary</code></td><td>Country-aware tax position for the period</td></tr></tbody></table><p><strong>VAT tools</strong> -- accept <code>year</code> plus <code>period</code> of <code>Q1</code>/<code>Q2</code>/<code>Q3</code>/<code>Q4</code>/<code>year</code>:</p><table tabindex="0"><thead><tr><th>Tool</th><th>Returns</th></tr></thead><tbody><tr><td><code>vat_aangifte_rubrieken</code></td><td>Unified NL aangifte sheet (sections 1-5, codes 1a-5g)</td></tr><tr><td><code>vat_pre_filing_checks</code></td><td>Concrete blockers before filing (drafts, missing receipts, ICP pending)</td></tr><tr><td><code>vat_kor_status</code></td><td>KOR threshold tracker -- YTD revenue versus EUR 20 000 limit</td></tr><tr><td><code>vat_kia_status</code></td><td>KIA bracket tracker -- investments and deduction amount</td></tr><tr><td><code>vat_icp_opgaaf</code></td><td>Per-customer intra-EU B2B sales</td></tr><tr><td><code>vat_oss_breakdown</code></td><td>Per-country EU OSS B2C sales</td></tr><tr><td><code>vat_foreign_refundables</code></td><td>Foreign VAT refundables, EU procedure deadline</td></tr></tbody></table><p><strong>Help tool</strong>:</p><table tabindex="0"><thead><tr><th>Tool</th><th>Returns</th></tr></thead><tbody><tr><td><code>search_help</code></td><td>Best-matching FAQ entry (semantic-search wrapper). Used for &quot;how do I X&quot; questions.</td></tr></tbody></table><p>The chat defaults to Gemini on Vertex AI <code>europe-west1</code>. When a workspace explicitly enables Ollama Cloud, the chat is pinned to <code>deepseek-v4-pro:cloud</code> -- empirical bench showed deepseek lands a 2-tool plan + synthesis in approximately 3 seconds, faster than <code>qwen3-coder-next:cloud</code> and <code>gpt-oss:120b-cloud</code> on this workload. Gemini is the fallback path when Ollama Cloud is unavailable or disabled.</p><p>The standalone <code>/api/vat-assistant/*</code> route is <strong>gone</strong> as of May 2026 -- VAT questions go through the same unified contextual-guide endpoint and the <code>vat_*</code> tools above. There is no separate model or panel.</p><h2 id="vendor-classifier-expenses" tabindex="-1">Vendor classifier (expenses) <a class="header-anchor" href="#vendor-classifier-expenses" aria-label="Permalink to &quot;Vendor classifier (expenses)&quot;">​</a></h2><p>When you enter an expense, the <strong>vendor classifier</strong> suggests the right category from your workspace&#39;s <code>expense_categories</code> table -- not a fixed built-in list. It runs through Gemini Flash-Lite with a Dutch SME bookkeeping prompt that:</p><ul><li>Matches the vendor against existing category keys before creating new ones.</li><li>Picks a <code>vat_treatment</code> (<code>standard</code> / <code>b2b_reverse_charge</code> / <code>foreign_vat_charged</code> / <code>import</code> / <code>kor</code>) based on the vendor&#39;s domain and country.</li><li>Auto-flags potential investments (hardware/equipment over EUR 450 ex-VAT) so depreciation kicks in.</li></ul><p>The classifier is invoked from <code>expense-categories.service.classifyVendor(companyId, { vendor, domain })</code> and is wired to the expense form&#39;s category field.</p><h2 id="ai-suggestions-with-rlhf-lite-feedback-loop" tabindex="-1">AI suggestions (with RLHF-lite feedback loop) <a class="header-anchor" href="#ai-suggestions-with-rlhf-lite-feedback-loop" aria-label="Permalink to &quot;AI suggestions (with RLHF-lite feedback loop)&quot;">​</a></h2><p>After an entity is created, the AI-suggestions service queues a low-priority LLM analysis and writes results into <code>ai_suggestions</code>. Surfaces include category fixes for expenses set to <code>other</code> and description-quality improvements for invoice line items, expenses, and customer notes.</p><p>The interesting bit is the <strong>feedback loop</strong>:</p><ol><li>When you accept a suggestion, the input text is embedded (Workers AI <code>bge-m3</code> primary, Gemini fallback) and stored in <code>suggestion_examples</code> with the accepted value.</li><li>The next time the same task fires for your workspace, the new input is embedded and the top-K nearest neighbours are pulled in as few-shot examples.</li><li>The prompt picks up your house style automatically -- &quot;prior examples this user accepted&quot; becomes part of the system message.</li></ol><p>A FIFO cap of 50 examples per <code>(company_id, task)</code> keeps lookup at microseconds without <code>pgvector</code>. Embeddings are stamped with provider + dimension so a provider swap doesn&#39;t poison the lookup; mismatched rows are skipped, not crashed on.</p><h2 id="industry-detection" tabindex="-1">Industry detection <a class="header-anchor" href="#industry-detection" aria-label="Permalink to &quot;Industry detection&quot;">​</a></h2><p>The local <strong>E2B</strong> model (Gemini Flash-Lite primary, Workers AI fallback for both processing modes; Ollama Cloud as an additional fallback in Optimise mode only) classifies your workspace&#39;s industry from invoice and expense history. It re-evaluates progressively:</p><table tabindex="0"><thead><tr><th>Workspace state</th><th>Behaviour</th></tr></thead><tbody><tr><td>0-2 entities</td><td>Skip -- not enough signal</td></tr><tr><td>3+ entities, no profile</td><td>First detection</td></tr><tr><td>Confidence &lt; 0.6 and entity count doubled</td><td>Re-evaluate</td></tr><tr><td>Confidence &gt;= 0.6 and doubled again</td><td>Re-evaluate</td></tr><tr><td>Confidence &gt;= 0.8</td><td>Stop</td></tr></tbody></table><p>The detected industry feeds back into the AI-suggestion prompts so categorisation matches how shops in your industry actually book things.</p><h2 id="receipt-scanner" tabindex="-1">Receipt scanner <a class="header-anchor" href="#receipt-scanner" aria-label="Permalink to &quot;Receipt scanner&quot;">​</a></h2><p>Receipts and supplier invoices are extracted to structured expense data via <code>POST /api/receipt-scanner</code>.</p><ul><li><strong>Primary path.</strong> PDFs and images go straight to Gemini multimodal as <code>inlineData</code>. No <code>tesseract.js</code>, no <code>unpdf</code>, no preprocessing -- Gemini reads the document.</li><li><strong>Fallback path.</strong> When AI privacy mode is on, the file is over 15 MB, or Gemini is rate-limited, the scanner falls back to local Ollama: PDFs through <code>unpdf</code> for text, images through <code>tesseract.js</code>, parsing by Gemma 4 E2B.</li><li><strong>Output.</strong> Date, supplier, amount, line items, suggested category, VAT rate. Pre-LLM page filtering (keywords) keeps long PDFs cheap; a post-LLM exact-match filter is the safety net.</li></ul><p>See <a href="/advanced/receipt-scanning">Receipt Scanning</a> for the user-facing flow.</p><h2 id="entity-suggestions" tabindex="-1">Entity suggestions <a class="header-anchor" href="#entity-suggestions" aria-label="Permalink to &quot;Entity suggestions&quot;">​</a></h2><p>The entity-suggestions service powers autocomplete-style inline hints on customer, project, and expense forms. It runs purely on database queries -- no LLM -- so it is instant and works offline. It complements the AI-suggestions service rather than competing with it: entity-suggestions is &quot;remember what you typed last time&quot;, AI-suggestions is &quot;what should this look like&quot;.</p><h2 id="text-check-typo-correction" tabindex="-1">Text check (typo correction) <a class="header-anchor" href="#text-check-typo-correction" aria-label="Permalink to &quot;Text check (typo correction)&quot;">​</a></h2><p>Form fields run an in-process <strong>SymSpell</strong> typo correction on debounce as you type. It is wired into nine entity forms (invoices, quotes, expenses, customers, projects, contracts, objects, recurring entries, time entries).</p><ul><li><strong>Endpoint.</strong> <code>POST /api/text-check</code> returns <code>{ corrected, hasCorrections, grammarAvailable: false }</code>.</li><li><strong>Languages.</strong> Dutch is the dictionary default; English, German, and French are supported.</li><li><strong>No grammar.</strong> Sentence-level grammar checking via LanguageTool was removed -- keystroke-grade grammar was a continuous tax for marginal value. A button-triggered &quot;Improve&quot; action may return as a separate feature.</li></ul><h2 id="translation-gemini-flash-lite" tabindex="-1">Translation (Gemini Flash-Lite) <a class="header-anchor" href="#translation-gemini-flash-lite" aria-label="Permalink to &quot;Translation (Gemini Flash-Lite)&quot;">​</a></h2><p>Runtime translation of short snippets goes through <code>translate.service</code> which wraps Gemini Flash-Lite. Three call sites use it today:</p><ul><li><strong>Chatbot.</strong> Pre-processing translates non-English questions to English before the model call, then translates the reply back to the company language.</li><li><strong>Support / feedback.</strong> Inbound messages are normalised to English for the operations team.</li><li><strong>AI suggestions.</strong> Description prompts run in English, output is translated back.</li></ul><p>Bulk locale-file sync (filling missing keys, re-translating drift across <code>nl/de/fr</code>) is <strong>not</strong> in this service -- it lives in the Huisbot weekly cron which opens PRs against <code>development</code> autonomously. The in-app UI never blocks on translation drift.</p><h2 id="dashboard-briefing-insight-pro" tabindex="-1">Dashboard briefing insight (Pro) <a class="header-anchor" href="#dashboard-briefing-insight-pro" aria-label="Permalink to &quot;Dashboard briefing insight (Pro)&quot;">​</a></h2><p>The dashboard briefing hero shows a short, personal AI-written briefing for Pro workspaces. The server generates it once per calendar day and caches it for the rest of the day.</p><ul><li><strong>Voice.</strong> The briefing speaks in the first person (&quot;ik&quot;) and addresses the user informally (&quot;je&quot;). It opens with the single most urgent action, adds at most one or two supporting points, and closes with a concrete suggested next step (e.g. &quot;stuur Atelier Norden vandaag een herinnering&quot;). Warm, confident, concise — the tone of a smart assistant who knows the business.</li><li><strong>Model.</strong> The endpoint <code>POST /api/dashboard/briefing-insight</code> runs on Vertex AI <code>europe-west1</code> (Gemini 2.5 Flash). Ollama Cloud is not used for this path.</li><li><strong>Input signals.</strong> The client sends a full digest of the day&#39;s business data: liquidity and runway, revenue and profit (MTD + YTD), overdue receivables (count, total, worst customer), bills (due soon + overdue), draft counts, project margins, VAT position (balance, deadline, checklist progress, reserve), unbilled hours, recent payments, and new customers. All amounts are rounded to whole euros before reaching the model.</li><li><strong>Locales.</strong> The model generates the briefing in <code>nl/de/en/fr</code> based on the user&#39;s locale. The client includes the ISO 639-1 code with the request.</li><li><strong>Plan gating.</strong> The endpoint is gated on the <code>ai_insights</code> feature flag, which requires Pro. When a workspace is not entitled, the client keeps the deterministic lede alone.</li><li><strong>Fallback.</strong> On any failure (model unavailable, 403, network error) the client uses the existing deterministic lede. No error is shown to the user.</li><li><strong>Client UX.</strong> While the AI briefing loads, the hero shows the previous day&#39;s cached deterministic lede. When the AI version arrives, a cross-fade transition (opacity + slide) replaces it. The AI briefing appears with a sparkle icon and primary text color. A layout-matched skeleton shimmer (<code>BriefingSkeleton</code>) holds the entire dashboard shape until core data settles, then dissolves into a coordinated staggered entrance animation. Reduced-motion users get no animations.</li></ul><h2 id="plan-gating" tabindex="-1">Plan gating <a class="header-anchor" href="#plan-gating" aria-label="Permalink to &quot;Plan gating&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Free</th><th>Starter</th><th>Pro</th></tr></thead><tbody><tr><td>Contextual guide (incl. VAT tools)</td><td>Limited messages, FAQ-only on overflow</td><td>Standard tier</td><td>Highest tier</td></tr><tr><td>AI suggestions</td><td>Off</td><td>On</td><td>On</td></tr><tr><td>Vendor classifier</td><td>Off</td><td>On</td><td>On</td></tr><tr><td>Receipt scanner</td><td>Off</td><td>On</td><td>On</td></tr><tr><td>Text check</td><td>On</td><td>On</td><td>On</td></tr><tr><td>Translation</td><td>On (UI strings only)</td><td>On</td><td>On</td></tr><tr><td>Briefing insight</td><td>Off</td><td>Off</td><td>On</td></tr></tbody></table><h2 id="ai-usage-caps-monthly" tabindex="-1">AI usage caps (monthly) <a class="header-anchor" href="#ai-usage-caps-monthly" aria-label="Permalink to &quot;AI usage caps (monthly)&quot;">​</a></h2><p>AI caps are monthly, not daily. A bookkeeper who batches 40 receipts on a Friday does not blow through a daily quota that resets at midnight. Cap tracking uses <code>ai_usage.date</code> with the first-of-month date. Monthly caps:</p><table tabindex="0"><thead><tr><th>Metric</th><th>Free</th><th>Starter</th><th>Pro</th></tr></thead><tbody><tr><td>AI chat messages</td><td>10</td><td>100</td><td>1 000</td></tr><tr><td>AI receipt scans</td><td>3</td><td>50</td><td>500</td></tr><tr><td>AI suggestions</td><td>10</td><td>200</td><td>2 000</td></tr></tbody></table><h2 id="privacy" tabindex="-1">Privacy <a class="header-anchor" href="#privacy" aria-label="Permalink to &quot;Privacy&quot;">​</a></h2><p>All cloud AI paths route through Vertex AI in <code>europe-west1</code> (EU) by default. MyCompanyDesk has a signed DPA with Google Cloud for Vertex AI usage. Ollama Cloud (ollama.com, US-hosted) is disabled by default because no DPA or Standard Contractual Clauses exist with Ollama Inc. Workspaces can enable it explicitly, but it is off by default for all plans.</p><p>When <code>ai_processing_mode</code> is set to <code>local_only</code> on the workspace, every AI path that supports it (receipt scanner, AI suggestions, text check, vendor classifier, industry detection) routes through the local Ollama instance and never leaves the server. The contextual guide is cloud-only by design -- it requires the chat-tier model and is disabled in <code>local_only</code> mode rather than degraded.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("advanced/ai-features.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const aiFeatures = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  aiFeatures as default
};
