import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Expenses","description":"","frontmatter":{"title":"Expenses","last_verified":"2026-05-09T00:00:00.000Z"},"headers":[],"relativePath":"features/expenses.md","filePath":"features/expenses.md"}');
const _sfc_main = { name: "features/expenses.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="expenses" tabindex="-1">Expenses <a class="header-anchor" href="#expenses" aria-label="Permalink to &quot;Expenses&quot;">​</a></h1><p>Track business expenses, scan receipts, manage workspace categories with their own VAT defaults, and let depreciation schedules run automatically for investments.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The expenses page lists every recorded expense. Filter by category, project, date range or status. Click an expense to open the detail page; click <strong>New</strong> to add one manually, or <strong>Scan receipt</strong> to extract one from an image or PDF.</p><h2 id="categories" tabindex="-1">Categories <a class="header-anchor" href="#categories" aria-label="Permalink to &quot;Categories&quot;">​</a></h2><p>Categories are stored per workspace in the <code>expense_categories</code> table — they aren&#39;t a hardcoded enum. Each category carries:</p><ul><li>A <strong>key</strong> (slug used by the API and the form).</li><li>A <strong>VAT treatment</strong> default (see below).</li><li>A <strong>deduction percentage</strong> for partially-deductible categories.</li><li>An <strong>auto-flag investment</strong> flag that turns matching expenses into investments automatically.</li><li>A <strong>default useful-life</strong> in months for the depreciation engine.</li></ul><p>The setup wizard seeds an industry-tailored set on top of the eleven system defaults, so a software-heavy workspace ends up with extra keys like <code>cloud_services_eu</code>, while a hospitality workspace might get <code>food_client_meeting</code>. New categories from the wizard validate the same way as system defaults — unknown or mistyped keys are rejected with a 400. Edit, archive or add categories from the categories settings page.</p><h2 id="creating-an-expense" tabindex="-1">Creating an expense <a class="header-anchor" href="#creating-an-expense" aria-label="Permalink to &quot;Creating an expense&quot;">​</a></h2><h3 id="manual-entry" tabindex="-1">Manual entry <a class="header-anchor" href="#manual-entry" aria-label="Permalink to &quot;Manual entry&quot;">​</a></h3><ol><li>Go to <strong>Expenses &gt; New</strong>.</li><li>Type the <strong>supplier</strong> name. The supplier autocomplete suggests previously-used vendors as you type.</li><li>Pick a <strong>category</strong>. The form pulls the category&#39;s default VAT treatment, default VAT rate hint and deduction percentage straight into the matching fields. If a workspace-wide default expense category is set under workspace settings, it is pre-filled here.</li><li>Fill in the <strong>description</strong>, <strong>amount excl. VAT</strong>, <strong>VAT rate</strong> and <strong>date</strong>. The VAT rate and the <strong>payment method</strong> are pre-filled from your workspace defaults (set under workspace settings as &quot;Standard VAT rate expenses&quot;, default expense category and default payment method). The VAT amount is calculated automatically; you can also enter the inclusive amount and let the form back out the components.</li><li>Optionally set the <strong>VAT treatment</strong> (overrides the category default), <strong>customer</strong>, <strong>project</strong>, <strong>reference</strong> and <strong>notes</strong>. The pre-filled category and payment method can be overridden on a per-expense basis.</li><li>Optionally attach a <strong>receipt</strong>.</li><li>Click <strong>Save</strong>.</li></ol><h3 id="generate-from-supplier" tabindex="-1">Generate from supplier <a class="header-anchor" href="#generate-from-supplier" aria-label="Permalink to &quot;Generate from supplier&quot;">​</a></h3><p>When you&#39;re creating a new expense and you&#39;ve typed a supplier name, the <strong>Generate</strong> button (sparkles icon, top right) runs an LLM prefill. It uses the supplier and any partial inputs to suggest a description, category, VAT treatment, amount and date. If the form&#39;s VAT rate still matches your workspace default, the prefill will overwrite it with what it finds on the receipt; otherwise it leaves your override alone. Review the result before saving — the prefill is a draft, not an autopilot.</p><h3 id="from-bank-transactions" tabindex="-1">From bank transactions <a class="header-anchor" href="#from-bank-transactions" aria-label="Permalink to &quot;From bank transactions&quot;">​</a></h3><p>When your <a href="/features/bank">bank feed</a> is connected, categorised outgoing transactions are automatically turned into draft expenses. Each one carries the transaction&#39;s counterpart as supplier, the absolute amount as gross, the category&#39;s default VAT rate and treatment, and the transaction&#39;s booking date. Draft expenses first appear in the bank-feed review inbox, where you can confirm, edit, or reject them before they land in your books. See <a href="/features/bank#reviewing-auto-imported-expenses">reviewing auto-imported expenses</a> for the full workflow.</p><h3 id="receipt-scanner" tabindex="-1">Receipt scanner <a class="header-anchor" href="#receipt-scanner" aria-label="Permalink to &quot;Receipt scanner&quot;">​</a></h3><p>For image or PDF receipts:</p><ol><li>Go to <strong>Expenses &gt; Scan receipt</strong>.</li><li>Upload a JPEG, PNG, WebP or PDF file.</li><li>Choose <strong>Single</strong> (one expense from the receipt) or <strong>Multiple</strong> (split a receipt into separate expenses).</li><li>Review the extracted supplier, date, amount, VAT and category.</li><li>Confirm to create the expense(s).</li></ol><p>Receipt scanning is on the Pro plan and above.</p><h2 id="vat-treatment" tabindex="-1">VAT treatment <a class="header-anchor" href="#vat-treatment" aria-label="Permalink to &quot;VAT treatment&quot;">​</a></h2><p>Every expense has a <code>vat_treatment</code> field that decides how it lands on your VAT return:</p><table tabindex="0"><thead><tr><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>standard</code></td><td>Domestic VAT charged by the supplier — the default.</td></tr><tr><td><code>b2b_reverse_charge</code></td><td>Verleggingsregeling: you self-account for the VAT on an EU B2B purchase.</td></tr><tr><td><code>vat_exempt</code></td><td>The supply is exempt from VAT.</td></tr><tr><td><code>foreign_vat_charged</code></td><td>A non-EU supplier charged you VAT (typically reclaimable through the EU refund procedure).</td></tr></tbody></table><p>The treatment is normally inherited from the category default. Override it on a per-expense basis when reality differs — for example, a Software-category expense from a US vendor that did charge VAT instead of applying the EU reverse-charge default.</p><h2 id="multi-rate-lines" tabindex="-1">Multi-rate lines <a class="header-anchor" href="#multi-rate-lines" aria-label="Permalink to &quot;Multi-rate lines&quot;">​</a></h2><p>Receipts that mix VAT rates (a supermarket bill with food at 9% and drinks at 21%, say) are recorded on the expense&#39;s <code>lines</code> column — a JSONB array on the <code>expenses</code> table:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;description&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Food&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;amount_excl_vat&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">22.50</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;vat_rate&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">9</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,  </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;vat_amount&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2.03</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;description&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Drinks&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;amount_excl_vat&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">12.00</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;vat_rate&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">21</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;vat_amount&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2.52</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span></code></pre></div><p>When <code>lines</code> is null or empty, the flat fields (<code>amount_excl_vat</code>, <code>vat_rate</code>, <code>vat_amount</code>, <code>amount_incl_vat</code>) are the source of truth — the single-rate path is unchanged. When <code>lines</code> is present, the line totals drive the flat fields and aggregators iterate the lines for accuracy on the VAT return.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The form UI for adding lines from the screen is a follow-up — at the moment lines are settable through the API. Single-rate entry from the form works exactly as before.</p></div><h2 id="depreciation-for-investments" tabindex="-1">Depreciation for investments <a class="header-anchor" href="#depreciation-for-investments" aria-label="Permalink to &quot;Depreciation for investments&quot;">​</a></h2><p>Categories with <code>auto_flag_investment = true</code> (typically equipment and other capex) turn an expense into an investment automatically:</p><ul><li>The expense is marked <code>is_investment = true</code>.</li><li>A monthly depreciation schedule is generated using the category&#39;s <code>useful_life_months</code> (default 60 if unset).</li><li>The schedule uses straight-line depreciation with daily pro-rata for the first and last calendar month, in line with Belastingdienst guidance.</li><li>Lines live in <code>expense_depreciation_lines</code> and feed your reports.</li></ul><p>Editing the category, date or amount on an existing expense re-triggers the recompute — old lines are deleted and a fresh schedule is written. Toggling an expense out of an investment-flagged category cleans up the depreciation lines too.</p><h2 id="linking-and-filtering" tabindex="-1">Linking and filtering <a class="header-anchor" href="#linking-and-filtering" aria-label="Permalink to &quot;Linking and filtering&quot;">​</a></h2><p>Link expenses to:</p><ul><li><strong>Projects</strong> — Track project costs.</li><li><strong>Customers</strong> — Associate purchases with a client (e.g. for billable expenses).</li><li><strong>Suppliers</strong> — Free-form supplier name; reused vendors surface in the autocomplete.</li></ul><p>Filters on the list cover category, project, customer, date range and status (active vs archived).</p><h2 id="bulk-actions" tabindex="-1">Bulk actions <a class="header-anchor" href="#bulk-actions" aria-label="Permalink to &quot;Bulk actions&quot;">​</a></h2><p>Select multiple expenses for:</p><ul><li><strong>Categorize</strong> — Re-assign the category (re-runs the depreciation hook for newly-investment-flagged rows).</li><li><strong>Archive</strong> — Move to archive.</li><li><strong>Delete</strong> — Permanently remove.</li><li><strong>Export</strong> — Download as CSV.</li></ul><h2 id="recurring-expenses" tabindex="-1">Recurring expenses <a class="header-anchor" href="#recurring-expenses" aria-label="Permalink to &quot;Recurring expenses&quot;">​</a></h2><p>For predictable costs (rent, subscriptions, hosting), set up <a href="/features/recurring-expenses">recurring expenses</a> to generate the records on schedule.</p><h2 id="import" tabindex="-1">Import <a class="header-anchor" href="#import" aria-label="Permalink to &quot;Import&quot;">​</a></h2><p>Import historical expenses from CSV via <strong>Profile &gt; Import</strong> &gt; <strong>Expenses</strong>. Map your columns to the expense fields, preview, and confirm.</p><h2 id="tips" tabindex="-1">Tips <a class="header-anchor" href="#tips" aria-label="Permalink to &quot;Tips&quot;">​</a></h2><ul><li>Pick the right category first — VAT treatment, deduction percentage and the investment flag all flow from it.</li><li>Set your workspace default VAT rate, default expense category and default payment method in <strong>Workspace settings</strong> to stop the form starting with empty fields every time. A 0% VAT setting works correctly; the form treats it as intentional.</li><li>Use the per-expense VAT treatment override sparingly; if you find yourself overriding every entry in a category, the category default is wrong and should be edited.</li><li>Always attach receipts. The pre-filing checks on the <a href="/features/vat">VAT page</a> flag missing receipts before you file.</li><li>For mixed-rate receipts, use the lines API path until the form UI ships — single-rate entry is fine for everything else.</li><li>Investment expenses can take a moment to recompute when you change the date or amount. The depreciation lines refresh in the background.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("features/expenses.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const expenses = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  expenses as default
};
