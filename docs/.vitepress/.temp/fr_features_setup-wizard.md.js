import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Assistant de configuration","description":"","frontmatter":{"title":"Assistant de configuration","last_verified":"2026-05-19T00:00:00.000Z"},"headers":[],"relativePath":"fr/features/setup-wizard.md","filePath":"fr/features/setup-wizard.md"}');
const _sfc_main = { name: "fr/features/setup-wizard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="assistant-de-configuration" tabindex="-1">Assistant de configuration <a class="header-anchor" href="#assistant-de-configuration" aria-label="Permalink to &quot;Assistant de configuration&quot;">​</a></h1><p>Une configuration magique en cinq etapes sur <code>/setup</code> qui capture votre identite d&#39;entreprise, recupere eventuellement votre inscription aupres de la KvK ou d&#39;un registre europeen, choisit une adresse web, genere des suggestions de marque et applique tout en un clic. L&#39;assistant est la porte d&#39;entree pour les nouveaux utilisateurs et reste disponible ensuite pour des ajustements.</p><p>Si vous cherchez le guide de base, commencez par <a href="/fr/getting-started/company-setup">Configurer votre entreprise</a>. Cette page est la reference approfondie : chaque etape, chaque option, chaque consequence.</p><h2 id="quand-l-assistant-est-propose" tabindex="-1">Quand l&#39;assistant est propose <a class="header-anchor" href="#quand-l-assistant-est-propose" aria-label="Permalink to &quot;Quand l&#39;assistant est propose&quot;">​</a></h2><p>L&#39;assistant est accessible de deux manieres :</p><ul><li><strong>Direct :</strong> naviguez vers <code>/setup</code> a tout moment.</li><li><strong>Banniere du tableau de bord :</strong> tant que l&#39;integration n&#39;est pas terminee, une banniere fermable &quot;Terminer la configuration&quot; s&#39;affiche en haut de <code>/dashboard</code>. Elle montre un decompte des champs en attente (ou un message generique &quot;Completer votre profil&quot; si le decompte est a zero) et un bouton retournant a l&#39;assistant. Un bouton de fermeture (icone X) masque la banniere par navigateur via localStorage, la maintenant masquee d&#39;un rechargement a l&#39;autre jusqu&#39;a ce que l&#39;assistant soit termine.</li></ul><p>L&#39;assistant est non bloquant : l&#39;ancienne redirection forcee vers <code>/setup</code> a l&#39;inscription (la porte <code>ONBOARDING_MANDATORY</code>) a ete supprimee. Les nouvelles inscriptions arrivent directement sur <code>/dashboard</code> et voient la banniere fermable a la place.</p><h2 id="mise-en-page" tabindex="-1">Mise en page <a class="header-anchor" href="#mise-en-page" aria-label="Permalink to &quot;Mise en page&quot;">​</a></h2><p>L&#39;assistant est une page unique (<code>/setup</code>) avec cinq etapes dans un stepper. Chaque etape ecrit ses reponses dans une colonne JSONB (<code>companies.onboarding_state</code>) a chaque modification, ce qui rend l&#39;assistant entierement reprenable : fermez l&#39;onglet au milieu d&#39;une etape et la prochaine visite vous ramene a la meme etape avec les memes reponses.</p><p>Trois boutons se trouvent en bas de chaque etape :</p><ul><li><strong>Retour</strong> — aller a l&#39;etape precedente (masque a l&#39;etape 1).</li><li><strong>Passer pour l&#39;instant</strong> — quitte sans marquer l&#39;integration comme terminee. La banniere du tableau de bord reste affichee pour que l&#39;utilisateur puisse revenir.</li><li><strong>Continuer</strong> / <strong>Terminer</strong> — controle par etape selon les reponses ci-dessous.</li></ul><p>Il n&#39;y a pas de &quot;enregistrer et quitter&quot; — l&#39;enregistrement est automatique. <strong>Passer pour l&#39;instant</strong> est une sortie propre, pas un rejet.</p><h2 id="etape-1-—-entreprise" tabindex="-1">Etape 1 — Entreprise <a class="header-anchor" href="#etape-1-—-entreprise" aria-label="Permalink to &quot;Etape 1 — Entreprise&quot;">​</a></h2><p>Capture les deux reponses dont depend chaque etape suivante.</p><table tabindex="0"><thead><tr><th>Champ</th><th>Stocke comme</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Obligatoire</th><th>Notes</th></tr></thead><tbody><tr><td>Nom de l&#39;entreprise</td><td><code>answers.businessName</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">oui</td><td>Utilise comme <code>display_name</code> et <code>company_name</code> a la fin (le nom legal de l&#39;etape registre l&#39;emporte pour <code>company_name</code> si les deux sont presents). Pre-remplit egalement la suggestion de slug de sous-domaine a l&#39;etape 3.</td></tr><tr><td>Ce que vous faites</td><td><code>answers.what</code></td><td style="${ssrRenderStyle({ "text-align": "center" })}">oui</td><td>Texte libre, 1–2 phrases. Envoye au generateur de suggestions (Gemini) de l&#39;etape magique avec le pays et le secteur.</td></tr></tbody></table><p><strong>Continuer est desactive</strong> jusqu&#39;a ce que les deux champs aient des valeurs non vides et nettoyees.</p><h2 id="etape-2-—-registre" tabindex="-1">Etape 2 — Registre <a class="header-anchor" href="#etape-2-—-registre" aria-label="Permalink to &quot;Etape 2 — Registre&quot;">​</a></h2><p>Choisit un pays et recherche l&#39;entreprise dans le registre officiel du pays, saisit les donnees manuellement, ou passe l&#39;etape.</p><h3 id="trois-voies" tabindex="-1">Trois voies <a class="header-anchor" href="#trois-voies" aria-label="Permalink to &quot;Trois voies&quot;">​</a></h3><ol><li><strong>Recherche:</strong> saisie predictive par nom d&#39;entreprise, choisissez une correspondance et laissez le backend recuperer le Basisprofiel (0,02 EUR par appel pour NL). Le champ de recherche n&#39;est affiche que lorsque l&#39;API KVK payante (<code>KVK_API_KEY</code>) est configuree sur le conteneur API. Lorsque la cle est absente, la recherche est sautee et l&#39;assistant demarre par defaut en saisie manuelle (voie 2).</li><li><strong>Manuel:</strong> saisissez le nom de l&#39;entreprise, le numero KVK (facultatif), l&#39;adresse, le code postal et la ville vous-meme. Les donnees sont ecrites directement dans la ligne de l&#39;entreprise via <code>PUT /company-settings/company</code> et <code>answers.kvk</code> est marque avec <code>manual: true</code>. La saisie manuelle est le comportement par defaut lorsque KVK_API_KEY n&#39;est pas defini, et existe aussi pour deux scenarios supplementaires meme lorsque la recherche est disponible : (a) les nouvelles entreprises qui ne sont pas encore dans le jeu de donnees gratuit OpenKVK, et (b) les entreprises dont le nom commercial ne correspond pas a ce que l&#39;utilisateur a tape dans la recherche.</li><li><strong>Passer</strong> — &quot;Pas de numero KvK?&quot; stocke <code>answers.kvk = null</code>. L&#39;assistant continue; les donnees de l&#39;entreprise peuvent etre remplies plus tard dans les parametres.</li></ol><p>Le passage de la recherche au manuel se fait en un clic (lorsque la recherche est disponible) : un bouton &quot;Saisir manuellement&quot; apparait sous les resultats de recherche, et un lien &quot;Retour a la recherche KVK&quot; se trouve en haut du formulaire manuel. Lorsque <code>KVK_API_KEY</code> est absent, le bouton de retour a la recherche est entierement masque et l&#39;utilisateur commence et reste en saisie manuelle.</p><h3 id="options-de-pays" tabindex="-1">Options de pays <a class="header-anchor" href="#options-de-pays" aria-label="Permalink to &quot;Options de pays&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Code</th><th>Registre affiche</th><th style="${ssrRenderStyle({ "text-align": "center" })}">Recherche active</th></tr></thead><tbody><tr><td><code>NL</code></td><td>KvK</td><td style="${ssrRenderStyle({ "text-align": "center" })}">oui</td></tr><tr><td><code>FR</code></td><td>SIRENE</td><td style="${ssrRenderStyle({ "text-align": "center" })}">oui</td></tr><tr><td><code>GB</code></td><td>Companies House</td><td style="${ssrRenderStyle({ "text-align": "center" })}">oui</td></tr><tr><td><code>DE</code></td><td>Handelsregister</td><td style="${ssrRenderStyle({ "text-align": "center" })}">non — saisie manuelle</td></tr><tr><td><code>OTHER</code></td><td>(aucun registre)</td><td style="${ssrRenderStyle({ "text-align": "center" })}">non — saisie manuelle</td></tr></tbody></table><p><code>NL</code> est la valeur par defaut. Le pays determine plusieurs choses a la fin : fuseau horaire (<code>Europe/Amsterdam</code> / <code>Europe/Berlin</code> / <code>Europe/Paris</code> / <code>Europe/London</code>), <code>pdf_language</code> (<code>nl</code> / <code>de</code> / <code>fr</code> / <code>en</code>), et le libelle du registre integre dans <code>footer_text</code> (<code>KvK 12345678</code>, <code>Handelsregister …</code>, <code>SIRENE …</code>, <code>CRN …</code>).</p><h3 id="mode-recherche" tabindex="-1">Mode recherche <a class="header-anchor" href="#mode-recherche" aria-label="Permalink to &quot;Mode recherche&quot;">​</a></h3><p>Pour les pays pris en charge, l&#39;utilisateur recherche par nom d&#39;entreprise. Le backend appelle l&#39;API publique correspondante et renvoie l&#39;un des resultats suivants :</p><ul><li><strong>ok</strong> — <code>legalName</code>, <code>address</code>, <code>postalCode</code>, <code>city</code>, <code>sector</code> remplis et stockes sous <code>answers.registry</code>.</li><li><strong>not-configured</strong> — fournisseur pas encore branche dans cet environnement ; l&#39;utilisateur est invite a passer a la saisie manuelle.</li><li><strong>not-found</strong> — numero non trouve ; l&#39;utilisateur peut reessayer ou passer en manuel.</li><li><strong>invalid</strong> — format du numero rejete.</li><li><strong>error</strong> — erreur du fournisseur ; la saisie manuelle reste une option.</li></ul><h4 id="kvk-basisprofiel-pays-bas-uniquement" tabindex="-1">KvK Basisprofiel (Pays-Bas uniquement) <a class="header-anchor" href="#kvk-basisprofiel-pays-bas-uniquement" aria-label="Permalink to &quot;KvK Basisprofiel (Pays-Bas uniquement)&quot;">​</a></h4><p>Pour les espaces de travail NL, la recherche KvK est un processus en deux etapes :</p><ol><li><strong>Typeahead</strong> — l&#39;utilisateur recherche par nom d&#39;entreprise. Le point de terminaison <code>zoeken</code> (gratuit) renvoie les entrees correspondantes. C&#39;est l&#39;etape d&#39;autocompletion qui alimente les reponses <code>ok</code> / <code>not-found</code> existantes. Lorsque la recherche ne renvoie aucun resultat, l&#39;UI affiche un panneau d&#39;etat vide (titre, explication et un bouton &quot;Remplir manuellement&quot; qui pre-remplit le formulaire manuel avec ce que l&#39;utilisateur a deja tape). C&#39;est frequent car le niveau gratuit d&#39;OpenKVK ne couvre pas beaucoup de jeunes entreprises.</li><li><strong>Basisprofiel</strong> — une fois une correspondance choisie, l&#39;assistant appelle le point de terminaison de detail Basisprofiel de la KvK. C&#39;est un appel payant (0,02 EUR, mis en cache 24h par numero KVK). Il renvoie le profil complet : <code>legalName</code>, <code>statutaireNaam</code> (nom statutaire), <code>tradeNames</code> (tous les noms commerciaux enregistres, tries), <code>rsin</code>, <code>legalForm</code>, <code>dateFounded</code>, adresses de visite et postales, codes SBI avec indicateur primaire, <code>employeeCount</code> et <code>indNonMailing</code> (indicateur pas-de-courrier). L&#39;assistant pre-remplit <code>business_page_hero_tagline</code> avec le premier nom commercial et la ville (&quot;[nom commercial] a [ville]&quot;), et <code>description</code> avec la description SBI principale (limitee a 280 caracteres). Les deux utilisent COALESCE pour que les modifications manuelles dans Parametres soient preservees.</li></ol><p>Lorsqu&#39;une correspondance est choisie (mode recherche), le frontend appelle immediatement <code>PUT /company-settings/company</code> avec le <code>name</code> de la correspondance comme <code>display_name</code>, le <code>kvkNumber</code> comme <code>chamber</code>, <code>city</code>, et <code>country</code> code en dur sur <code>&quot;NL&quot;</code>. Cela ecrit le nom d&#39;affichage visible par le client immediatement, afin que le message d&#39;accueil du tableau de bord et le site ensemence utilisent le bon nom des le moment ou l&#39;utilisateur clique sur la correspondance, plutot que d&#39;attendre l&#39;etape de fin de l&#39;assistant.</p><p>Il y a deux flags de fonctionnalite independants pour les recherches KVK aux Pays-Bas :</p><ul><li><code>KVK_API_KEY</code> (variable d&#39;environnement sur le conteneur API) : lorsque cette cle est absente, le champ de recherche n&#39;est pas du tout affiche et l&#39;assistant demarre par defaut en saisie manuelle a cette etape. Le niveau gratuit OpenKVK seul est trop limite (~2% de taux de succes, manque presque toutes les nouvelles inscriptions). Le flag bascule automatiquement des que la cle est definie.</li><li><code>KVK_BASISPROFIEL_ENABLED</code> : lorsque desactive (ou lorsque <code>KVK_API_KEY</code> est absent), l&#39;assistant utilise uniquement le resultat gratuit <code>zoeken</code> (les memes champs <code>ok</code> que ci-dessus). Lorsque active et que <code>KVK_API_KEY</code> est present, les donnees Basisprofiel enrichissent la charge utile <code>answers.registry</code> et l&#39;etape de Verification montre chaque champ qui sera ecrit dans la ligne de l&#39;entreprise.</li></ul><h3 id="mode-manuel" tabindex="-1">Mode manuel <a class="header-anchor" href="#mode-manuel" aria-label="Permalink to &quot;Mode manuel&quot;">​</a></h3><p>L&#39;utilisateur remplit <code>chosen</code> (son numero d&#39;enregistrement), et eventuellement <code>legalName</code>, <code>address</code>, <code>sector</code>. Les quatre champs sont optionnels dans ce mode.</p><p>Lors de l&#39;enregistrement, le frontend appelle <code>PUT /company-settings/company</code> directement avec les valeurs saisies manuellement, plus <code>display_name</code> defini sur le nom d&#39;entreprise saisi ici et <code>country</code> code en dur sur <code>&quot;NL&quot;</code>. Cela ecrit immediatement le nom visible par le client et le pays, au lieu d&#39;attendre l&#39;etape de fin. La logique d&#39;application de l&#39;etape de fin lit toujours <code>answers.kvk</code> pour le chemin de registre herite, mais le nouveau chemin de l&#39;assistant en 2 etapes lit <code>answers.kvk.legalName</code> et <code>answers.kvk.kvkNumber</code> directement, de sorte que le PUT inline pendant cette etape est l&#39;ecriture autoritaire.</p><h3 id="mode-passer" tabindex="-1">Mode passer <a class="header-anchor" href="#mode-passer" aria-label="Permalink to &quot;Mode passer&quot;">​</a></h3><p>Un bouton &quot;Pas encore enregistre&quot; stocke <code>answers.registry = null</code>. <strong>Continuer est toujours autorise a cette etape</strong>, quel que soit le mode — les donnees du registre sont une commodite, pas un obstacle.</p><h2 id="etape-3-—-domaine" tabindex="-1">Etape 3 — Domaine <a class="header-anchor" href="#etape-3-—-domaine" aria-label="Permalink to &quot;Etape 3 — Domaine&quot;">​</a></h2><p>Choisissez l&#39;adresse web que vos clients verront sur la page d&#39;entreprise publique et dans votre boite de reception. Trois chemins, affiches sous forme de cartes, couvrent chaque scenario, du demarrage rapide gratuit a l&#39;achat d&#39;un domaine directement dans l&#39;assistant.</p><h3 id="selecteur-a-trois-choix" tabindex="-1">Selecteur a trois choix <a class="header-anchor" href="#selecteur-a-trois-choix" aria-label="Permalink to &quot;Selecteur a trois choix&quot;">​</a></h3><p>Une grille de trois cartes presente le choix. La selection d&#39;un chemin revele l&#39;editeur correspondant en dessous ; un seul chemin est actif a la fois.</p><p><strong>Sous-domaine (gratuit) :</strong> l&#39;utilisateur choisit un slug ; un selecteur de TLD permet de choisir entre <code>.mycompanydesk.nl</code> et <code>.mycompanydesk.com</code>. Le slug est pre-rempli a partir du nom legal KVK lorsqu&#39;il est disponible (minuscules, accents supprimes, caracteres non-ASCII supprimes, tronque a 63 caracteres), de sorte que la plupart des utilisateurs peuvent cliquer-et-continuer sans taper. La disponibilite est verifiee en direct avec un delai de 350 ms pendant la saisie. A la fin, le sous-domaine est provisionne via l&#39;API Cloudflare et le site web de l&#39;entreprise est immediatement accessible.</p><p>Lorsque l&#39;assistant est execute dans le flux en 2 etapes (controle par le plan), l&#39;etape Domaine est entierement omise. L&#39;etape de fin provisionne automatiquement un sous-domaine d&#39;espace de travail a partir de la valeur <code>display_name</code> : le slug est derive du nom d&#39;affichage (avec suffixes de reessai en cas de collision jusqu&#39;a 5 tentatives), et <code>activateSubdomain</code> l&#39;enregistre comme URL publique du site. Meilleur effort : une collision ou une erreur est journalisee et n&#39;empeche pas l&#39;assistant de terminer.</p><p><strong>Domaine personnel :</strong> l&#39;utilisateur saisit un domaine qu&#39;il possede deja. Une validation regex en direct verifie le format pendant la saisie (<code>[a-z0-9][a-z0-9-]*(.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+</code>). A la fin, l&#39;assistant ajoute le domaine a la liste des domaines de l&#39;espace de travail (sans effet s&#39;il etait deja ajoute) et active automatiquement la boite de reception : <code>info@&lt;domaine&gt;</code> comme boite par defaut plus les alias <code>support@</code>, <code>sales@</code> et <code>noreply@</code>. Le chemin 409-deja-existant de <code>apply.service</code> est gere avec elegance.</p><p>Si le domaine ne pointe pas encore vers les serveurs de noms de MCD, la fin redirige vers <code>/workspace/organization/company/address</code> pour que l&#39;utilisateur voie immediatement les instructions DNS et un bouton <strong>Verifier</strong>. Sinon, cela va au tableau de bord.</p><p><strong>Enregistrer un domaine :</strong> integre les composants live <code>DomainPurchaseCard</code> + <code>DomainClaimModal</code> de l&#39;interface des parametres. L&#39;utilisateur peut rechercher un domaine, verifier la disponibilite et le prix, puis l&#39;acheter via OpenProvider ou le reclamer gratuitement en tant que Founding Member. En cas de reclamation ou d&#39;achat reussi, le domaine est deja attache a l&#39;espace de travail cote serveur via le flux <code>/api/domain-purchase</code>, de sorte que l&#39;assistant enregistre la reponse comme <code>mode=&#39;own&#39;</code> avec le nom enregistre et <code>registered: true</code> ; <code>apply.service</code> le traite comme un re-ajout sans effet. Une banniere de succes verte affiche le nom de domaine enregistre et laisse l&#39;utilisateur continuer.</p><p>Si l&#39;utilisateur ouvre le chemin Enregistrer mais ne finalise pas l&#39;achat, l&#39;etape est marquee comme sautee pour que l&#39;assistant puisse continuer. Il peut revenir plus tard via <code>Entreprise &gt; Votre propre adresse .com</code> quand il est pret.</p><h3 id="revenir-d-un-domaine-personnel-a-un-sous-domaine" tabindex="-1">Revenir d&#39;un domaine personnel a un sous-domaine <a class="header-anchor" href="#revenir-d-un-domaine-personnel-a-un-sous-domaine" aria-label="Permalink to &quot;Revenir d&#39;un domaine personnel a un sous-domaine&quot;">​</a></h3><p>Si l&#39;espace de travail possede deja un domaine de site web personnalise, l&#39;assistant affiche un avertissement avant de laisser l&#39;utilisateur passer au chemin du sous-domaine — <code>activateSubdomain</code> refuse les entreprises avec un domaine personnalise encore attache, et l&#39;echec ne serait sinon visible qu&#39;a la fin.</p><h2 id="etape-4-—-magie" tabindex="-1">Etape 4 — Magie <a class="header-anchor" href="#etape-4-—-magie" aria-label="Permalink to &quot;Etape 4 — Magie&quot;">​</a></h2><p>Genere des suggestions de marque a partir des reponses saisies jusqu&#39;a present. Champs :</p><ul><li><strong>Couleur de marque</strong> — hex. Si l&#39;utilisateur telecharge un logo a cette etape, la couleur dominante non blanche est extraite du raster (via <code>sharp</code>) et utilisee ; cela remplace la suggestion Gemini, et un flag (<code>brandColorFromLogo</code>) empeche une regeneration ulterieure de l&#39;ecraser. Les logos SVG uniquement et les entrees principalement blanches reviennent a la palette Gemini.</li><li><strong>Accroche</strong> — courte ligne hero pour la page d&#39;entreprise publique.</li><li><strong>Texte a propos</strong> — paragraphe pour la page d&#39;entreprise.</li><li><strong>Services</strong> — liste de jusqu&#39;a 8 noms de services. Chacun est insere dans <code>company_services</code> a la fin, mais <strong>uniquement si l&#39;entreprise n&#39;a encore aucun service</strong> — l&#39;assistant n&#39;ecrase jamais une liste de services existante.</li><li><strong>Ton de l&#39;e-mail</strong> — <code>formal</code> / <code>friendly</code> / <code>casual</code>. Correspond au style d&#39;e-mail : <code>formal → classic</code>, <code>friendly</code> et <code>casual → modern</code>.</li><li><strong>Modeles d&#39;e-mail</strong> — cles par type de modele (<code>invoice_default</code>, <code>reminder_default</code>, etc.), chacun avec <code>subject</code> + <code>body</code>. Enregistres via le depot de modeles d&#39;e-mail a la fin.</li></ul><p>L&#39;utilisateur peut modifier chaque suggestion en ligne avant de continuer. <strong>Continuer est desactive</strong> jusqu&#39;a ce que des suggestions existent (cliquer sur <strong>Generer des suggestions</strong> au moins une fois).</p><h3 id="telechargement-de-logo" tabindex="-1">Telechargement de logo <a class="header-anchor" href="#telechargement-de-logo" aria-label="Permalink to &quot;Telechargement de logo&quot;">​</a></h3><p>Cliquer sur la vignette de telechargement permet a l&#39;utilisateur de choisir une image jusqu&#39;a 4 Mo. Le fichier est envoye en tant qu&#39;URI de donnees base64 vers <code>/onboarding/upload-logo</code>, qui :</p><ol><li>Le stocke via le chemin canonique des logos d&#39;entreprise.</li><li>Renvoie la couleur dominante non blanche, qui est automatiquement appliquee a la suggestion de couleur de marque.</li><li>Definit <code>answers.logoUploaded = true</code> afin que le texte de Verification s&#39;adapte (&quot;nous utiliserons votre logo&quot; au lieu de &quot;nous creerons un logo a initiales a partir du nom de votre entreprise&quot;) et que l&#39;etape d&#39;application ignore le generateur d&#39;initiales.</li></ol><p>Si l&#39;utilisateur avait deja un logo, il est affiche comme apercu existant (&quot;vous avez deja un logo&quot;) au lieu d&#39;un CTA de telechargement vide.</p><h2 id="etape-5-—-verification" tabindex="-1">Etape 5 — Verification <a class="header-anchor" href="#etape-5-—-verification" aria-label="Permalink to &quot;Etape 5 — Verification&quot;">​</a></h2><p>Diff en lecture seule de chaque champ que l&#39;assistant modifierait. Deux sections :</p><ul><li><strong>Modifications</strong> — lignes <code>actuel → nouveau</code>. Inclut un echantillon pour <code>brandColor</code>. Seuls les champs que l&#39;assistant a ecrits et qui different de la ligne actuelle de l&#39;entreprise apparaissent ici.</li><li><strong>Deja defini</strong> — champs que l&#39;assistant a captures mais qui correspondent deja a la ligne de l&#39;entreprise.</li></ul><p>Champs affiches : nom de l&#39;entreprise, pays, numero de registre, nom legal, adresse, couleur de marque, accroche, texte a propos, resume du domaine (<code>info@&lt;domaine-resolu&gt;</code> est affiche si un domaine est defini).</p><p>Le bouton <strong>Terminer</strong> dans le pied de page appelle <code>/onboarding/complete</code>. La locale actuelle est transmise pour que le texte par defaut (par ex. les instructions de paiement) soit localise.</p><h2 id="ce-que-terminer-applique-reellement" tabindex="-1">Ce que Terminer applique reellement <a class="header-anchor" href="#ce-que-terminer-applique-reellement" aria-label="Permalink to &quot;Ce que Terminer applique reellement&quot;">​</a></h2><p><code>apply.service.js</code> parcourt les reponses et les ecrit dans la ligne reelle de l&#39;entreprise. Deux semantiques, deliberement separees :</p><p><strong>Toujours ecraser</strong> lorsque la reponse de l&#39;assistant est une chaine non vide et differe de la valeur actuelle :</p><ul><li><code>display_name</code>, <code>company_name</code></li><li><code>country</code> (revient a <code>&quot;NL&quot;</code> si <code>answers.country</code> n&#39;est pas defini , l&#39;assistant en 2 etapes ne demande pas le pays, donc NL est la valeur par defaut implicite)</li><li><code>chamber</code> (via <code>answers.kvk.kvkNumber</code> lorsque present)</li></ul><p>Si l&#39;assistant en 2 etapes n&#39;a pas capture de reponse de domaine, la fin provisionne automatiquement un sous-domaine d&#39;espace de travail a partir de la valeur <code>display_name</code> lorsque le plan autorise les sous-domaines et que l&#39;entreprise n&#39;a pas de domaine personnalise. Le slug est derive de <code>display_name</code> (minuscules, ASCII, max 60 caracteres, reessaye avec suffixe <code>-2</code>…<code>-5</code> en cas de collision).</p><ul><li><code>chamber</code>, <code>address</code>, <code>postal_code</code>, <code>city</code></li><li><code>brand_color</code>, <code>description</code>, <code>business_page_hero_tagline</code></li></ul><p><strong>Remplir uniquement les vides</strong> (ne jamais ecraser un choix manuel existant, pour que les reexecutions ne surprennent pas les utilisateurs) :</p><ul><li><code>second_accent_color</code> (deduit de la couleur de marque si vide)</li><li><code>email_style</code> (du ton de l&#39;e-mail)</li><li><code>timezone</code> (par defaut du pays)</li><li><code>pdf_language</code> (par defaut du pays)</li><li><code>footer_text</code> (style <code>Nom commercial - KvK 12345678</code>)</li><li><code>payment_options_enabled</code> (<code>bank_transfer</code>)</li><li><code>payment_default_method</code> (<code>bank_transfer</code>)</li><li><code>payment_instructions</code> (texte par defaut localise)</li><li><code>email_footer_show_website</code>, <code>email_footer_show_support_email</code>, <code>email_footer_show_business_page</code> (booleens)</li></ul><p>Effets secondaires au-dela des ecritures de colonnes :</p><ul><li><strong>Logo a initiales</strong> est genere a partir du nom de l&#39;entreprise + couleur de marque, mais uniquement si <code>logo_path</code> et <code>logo_svg</code> sont tous les deux vides. Entierement ignore si l&#39;utilisateur a telecharge un vrai logo a l&#39;etape Magie.</li><li><strong>Services</strong> — jusqu&#39;a 8 insertions dans <code>company_services</code>, uniquement si l&#39;entreprise n&#39;en a encore aucun.</li><li><strong>Modeles d&#39;e-mail</strong> — enregistres par type via le depot de modeles.</li><li><strong>Amorcage du site web</strong> — lors de la premiere fin qui trouve zero page dans l&#39;espace de travail, <code>apply.service.js</code> cree un site par defaut avec une page d&#39;accueil en brouillon (<code>/</code>, modele &quot;home&quot;, <code>is_home: true</code>) et remplit les jetons de design avec la couleur de marque capturee dans l&#39;assistant. Les reexecutions ignorent la creation lorsqu&#39;une page existe deja, de sorte que l&#39;assistant n&#39;ecrase jamais les modifications manuelles.</li><li><strong>Domaine</strong> — <code>activateSubdomain</code> pour le chemin du sous-domaine, ou <code>addDomain</code> + <code>quickEnableInbox</code> (+ boite personnelle optionnelle) pour le chemin du domaine personnel.</li></ul><p>Si le provisionnement du domaine echoue, le reste de la fin est toujours applique — l&#39;echec s&#39;affiche sur l&#39;ecran de fin avec un code d&#39;erreur specifique (<code>subdomain_failed</code>, <code>domain_failed</code>, <code>inbox_enable_failed</code>, <code>personal_mailbox_failed</code>, <code>personal_mailbox_remove_failed</code>, <code>personal_mailbox_list_failed</code>) traduit en une ligne lisible par l&#39;utilisateur.</p><h2 id="ecran-de-fin" tabindex="-1">Ecran de fin <a class="header-anchor" href="#ecran-de-fin" aria-label="Permalink to &quot;Ecran de fin&quot;">​</a></h2><p>Un panneau de confirmation vert qui s&#39;affiche pendant 4,2 secondes (ou 0,9 seconde lorsque rien n&#39;a ete provisionne) avant de rediriger. Il liste chaque element que l&#39;assistant a configure dans une liste de cartes recapitulatives :</p><ul><li><strong>Categories</strong> — les categories de depenses systeme creees pour l&#39;espace de travail (par ex. &quot;Bureau, Voyages, Logiciels&quot;).</li><li><strong>Services</strong> — nombre de services ajoutes a la page d&#39;entreprise.</li><li><strong>Logo</strong> — confirmation lorsqu&#39;un logo a initiales a ete genere.</li><li><strong>Modeles d&#39;e-mail</strong> — nombre de modeles enregistres dans la tonalite de marque de l&#39;espace de travail.</li><li><strong>Site web</strong> — confirmation qu&#39;un site par defaut avec page d&#39;accueil, navigation et couleur de marque est pret. Visible uniquement lorsque le site vient d&#39;etre cree.</li><li><strong>Domaine</strong> — l&#39;URL prete pour les sous-domaines ou le domaine ajoute pour les configurations de domaine personnel.</li></ul><p>Deux boutons apparaissent sous le recapitulatif :</p><ul><li><strong>Aller au tableau de bord</strong> — amene l&#39;utilisateur directement a <code>/dashboard</code>, en sautant le minuteur de redirection automatique.</li><li><strong>Ouvrir le constructeur de site</strong> — visible uniquement lorsqu&#39;un site a ete cree. Amene l&#39;utilisateur directement a <code>/website</code> afin qu&#39;il puisse commencer a modifier sa page d&#39;accueil.</li></ul><p>Ensuite, l&#39;utilisateur arrive sur <code>/dashboard</code> (chemin par defaut), <code>/website</code> (lorsqu&#39;il clique sur le CTA du constructeur de site), ou <code>/workspace/organization/company/address</code> (lorsqu&#39;un domaine personnel est en attente de verification DNS).</p><h2 id="reexecuter-l-assistant" tabindex="-1">Reexecuter l&#39;assistant <a class="header-anchor" href="#reexecuter-l-assistant" aria-label="Permalink to &quot;Reexecuter l&#39;assistant&quot;">​</a></h2><p>L&#39;assistant est entierement reexecutable. Les utilisateurs recurrents arrivent a l&#39;etape 1 avec leurs reponses actuelles visibles. Rien ne les oblige a passer par chaque etape — ils peuvent modifier un champ sur une etape et cliquer sur Terminer.</p><p>Le diff de l&#39;etape de Verification est le filet de securite : il montre a l&#39;utilisateur chaque ecrasement qui va se produire. Les champs par defaut raisonnables (fuseau horaire, pdf_language, valeurs par defaut de paiement, texte de pied de page) ne sont pas dans le diff car Terminer ne fait que remplir les vides pour ceux-ci — les modifier silencieusement surprendrait les utilisateurs qui les ont definis deliberement.</p><p>Pour revoir les parametres individuels sans l&#39;assistant, allez a :</p><ul><li><code>/workspace/organization/company/about</code> — nom, enregistrement, adresse, TVA.</li><li><code>/workspace/organization/company/look</code> — couleur de marque, logo.</li><li><code>/workspace/organization/company/website</code> — accroche, texte a propos, services.</li><li><code>/workspace/organization/company/address</code> — domaine personnel + DNS.</li><li><code>/workspace/email</code> — boite de reception, boites aux lettres, modeles.</li></ul><p>La carte complète se trouve dans l&#39;<a href="/fr/settings/">aperçu des paramètres</a>.</p><h2 id="cas-particuliers" tabindex="-1">Cas particuliers <a class="header-anchor" href="#cas-particuliers" aria-label="Permalink to &quot;Cas particuliers&quot;">​</a></h2><ul><li><strong>Passer une etape.</strong> Continuer est controle par etape sur les reponses minimales requises. L&#39;etape Registre n&#39;a pas d&#39;obstacle ; Domaine necessite un chemin choisi avec une valeur non vide, ou un achat termine pour le chemin Enregistrer, ou le drapeau passe ; Magie necessite que Generer ait ete execute ; Entreprise et Verification ont leurs propres obstacles.</li><li><strong>Fermer au milieu d&#39;une etape.</strong> Chaque reponse est enregistree a la modification, donc la prochaine visite reprend la ou l&#39;utilisateur s&#39;etait arrete. L&#39;index d&#39;etape est egalement enregistre (<code>answers</code> et <code>currentStep</code> vivent dans la meme colonne JSONB).</li><li><strong>Changer d&#39;avis a l&#39;etape Domaine.</strong> Passer de <code>personnel</code> a <code>sous-domaine</code> apres avoir saisi un domaine reecrit <code>answers.domain</code> a <code>null</code> jusqu&#39;a ce que l&#39;utilisateur choisisse un slug. Passer au chemin Enregistrer enregistre une reponse passee pour qu.un nouvel inscrit ne soit pas bloque s.il ouvre Enregistrer mais reporte l.achat. Passer a un sous-domaine lorsqu.un domaine personnalise est deja attache affiche un avertissement prealable.</li><li><strong>Echec de l&#39;extraction du logo.</strong> Les logos principalement blancs et les entrees SVG uniquement que <code>sharp</code> ne peut pas rasteriser renvoient <code>color: null</code>. La suggestion de couleur de marque Gemini est alors utilisee.</li><li><strong>Domaine deja ajoute a la fin du domaine personnel.</strong> Un 409 de <code>addDomain</code> revient a la ligne existante pour que l&#39;etape d&#39;activation de la boite de reception soit toujours executee.</li><li><strong>Boite personnelle existe deja.</strong> Un 409 de <code>createMailbox</code> est traite comme un succes.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("fr/features/setup-wizard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const setupWizard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  setupWizard as default
};
