import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Bank Feed","description":"","frontmatter":{"title":"Bank Feed","last_verified":"2026-05-16T00:00:00.000Z"},"headers":[],"relativePath":"features/bank.md","filePath":"features/bank.md"}');
const _sfc_main = { name: "features/bank.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="bank-feed" tabindex="-1">Bank Feed <a class="header-anchor" href="#bank-feed" aria-label="Permalink to &quot;Bank Feed&quot;">​</a></h1><p>Connect your bank accounts to MyCompanyDesk for automatic transaction imports, matching, and real-time alerts. The bank feed keeps your books current without manual entry.</p><h2 id="connecting-a-bank-account" tabindex="-1">Connecting a bank account <a class="header-anchor" href="#connecting-a-bank-account" aria-label="Permalink to &quot;Connecting a bank account&quot;">​</a></h2><ol><li>Go to <strong>Workspace</strong> → <strong>Financial</strong> → <strong>Bank Accounts</strong>.</li><li>Click <strong>Connect bank</strong> and choose your institution from the list.</li><li>Complete the authorisation flow in the pop-up window.</li><li>Once connected, MyCompanyDesk syncs historical transactions from the past 90 days and keeps the feed current with periodic automatic syncs.</li></ol><p>You can connect multiple accounts across different banks. Each account appears as a separate connection with its own sync cadence and transaction list.</p><h2 id="transaction-syncing" tabindex="-1">Transaction syncing <a class="header-anchor" href="#transaction-syncing" aria-label="Permalink to &quot;Transaction syncing&quot;">​</a></h2><p>MyCompanyDesk syncs transactions from your connected accounts on an automatic schedule. You can also trigger a manual sync from the bank-accounts page at any time. New transactions are matched against your existing customers, invoices, and expenses using vendor rules and AI matching. Matched transactions flow directly into your books; unmatched ones appear in the bank feed for manual review.</p><h3 id="expense-materialisation" tabindex="-1">Expense materialisation <a class="header-anchor" href="#expense-materialisation" aria-label="Permalink to &quot;Expense materialisation&quot;">​</a></h3><p>When a bank transaction is categorised (by a vendor rule, a default category, or the AI classifier), MyCompanyDesk automatically creates a draft expense in your books. The expense is filled with:</p><ul><li>The transaction&#39;s counterpart name as the supplier.</li><li>The absolute amount as the gross cost.</li><li>The category&#39;s default VAT rate (or the AI-suggested rate when available).</li><li>The category key matching your workspace&#39;s real expense categories.</li><li>The transaction&#39;s booking date as the expense date.</li></ul><p>The transaction is then marked as matched so it does not create a duplicate on the next sync. The new expense appears immediately in your expense list and flows into your reports and VAT summaries.</p><p>Materialisation runs in two passes: first, after vendor-rule and default-category matching (synchronous, during the sync); second, after the AI classifier completes (asynchronous). In both cases the created expense uses your workspace&#39;s actual expense categories and their default VAT treatments, so the numbers stay consistent with your manual entries.</p><p>Only outgoing transactions (amount below zero) are materialised. Incoming payments are not turned into expenses. If your workspace has no expense categories configured yet, the materialisation step is skipped and transactions wait for manual review.</p><h3 id="reviewing-auto-imported-expenses" tabindex="-1">Reviewing auto-imported expenses <a class="header-anchor" href="#reviewing-auto-imported-expenses" aria-label="Permalink to &quot;Reviewing auto-imported expenses&quot;">​</a></h3><p>When draft expenses are created from bank transactions, they first land in the review inbox. Go to <strong>Workspace</strong> → <strong>Financial</strong> → <strong>Bank Accounts</strong> and open the <strong>Review</strong> tab. You will see a list of draft expenses, each showing the supplier, amount, category suggestion, and the source of that suggestion (vendor rule, default category, AI suggestion, or bank feed).</p><p>For each expense you can:</p><ul><li><strong>Confirm</strong>: Book the expense as-is. It moves to your active expense list and flows into reports and VAT summaries.</li><li><strong>Edit</strong>: Open the expense form to tweak the category, VAT treatment, description, or amount before booking.</li><li><strong>Reject</strong>: Discard the draft. The transaction returns to the unmatched queue in the bank feed for manual handling.</li></ul><p>The review step is designed so you stay in control: no expense lands in your books without someone checking it first. Confirmed expenses show a brief toast confirming the supplier name; rejected ones show the rejection. If an action fails, a retry message appears and you can try again.</p><p>Each expense card shows a source tag so you know why MyCompanyDesk made that particular suggestion. The possible sources are <code>vendor_rule</code>, <code>workspace_default</code>, <code>ai_flag</code>, and <code>feed</code>, the same categorisation pipeline described above.</p><h3 id="sync-failures" tabindex="-1">Sync failures <a class="header-anchor" href="#sync-failures" aria-label="Permalink to &quot;Sync failures&quot;">​</a></h3><p>If a sync fails (for example, the bank&#39;s authorisation expired or the connection dropped), MyCompanyDesk sends you an email and an in-app notification with the connection name and a link to fix it. You can also opt into push notifications for sync errors under notification preferences.</p><h2 id="notifications" tabindex="-1">Notifications <a class="header-anchor" href="#notifications" aria-label="Permalink to &quot;Notifications&quot;">​</a></h2><p>The bank feed supports four notification preferences, controllable per workspace from the bank-accounts settings page:</p><table tabindex="0"><thead><tr><th>Preference</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Sync-error alerts</strong></td><td>Email and in-app notification when a bank connection fails to sync.</td></tr><tr><td><strong>Large-transaction alerts</strong></td><td>Push and in-app alert when a transaction exceeds a threshold you set (for example, €5,000).</td></tr><tr><td><strong>Weekly digest</strong></td><td>A Monday-morning email summarising the week&#39;s imported transactions, unmatched items, and top auto-categorised vendors.</td></tr><tr><td><strong>Receipt requests</strong></td><td>An automated daily sweep that emails your suppliers asking for missing receipts on outgoing unmatched transactions.</td></tr></tbody></table><p>All notifications are best-effort: they never block or interrupt a sync. You can turn each one on or off independently.</p><h3 id="large-transaction-threshold" tabindex="-1">Large-transaction threshold <a class="header-anchor" href="#large-transaction-threshold" aria-label="Permalink to &quot;Large-transaction threshold&quot;">​</a></h3><p>Set a euro amount in the bank-accounts settings. Any incoming or outgoing transaction whose absolute value meets or exceeds that amount triggers a push notification and an in-app alert. This is useful for spotting large payments or unexpected charges quickly.</p><h3 id="weekly-digest" tabindex="-1">Weekly digest <a class="header-anchor" href="#weekly-digest" aria-label="Permalink to &quot;Weekly digest&quot;">​</a></h3><p>Every Monday at 08:00 (workspace-local time), MyCompanyDesk emails you a short summary of your bank-feed activity from the past seven days:</p><ul><li>Number of new transactions imported.</li><li>Number of unmatched outgoing transactions still waiting for a receipt.</li><li>Top three auto-categorised vendors.</li></ul><p>If there was no activity that week the digest is skipped. The digest honours your workspace notification setting and only sends when <code>notify_weekly_summary</code> is on.</p><h3 id="supplier-receipt-requests" tabindex="-1">Supplier receipt requests <a class="header-anchor" href="#supplier-receipt-requests" aria-label="Permalink to &quot;Supplier receipt requests&quot;">​</a></h3><p>When enabled, MyCompanyDesk runs a daily sweep at 06:00 local time. For each workspace with <code>ask_receipt_email</code> turned on, it finds outgoing unmatched transactions from the last 30 days whose counterpart matches a customer with an email address on file. It sends each supplier one consolidated email listing all the transactions that need a receipt, then tags those transactions so the same supplier isn&#39;t asked again.</p><h2 id="managing-vendor-rules" tabindex="-1">Managing vendor rules <a class="header-anchor" href="#managing-vendor-rules" aria-label="Permalink to &quot;Managing vendor rules&quot;">​</a></h2><p>Vendor rules let you teach MyCompanyDesk how to categorise recurring transactions:</p><ul><li>Create a rule from any transaction by clicking <strong>Create rule</strong> in the bank feed.</li><li>Rules match on counterpart name, description, or amount patterns.</li><li>Matched transactions are linked to the correct customer, invoice, or expense automatically.</li><li>The weekly digest highlights your top-performing rules.</li></ul><p>Rules you create are workspace-scoped and apply to all connected bank accounts.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><p><strong>My bank connection shows an error.</strong> Open the bank-accounts settings page. If your bank requires re-authorisation you&#39;ll see a prompt to reconnect. Sync-error notifications also link you there.</p><p><strong>A transaction didn&#39;t match.</strong> Review the transaction in the bank feed under the unmatched filter. You can match it manually, create a new customer or expense from it, or create a vendor rule so it&#39;s matched automatically next time.</p><p><strong>I&#39;m not getting notifications.</strong> Check the notification toggles in the bank-accounts settings page. For push notifications, make sure push is enabled in your account profile. For the weekly digest, confirm <code>notify_weekly_summary</code> is on and that there was actual bank activity that week.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("features/bank.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const bank = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  bank as default
};
