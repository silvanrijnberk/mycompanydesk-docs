import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Keyboard Shortcuts","description":"","frontmatter":{"title":"Keyboard Shortcuts"},"headers":[],"relativePath":"advanced/keyboard-shortcuts.md","filePath":"advanced/keyboard-shortcuts.md"}');
const _sfc_main = { name: "advanced/keyboard-shortcuts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="keyboard-shortcuts" tabindex="-1">Keyboard Shortcuts <a class="header-anchor" href="#keyboard-shortcuts" aria-label="Permalink to &quot;Keyboard Shortcuts&quot;">​</a></h1><p>MyCompanyDesk keeps its set of keyboard shortcuts deliberately small. The shortcuts below are the ones that work today.</p><h2 id="search-palette" tabindex="-1">Search palette <a class="header-anchor" href="#search-palette" aria-label="Permalink to &quot;Search palette&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th></tr></thead><tbody><tr><td><code>Cmd+K</code> (Mac) / <code>Ctrl+K</code> (Windows/Linux)</td><td>Open or close the search palette</td></tr></tbody></table><p>The search palette is the fastest way to move around the app:</p><ul><li>Type at least two characters to search your customers, invoices, and expenses (plus projects, contracts, and properties when those features are enabled).</li><li>Settings are searchable too: type something like &quot;VAT&quot; or &quot;reminders&quot; to jump straight to the matching settings card.</li><li>Quick actions let you jump to any section or start a new invoice, quote, expense, or customer.</li><li>Filter chips under the search box narrow results to a single type.</li></ul><p>Inside the palette:</p><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th></tr></thead><tbody><tr><td><code>Up</code> / <code>Down</code></td><td>Move through the results</td></tr><tr><td><code>Enter</code></td><td>Open the highlighted result</td></tr><tr><td><code>Esc</code></td><td>Close the palette</td></tr></tbody></table><h2 id="escape" tabindex="-1">Escape <a class="header-anchor" href="#escape" aria-label="Permalink to &quot;Escape&quot;">​</a></h2><p><code>Esc</code> steps back one level at a time:</p><ul><li>While typing in a field, it removes focus from that field.</li><li>With a dialog open, it closes the dialog.</li><li>On a detail page, it takes you back to the list (the same as the back button).</li></ul><h2 id="site-builder" tabindex="-1">Site builder <a class="header-anchor" href="#site-builder" aria-label="Permalink to &quot;Site builder&quot;">​</a></h2><p>While editing your website in the site builder:</p><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th></tr></thead><tbody><tr><td><code>Cmd+Z</code> / <code>Ctrl+Z</code></td><td>Undo</td></tr><tr><td><code>Cmd+Shift+Z</code> / <code>Ctrl+Shift+Z</code> or <code>Ctrl+Y</code></td><td>Redo</td></tr></tbody></table><p>While a text field is focused, undo and redo apply to the text you are typing. Click outside the field first to undo a whole editing step.</p><h2 id="forms" tabindex="-1">Forms <a class="header-anchor" href="#forms" aria-label="Permalink to &quot;Forms&quot;">​</a></h2><p>Standard browser keys work everywhere: <code>Tab</code> and <code>Shift+Tab</code> move between fields, and <code>Enter</code> submits most forms.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("advanced/keyboard-shortcuts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const keyboardShortcuts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  keyboardShortcuts as default
};
