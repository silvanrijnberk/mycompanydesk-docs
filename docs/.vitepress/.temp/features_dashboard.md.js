import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Dashboard","description":"","frontmatter":{"title":"Dashboard","last_verified":"2026-05-20T00:00:00.000Z"},"headers":[],"relativePath":"features/dashboard.md","filePath":"features/dashboard.md"}');
const _sfc_main = { name: "features/dashboard.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="dashboard" tabindex="-1">Dashboard <a class="header-anchor" href="#dashboard" aria-label="Permalink to &quot;Dashboard&quot;">​</a></h1><p>The dashboard at <code>/dashboard</code> is your home base. It presents a fixed briefing layout that surfaces what needs your attention, key financial indicators, and recent activity in a single scrollable view.</p><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><p>The dashboard is a single page (<code>BriefingDashboard</code>) composed of editorial blocks. There is no per-user customization. Every user in the workspace sees the same structure, fed from the same live data.</p><h3 id="welcome-screen" tabindex="-1">Welcome screen <a class="header-anchor" href="#welcome-screen" aria-label="Permalink to &quot;Welcome screen&quot;">​</a></h3><p>On a brand-new account with no invoices or customers, the dashboard shows a <strong>welcome screen</strong> (<code>BriefingWelcome</code>) instead of the full briefing. It guides the user to three core first actions: create an invoice, add a customer, or log an expense. A link to the <a href="/getting-started/introduction">Getting Started</a> docs sits below the action cards. Once at least one invoice or customer exists, the welcome screen disappears permanently and the full dashboard takes its place.</p><h3 id="skeleton-and-first-load" tabindex="-1">Skeleton and first load <a class="header-anchor" href="#skeleton-and-first-load" aria-label="Permalink to &quot;Skeleton and first load&quot;">​</a></h3><p>While data loads, the dashboard shows a <strong>skeleton</strong> placeholder (<code>BriefingSkeleton</code>): a layout-matched shimmer that mirrors the final shape of each card. Once the core data is settled (metrics cached and liquidity resolved), the skeleton dissolves into a coordinated fade-up entrance. Every top-level block rises with a staggered delay so the whole dashboard appears in one smooth motion. A safety net of 2.5 seconds ensures the skeleton never traps the user on slow connections. The <code>prefers-reduced-motion: reduce</code> media query disables all entry animations.</p><p>The shell loads:</p><ul><li>A <strong>hero</strong> panel with greeting, AI-lede, and period context</li><li>A <strong>pulse</strong> row of four KPIs: liquidity/runway, revenue (month + YTD), receivables + DSO, and VAT balance + deadline</li><li>A <strong>briefing feed</strong> split into three tabs: <strong>Now</strong> (requires attention), <strong>This Week</strong> (upcoming), and <strong>Good News</strong> (confirmations)</li><li>A <strong>cash chart</strong> spanning a 12-month window with actual + forecast</li><li>A <strong>week card</strong>, <strong>project margins</strong> list, <strong>top clients</strong> list, <strong>activity feed</strong>, and a <strong>VAT ring</strong></li><li>A <strong>setup banner</strong> that persists until the wizard at <code>/setup</code> is completed</li></ul><h2 id="hero" tabindex="-1">Hero <a class="header-anchor" href="#hero" aria-label="Permalink to &quot;Hero&quot;">​</a></h2><p>The top-of-page greets with a time-of-day greeting. The AI-lede is the hero&#39;s centerpiece: a short, personal, AI-written briefing that synthesizes the full picture of the business.</p><p>The AI briefing speaks in the first person (&quot;ik&quot;) and addresses the user informally (&quot;je&quot;). It opens with the single most urgent action for today, then at most one or two supporting points where they add value. It closes with a suggested in-app next step (e.g. &quot;stuur Atelier Norden vandaag een herinnering&quot;, &quot;rond je BTW af&quot;). The model draws on a full digest of live signals: liquidity and runway, revenue and profit (MTD + YTD), overdue receivables (count, total, worst customer), bills (due soon + overdue), draft counts, project margins, VAT position (balance, deadline, checklist progress, reserve), unbilled hours, recent payments, and new customers. All amounts are rounded to whole euros.</p><p>When the AI briefing is still loading, the hero shows the previous day&#39;s cached deterministic lede. The cross-fade to the AI version is a smooth opacity-and-slide transition (<code>Transition</code> with <code>mode=&quot;out-in&quot;</code>). The AI briefing appears with a sparkle icon and the primary text color.</p><p>The AI briefing is available in all four supported languages. It is generated once per calendar day per company on Vertex AI <code>europe-west1</code> (Gemini 2.5 Flash) and cached for the rest of the day. When the model is unavailable or the workspace is not entitled (Pro only), the deterministic lede is shown alone and no cross-fade occurs.</p><p>The hero also carries the period label (month is default).</p><h2 id="pulse-row" tabindex="-1">Pulse row <a class="header-anchor" href="#pulse-row" aria-label="Permalink to &quot;Pulse row&quot;">​</a></h2><p>The four-cell pulse row replaces the old money tiles and KPI headline. Each cell presents a focused metric with context:</p><table tabindex="0"><thead><tr><th>Cell</th><th>What it shows</th></tr></thead><tbody><tr><td><strong>Liquidity</strong></td><td>Current balance (actual when a bank connection is active, estimated otherwise), plus runway in weeks</td></tr><tr><td><strong>Revenue</strong></td><td>Current month revenue with percentage vs previous month, plus year-to-date</td></tr><tr><td><strong>Receivables</strong></td><td>Total outstanding, overdue count, and days sales outstanding</td></tr><tr><td><strong>VAT</strong></td><td>Balance (refund or pay), deadline date, and tax reserve</td></tr></tbody></table><h2 id="briefing-feed" tabindex="-1">Briefing feed <a class="header-anchor" href="#briefing-feed" aria-label="Permalink to &quot;Briefing feed&quot;">​</a></h2><p>The feed is the main attention surface. It assembles real signals from workspace data into a structured list with three tab views:</p><ul><li><strong>Now.</strong> Items that require action today: overdue invoices, overdue bills, open receipts, draft invoices waiting for send, and VAT checklist items.</li><li><strong>This week.</strong> Items due in the coming week: invoices due, bills to pay, and VAT deadlines.</li><li><strong>Good news.</strong> Confirmations: recently paid invoices, revenue growth, and new customers this month.</li></ul><p>Each feed item shows the entity, amount, and contextual detail. The feed opens from a tab bar anchored to the top of the list.</p><h2 id="right-rail-and-bottom-row" tabindex="-1">Right rail and bottom row <a class="header-anchor" href="#right-rail-and-bottom-row" aria-label="Permalink to &quot;Right rail and bottom row&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Card</th><th>Content</th></tr></thead><tbody><tr><td><strong>Cash (12-month)</strong></td><td>A line chart with actual cash position through today and a forecast forward to month-end. Requires enough history to generate the projection.</td></tr><tr><td><strong>Week card</strong></td><td>Hours logged this week.</td></tr><tr><td><strong>Project margins</strong></td><td>All projects with revenue, sorted by margin percentage, colour-coded green/amber/red.</td></tr><tr><td><strong>Top clients</strong></td><td>Top clients by revenue this year, with percentage of total.</td></tr><tr><td><strong>Recent activity</strong></td><td>Invoice created, invoice paid, and expense added events.</td></tr><tr><td><strong>VAT ring</strong></td><td>Current VAT return period, checklist completion, and open receipt count. Links to the VAT return.</td></tr></tbody></table><h2 id="setup-banner" tabindex="-1">Setup banner <a class="header-anchor" href="#setup-banner" aria-label="Permalink to &quot;Setup banner&quot;">​</a></h2><p>While the setup wizard at <code>/setup</code> still has fields to fill, a <code>FinishSetupBanner</code> pins itself above the dashboard with the count of pending fields and a <strong>Resume setup</strong> button. It includes a dismiss button (X icon) that hides the banner per browser via localStorage, surviving reloads until the wizard is completed. The wizard is non-blocking: new signups land on <code>/dashboard</code> directly and are not force-redirected.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("features/dashboard.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dashboard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dashboard as default
};
