import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Domains, Website, and Inbox","description":"","frontmatter":{"title":"Domains, Website, and Inbox","last_verified":"2026-05-09T00:00:00.000Z"},"headers":[],"relativePath":"features/domains-website-inbox.md","filePath":"features/domains-website-inbox.md"}');
const _sfc_main = { name: "features/domains-website-inbox.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="domains-website-and-inbox" tabindex="-1">Domains, Website, and Inbox <a class="header-anchor" href="#domains-website-and-inbox" aria-label="Permalink to &quot;Domains, Website, and Inbox&quot;">​</a></h1><blockquote><p><strong>Status: pre-launch.</strong> All three features in this page roll out together as a single bundle. They are gated by the <code>custom_domains</code> and <code>public_business_page</code> feature flags and are still being onboarded onto the public plans. Behaviour described here matches the codebase as of 2026-05-09; if a screen looks different in your workspace, the bundle has not been enabled there yet.</p></blockquote><p>Custom domains, the hosted business website, and the shared email inbox ship as one product. The reason: they share state. The same <code>domains</code> row that proves you control <code>acme.nl</code> also makes <code>acme.nl</code> your website&#39;s URL and lets <code>info@acme.nl</code> start receiving mail. There is one onboarding flow, one settings tree, and one place in the app to manage all of it.</p><h2 id="the-bundled-value" tabindex="-1">The bundled value <a class="header-anchor" href="#the-bundled-value" aria-label="Permalink to &quot;The bundled value&quot;">​</a></h2><p>Add a domain once and you get three things:</p><ul><li><strong>A custom address.</strong> Your business lives at <code>acme.nl</code> instead of <code>acme.mycompanydesk.com</code>.</li><li><strong>A live website.</strong> The hosted business page is automatically published on the verified domain.</li><li><strong>A working inbox.</strong> <code>info@acme.nl</code>, plus <code>support@</code>, <code>sales@</code> and a send-only <code>noreply@</code> alias, start catching mail and sending replies.</li></ul><p>You can run the bundled flow from the <a href="/getting-started/company-setup">Setup wizard</a> (<code>/setup</code>, the &quot;Your web address&quot; step), or piece-by-piece from <code>Company › Your own .com address</code> and the Inbox.</p><h2 id="one-onboarding-flow" tabindex="-1">One onboarding flow <a class="header-anchor" href="#one-onboarding-flow" aria-label="Permalink to &quot;One onboarding flow&quot;">​</a></h2><p>The wizard step at <code>/setup</code> is the recommended entry point. It applies through <code>apply.service.js → activateSubdomain | addDomain → quickEnableInbox</code> in a single submit, so the user answers a few questions and the platform wires everything underneath.</p><h3 id="step-1-—-add-a-domain" tabindex="-1">Step 1 — Add a domain <a class="header-anchor" href="#step-1-—-add-a-domain" aria-label="Permalink to &quot;Step 1 — Add a domain&quot;">​</a></h3><p>Two paths in the wizard, both stored on the <code>domains</code> table:</p><ul><li><strong>Free workspace subdomain</strong> — <code>your-slug.mycompanydesk.com</code> (or <code>.nl</code> for NL workspaces). No DNS work; the slug is registered as a Cloudflare Pages custom domain and the website is live within seconds. This is the default for new workspaces.</li><li><strong>Your own domain</strong> — paste <code>acme.nl</code>. Two setup modes are supported: <ul><li><strong>Nameserver mode</strong> (recommended) — a Cloudflare zone is created for the domain. You change your registrar&#39;s nameservers to the two <code>*.ns.cloudflare.com</code> hostnames the wizard shows. Cloudflare becomes authoritative DNS for the domain, which is what unlocks email, SSL and DNS-record management inside MyCompanyDesk.</li><li><strong>CNAME mode</strong> — for subdomains only (e.g. <code>portal.acme.nl</code>). You add a single CNAME record pointing at <code>mycompanydesk-app.pages.dev</code>. No nameserver change. Email routing is not available in this mode.</li></ul></li></ul><p>Adding a custom domain automatically deactivates the workspace subdomain — there is one canonical website per company, never two.</p><h3 id="step-2-—-verify" tabindex="-1">Step 2 — Verify <a class="header-anchor" href="#step-2-—-verify" aria-label="Permalink to &quot;Step 2 — Verify&quot;">​</a></h3><p>Verification runs both on demand and on a poll. The detail page exposes a <strong>Verify</strong> button (<code>POST /api/domains/:id/verify</code>), and a background job re-checks every pending domain at intervals.</p><ul><li><strong>Nameserver mode</strong> is verified once Cloudflare reports the zone as <code>active</code>. The status moves <code>pending_nameservers → pending_verification → active</code>. The user is notified via the in-app notification bell when the flip happens.</li><li><strong>CNAME mode</strong> is verified by resolving the CNAME and confirming it points at the Pages target. Status moves <code>pending_cname → active</code>.</li></ul><h3 id="step-3-—-ssl" tabindex="-1">Step 3 — SSL <a class="header-anchor" href="#step-3-—-ssl" aria-label="Permalink to &quot;Step 3 — SSL&quot;">​</a></h3><p>SSL is provisioned by Cloudflare automatically once the zone is active. The default mode is <strong>Full (strict)</strong>; you can change it from <code>Domain detail › SSL</code> (<code>off / flexible / full / strict</code>). The certificate-status field on the SSL panel mirrors Cloudflare&#39;s verification result.</p><h3 id="step-4-—-website-goes-live" tabindex="-1">Step 4 — Website goes live <a class="header-anchor" href="#step-4-—-website-goes-live" aria-label="Permalink to &quot;Step 4 — Website goes live&quot;">​</a></h3><p>The hosted business page (see <a href="/advanced/business-page">Site Builder</a>) is automatically published at the domain root once the zone is active. The wizard&#39;s <code>getBusinessPageUrl</code> resolver returns, in priority:</p><ol><li>A custom domain with <code>business_page_enabled = true</code> → <code>https://acme.nl</code></li><li>A custom domain with <code>portal_subdomain_enabled = true</code> → <code>https://portal.acme.nl</code></li><li>The workspace subdomain → <code>https://acme.mycompanydesk.com</code></li><li>The fallback portal route (<code>/portal/&lt;slug&gt;</code>) when nothing else is configured.</li></ol><h3 id="step-5-—-inbox-catches-mail" tabindex="-1">Step 5 — Inbox catches mail <a class="header-anchor" href="#step-5-—-inbox-catches-mail" aria-label="Permalink to &quot;Step 5 — Inbox catches mail&quot;">​</a></h3><p>For nameserver-mode custom domains, the wizard runs <code>quickEnableInbox</code> after verification. That call is idempotent and does the following:</p><ul><li>Provisions the CF Email Sending identity on the bare apex domain (<code>acme.nl</code> by default) and writes the DKIM and SPF DNS records. Passing an explicit subdomain label provisions <code>&lt;label&gt;.&lt;domain&gt;</code> instead (e.g. <code>mail.acme.nl</code>).</li><li>Sets a Cloudflare Email Routing catch-all rule on the zone, pointed at the <code>inbox-inbound</code> Worker.</li><li>Inspects the apex MX records. If they are empty or already point at Cloudflare, the wizard installs the Cloudflare MX. If a third-party provider (Google Workspace, Microsoft 365) is already there, the wizard refuses to overwrite and surfaces a <code>conflict</code> warning so you can decide.</li><li>Creates <code>info@acme.nl</code> as the default shared mailbox.</li><li>Provisions <code>support@</code> and <code>sales@</code> as bidirectional aliases of <code>info@</code>, and <code>noreply@</code> as a send-only alias (allowed in From, dropped on inbound).</li><li>Optionally creates a personal mailbox (<code>silvan@acme.nl</code>) when you ticked the box in the wizard.</li></ul><h2 id="per-feature-reference" tabindex="-1">Per-feature reference <a class="header-anchor" href="#per-feature-reference" aria-label="Permalink to &quot;Per-feature reference&quot;">​</a></h2><h3 id="custom-domains" tabindex="-1">Custom domains <a class="header-anchor" href="#custom-domains" aria-label="Permalink to &quot;Custom domains&quot;">​</a></h3><p>UI lives at <code>Website › Domein &amp; SEO</code> -- the page is <code>/website?tab=domein</code>, rendering the <code>SettingsDomains</code> component. The older paths <code>/workspace/organization/company/address</code>, <code>/workspace/organization/domains</code>, and <code>/workspace/communication/domains</code> all redirect here.</p><p>The page splits into two sections:</p><ul><li><strong>Pending domains</strong>: Domains still being verified always appear at the top, regardless of the topbar domain switcher state. This lets you reach verification instructions for newly added domains before they become active.</li><li><strong>Active domain panel</strong>: DNS, SSL, redirects, analytics, security, and SEO tabs are scoped to the domain selected in the topbar domain switcher (accessible from the site builder at <code>/website</code>). Selecting the default site (shown as your workspace name in the switcher) hides the per-domain panel entirely. Switching domains resets the active tab to Routing.</li></ul><p>The page defaults to a clean view with the most commonly needed tabs. Six power-user tabs are hidden unless you turn on <strong>Advanced Mode</strong>. Those tabs are: DNS, SSL, Redirects, Analytics, Quick Settings, and Security. Advanced mode is a per-device switch on the <strong>Weergave</strong> (display) page in settings; see the <a href="/settings/">Settings overview</a>.</p><p>What the page lets you do:</p><ul><li><strong>Buy or claim a domain</strong> via the domain purchase card. Enter a domain name, check availability against OpenProvider, and either buy it or claim it free if you qualify as a Founding Member.</li><li><strong>Add a domain</strong> (nameserver or CNAME mode) via a dedicated card that is always visible.</li><li><strong>Verify</strong> a pending domain.</li><li><strong>Manage DNS records</strong> for the selected active domain -- A, AAAA, CNAME, MX, TXT, SRV, CAA, NS. CRUD goes through Cloudflare via the API.</li><li><strong>SSL</strong> for the selected domain -- view certificate status, change SSL mode.</li><li><strong>URL redirects</strong> for the selected domain -- three free Cloudflare Page Rules per zone. Source pattern + destination + 301/302.</li><li><strong>Email security</strong> for the selected domain -- SPF/DMARC/DKIM check with a one-click &quot;fix&quot; that writes safe defaults (<code>v=spf1 ~all</code>, <code>v=DMARC1; p=quarantine; …</code>).</li><li><strong>Quick settings</strong> for the selected domain -- toggle Cloudflare Development Mode, toggle &quot;Under attack&quot; security level, purge cache.</li><li><strong>Analytics</strong> for the selected domain -- last 30 days of requests, bandwidth, threats, visitors, pageviews. The current Cloudflare Analytics endpoint is sunset; the page renders an empty <code>unavailable</code> state until the GraphQL migration lands.</li><li><strong>Remove</strong> the selected domain -- soft-deletes the row (<code>status = &#39;removed&#39;</code>) and tears down the Cloudflare zone (or the Pages domain in CNAME mode).</li></ul><h4 id="domains-table-—-the-shared-state" tabindex="-1"><code>domains</code> table — the shared state <a class="header-anchor" href="#domains-table-—-the-shared-state" aria-label="Permalink to &quot;\`domains\` table — the shared state&quot;">​</a></h4><p>Notable columns the app reads from:</p><table tabindex="0"><thead><tr><th>Column</th><th>Purpose</th></tr></thead><tbody><tr><td><code>domain_name</code></td><td>The hostname, e.g. <code>acme.nl</code>.</td></tr><tr><td><code>setup_mode</code></td><td><code>nameserver</code> (full delegation) or <code>cname</code> (single subdomain).</td></tr><tr><td><code>status</code></td><td><code>pending_nameservers</code>, <code>pending_verification</code>, <code>pending_cname</code>, <code>active</code>, <code>failed</code>, <code>removed</code>.</td></tr><tr><td><code>cloudflare_zone_id</code></td><td>Set in nameserver mode. Drives DNS, SSL, redirects, analytics, email-routing.</td></tr><tr><td><code>nameserver_1</code>, <code>nameserver_2</code></td><td>Shown to the user during nameserver setup.</td></tr><tr><td><code>cname_hostname</code>, <code>cname_target</code></td><td>Set in CNAME mode.</td></tr><tr><td><code>email_routing_enabled</code></td><td><code>true</code> once the Cloudflare Email Routing zone is enabled.</td></tr><tr><td><code>inbox_enabled</code>, <code>inbox_subdomain_tag</code>, <code>inbox_dkim_ready</code></td><td>Flipped by <code>quickEnableInbox</code>. The CF Email Sending identity (apex domain by default; <code>mail.acme.nl</code> when a subdomain label is given) and DKIM provisioning state.</td></tr><tr><td><code>business_page_enabled</code>, <code>portal_subdomain_enabled</code></td><td>Determine which hostname serves the public website.</td></tr><tr><td><code>verified_at</code></td><td>Set when verification succeeds.</td></tr><tr><td><code>registrar</code></td><td>The registrar service, currently <code>openprovider</code> for domains purchased through the buy-a-domain flow.</td></tr><tr><td><code>registrar_domain_id</code></td><td>The registrar-side identifier for purchased domains.</td></tr><tr><td><code>purchase_price_period</code></td><td>Billing period for purchased domains (<code>yearly</code>).</td></tr><tr><td><code>purchase_intent_id</code></td><td>Links to the <code>domain_purchase_intents</code> row for paid purchases.</td></tr><tr><td><code>founder_claim_id</code></td><td>Links to the <code>founder_domain_claims</code> row for Founder free claims.</td></tr><tr><td><code>transferred_out_at</code></td><td>Set when a domain is detected as transferred away from the MCD registrar account during the weekly sync.</td></tr></tbody></table><h4 id="renewal-lifecycle" tabindex="-1">Renewal lifecycle <a class="header-anchor" href="#renewal-lifecycle" aria-label="Permalink to &quot;Renewal lifecycle&quot;">​</a></h4><p>Domain renewal follows three paths depending on how the domain was acquired:</p><ol><li><strong>Free bundled renewal</strong> (Founder-tier or Pro-converted trial-tier): MCD absorbs the wholesale renewal cost. The domain auto-renews as long as the workspace stays on Pro. No payment method needed.</li><li><strong>Paid auto-renewal</strong> (paid purchase, or trial-tier without Pro): Charged annually via the saved card. Works like any subscription renewal.</li><li><strong>Manual renewal</strong>: If a trial-tier workspace falls off Pro AND has no saved card, the auto-renewal path skips it. The user sees a notification and can trigger a one-off payment via <code>POST /api/domains/renew/:domainId</code>, which creates a Stripe Embedded Checkout session for the renewal. This is the only way to keep a domain alive without an active subscription or saved card.</li></ol><h4 id="trial-exit-domain-buy-out" tabindex="-1">Trial-exit domain buy-out <a class="header-anchor" href="#trial-exit-domain-buy-out" aria-label="Permalink to &quot;Trial-exit domain buy-out&quot;">​</a></h4><p>When a customer on a Pro trial decides to leave before converting to paid Pro, they have a third option for their free <code>.nl</code> domain: buy it out for a flat €15 (all-in, one-time). The buy-out flow (<code>DomainBuyoutModal.vue</code>) lets the customer pay via Stripe Embedded Checkout and receive full ownership. Once paid, the domain holder is transferred from MCD to the customer and the EPP (transfer) code is shown so the domain can be moved to any registrar.</p><p>The buy-out price is a product price, not a transfer surcharge. MCD never charges for the transfer token itself once the customer is the registered holder. The distinction is documented in the internal legal memo <code>docs/legal/gratis-domein-voorwaarden.md</code> in the RichardTool repo.</p><p>Database tables involved:</p><ul><li><code>domain_buyout_intents</code> — tracks buy-out payment intents with Stripe PaymentIntent IDs and status.</li></ul><h4 id="transfer-consequences" tabindex="-1">Transfer consequences <a class="header-anchor" href="#transfer-consequences" aria-label="Permalink to &quot;Transfer consequences&quot;">​</a></h4><p>Transferring a domain registered through MyCompanyDesk to another registrar has permanent consequences, enforced by the weekly OpenProvider status sync:</p><ul><li><strong>Founder-tier domains</strong>: The Founder claim is deleted, and the workspace&#39;s internal lifetime-Pro subscription is cancelled. The workspace becomes a regular paid customer. This is irreversible. The Founder status cannot be reclaimed.</li><li><strong>Trial-tier / Pro-bundled domains</strong>: The bundled-free status is lost. The workspace can never claim another free domain (already enforced via the retained-claims list). Note that buying out the domain during the trial (see buy-out section above) is not a transfer — it is a holder handover that gives the customer ownership before any transfer happens, so the free-domain perk is preserved for the duration of the trial.</li><li><strong>Paid domains</strong>: No perk revocation. The domain simply moves to <code>status = &#39;transferred_out&#39;</code>.</li></ul><p>The claim modal warns about these consequences before a free-domain claim is submitted, and requires explicit acknowledgement from the user. A &quot;Held during trial&quot; notice explains that the domain is registered under MCD during the trial and will be transferred to the customer for free on Pro conversion, or available for buy-out at €15 on early exit. Revocation details are recorded in the <code>domain_perk_revocations</code> audit table for support reference.</p><h4 id="buy-or-claim-a-domain" tabindex="-1">Buy or claim a domain <a class="header-anchor" href="#buy-or-claim-a-domain" aria-label="Permalink to &quot;Buy or claim a domain&quot;">​</a></h4><p>The domain purchase card (<code>DomainPurchaseCard.vue</code>, <code>domain-purchase.service.ts</code>) is the first card on the Domains settings page. It appears when the workspace has no active custom domain yet. The card lets the user pick and acquire a domain through two paths, both opening a dedicated two-step purchase modal (<code>DomainClaimModal.vue</code>). Step 1 collects registrant details (the data required by the registrar for WHOIS). Step 2 handles payment or claim submission:</p><ul><li><strong>Buy</strong> -- Paid purchase via OpenProvider. The user enters a domain name, the card calls <code>GET /api/domain-purchase/quote</code> to check availability and pricing, and then opens the purchase modal. After collecting the registrant details, the modal calls <code>POST /api/domain-purchase/checkout-session</code> to create a Stripe payment session and mounts Stripe Embedded Checkout for the payment. Once complete, <code>POST /api/domain-purchase/finalize</code> registers the domain with OpenProvider and creates the <code>domains</code> row in nameserver mode, wired to Cloudflare.</li><li><strong>Free claim</strong> -- Eligible workspaces on a Pro trial (including new Founding Members from 2026-05-20 onward) can claim one <code>.nl</code> domain free of charge for the first year. The card calls <code>GET /api/domain-purchase/founder/eligibility</code> to check the workspace&#39;s claim tier (<code>trial</code> for trial members, <code>founder</code> for grandfathered original-cohort Founding Members) and gate status. The modal collects the registrant details, and on submit calls <code>POST /api/domain-purchase/founder/claim</code>. The platform pays the first-year registration fee.</li></ul><p>Founder claims now have two tiers for renewal, governed by the Founding Member grant type:</p><ul><li><strong>Founder tier</strong> (grandfathered only) -- Workspaces with the original grant type <code>free_for_life</code> (claimed before 2026-05-20) receive lifetime-free domain renewal. No payment method is required. Renewal is handled automatically by the platform, with MCD absorbing the wholesale cost. New Founding Members from 2026-05-20 onward do NOT get this tier; they claim under the trial tier like any other trial workspace.</li><li><strong>Trial tier</strong> -- Workspaces on a trial (including new Founding Members with grant type <code>trial_plus_discount</code>). The first year is free. At the end of the free year the workspace must be on a paid Pro plan; the domain then renews as part of the Pro subscription, paid by the workspace. If the workspace stops paying Pro after the free year, the domain lapses and must be renewed manually. During the trial year the user can optionally save a card via Stripe SetupIntent in the modal for future automatic renewal.</li><li><strong>Paid tier</strong> -- Standard domains purchased at full price. Renewal is charged via the saved payment method on the annual cycle. If the charge fails, a manual-renewal notification is sent.</li></ul><p>The eligibility endpoint (<code>GET /api/domain-purchase/founder/eligibility</code>) returns a <code>tier</code> field (<code>founder</code> | <code>trial</code> | <code>paid</code> | <code>free</code>) and <code>founderSlotsRemaining</code> alongside the existing gates. The 50-slot cap applies only to Founder-tier claims (grandfathered cohort); trial-tier claims do not count against the Founder cap.</p><p>Founder eligibility is determined by a set of hard gates checked server-side in <code>founder-domain-claim.service.js</code>:</p><ul><li><strong>Founding Member status</strong> -- the workspace must have the Founding Member flag (grant type determines tier: <code>free_for_life</code> maps to <code>founder</code>, <code>trial_plus_discount</code> maps to <code>trial</code>).</li><li><strong>Free-domain slots</strong> capped at 50 across all grandfathered Founding Members (<code>free_for_life</code>) for Founder-tier claims. Trial-tier claims are not counted against this cap.</li><li><strong>KVK required</strong> -- the workspace must have a linked KVK number.</li><li><strong>Domain must be <code>.nl</code></strong> -- the free program only covers the NL TLD.</li><li><strong>Domain must match the KVK name</strong> -- the domain must correspond to the registered legal name or a trade name.</li><li><strong>KVK must not be on the retained-claims list</strong> -- one free domain per KVK number. A KVK that has already claimed (and then transferred away) a free domain is blocked permanently.</li></ul><p>Account age and site-content quality are not hard gates. They would block legitimate onboarding-day claims, which contradicts the &quot;set up your business in a day, domain included&quot; pitch. Instead, both flow into the Gemini abuse score as soft signals: a brand-new account with a template site scores low and lands in manual review; a real business with real content auto-approves regardless of age. The eligibility response carries a <code>softSignals</code> block (<code>ageDays</code>, <code>sitePublished</code>, <code>paragraphCount</code>) so the UI can surface a hint without blocking the claim.</p><p>When a gate fails, the card lists the remaining requirements so the user can see what is left to unlock before the free claim becomes available.</p><p>The supported TLDs for purchase are <code>.nl</code>, <code>.eu</code>, <code>.com</code>, <code>.net</code>, and <code>.org</code>. Other TLDs show an unsupported message with a suggestion to buy the domain elsewhere and add it via the existing BYO flow.</p><p>New database tables introduced by this feature:</p><ul><li><code>domain_purchase_intents</code> -- tracks paid purchase intents with Stripe PaymentIntent IDs, registrant details, and purchase status.</li><li><code>founder_domain_claims</code> -- tracks Founder free claims with eligibility snapshots, abuse scoring, and claim status.</li><li><code>domain_buyout_intents</code> -- tracks trial-exit buy-out payment intents with Stripe PaymentIntent IDs and handover status.</li><li><code>domain_registrar_columns</code> migration adds registrar-related columns to the existing <code>domains</code> table.</li></ul><h3 id="hosted-website" tabindex="-1">Hosted website <a class="header-anchor" href="#hosted-website" aria-label="Permalink to &quot;Hosted website&quot;">​</a></h3><p>The site builder lives at <code>Company › Your website</code> (<code>/website</code>). It is a full multi-page editor with sections, blocks, design tokens, and publish snapshots. When your workspace has multiple active custom domains (Pro plan), a domain switcher in the top bar lets you edit a per-domain variant of the site. Each domain gets its own pages, navigation, design tokens, and publish snapshot. Switching domains resets the active tab. The public face is served from your custom domain (or workspace subdomain / fallback portal route) once published.</p><p>What the editor surfaces:</p><ul><li><strong>Domain switcher</strong> (top bar) -- When more than one active domain exists, a dropdown lets you pick which site variant to edit. The default site option is labelled with your workspace name. Domains that have not been initialized yet show an &quot;initialize&quot; hint and clone the default site on first selection.</li><li><strong>Editor tab</strong> — Compose pages by adding and arranging sections (hero, text, gallery, services, team, testimonials, contact form, pricing, product, custom HTML). Inspect and edit section content, layout, style, and animation. Save sections as reusable blocks to build a personal library of pre-styled layouts you can drop onto any page. Pricing tiers can optionally carry a buy button that redirects to Mollie or Stripe Connect checkout. Each tier has a configurable VAT rate (21%, 9% or 0%), with the price shown to the customer being the final price, VAT included. The product block is a single-item buy card with the same payment plumbing.</li><li><strong>Pages tab</strong> — Create, rename, delete, and filter pages by status (live, draft, scheduled). Pick a template when creating a new page.</li><li><strong>Style tab</strong> — Design tokens for colors, fonts, scale, motion, buttons, custom CSS, and head snippets (analytics, font preconnects).</li><li><strong>Domain &amp; SEO tab</strong> — Custom domain management. See the custom domains section above.</li><li><strong>Integrations tab</strong>: Connect payment processors (Mollie, Stripe Connect) with inline setup cards, plus third-party services like Mailchimp, Plausible and Trustpilot. The same payment connection powers pay buttons on invoices; see the <a href="/faq/payment-methods">payment methods FAQ</a> for the full configuration.</li><li><strong>Navigation editor</strong> — Drag-and-drop reorder of header links, with dropdown groups and external links.</li><li><strong>Publish button</strong> — Shows unpublished change count. Pushes a snapshot live with one click.</li><li><strong>Responsive preview</strong> — Toggle desktop, tablet, and mobile viewports in the editor.</li></ul><p>The public site is served at the highest-priority URL the company owns: custom domain root → workspace subdomain → fallback <code>/portal/&lt;slug&gt;</code> route.</p><p>See <a href="/advanced/business-page">Site Builder</a> for the full editor guide.</p><h3 id="email-inbox" tabindex="-1">Email inbox <a class="header-anchor" href="#email-inbox" aria-label="Permalink to &quot;Email inbox&quot;">​</a></h3><p>The inbox is a top-level surface at <code>/inbox</code> (<code>apps/web/pages/inbox/index.vue</code>). Backend lives in <code>apps/api/src/modules/inbox/*</code> and writes to a separate set of tables (<code>company_email_domains</code>, <code>company_mailboxes</code>, <code>email_threads</code>, <code>email_messages</code>, <code>email_attachments</code>, <code>email_events</code>).</p><p>Capabilities:</p><ul><li><strong>Threading</strong>: inbound mail is grouped into threads keyed by RFC 822 <code>Message-ID</code> / <code>In-Reply-To</code> / <code>References</code>. Each thread carries <code>last_message_preview</code>, <code>participants</code>, status (<code>open / snoozed / closed / spam / deleted</code>) and labels. Long threads collapse the middle messages behind a &quot;Show {n} earlier messages&quot; pill, keeping the oldest message and the newest two visible (Gmail/Outlook convention). Click the pill to expand everything.</li><li><strong>Reply</strong> — inline reply box on the thread. Smart <code>From</code> picks the address the original mail was sent to, so a customer who emailed <code>support@acme.nl</code> gets a reply from <code>support@</code>, not <code>info@</code>.</li><li><strong>Reply all</strong>: reply to all participants on the thread with one click. The action appears next to reply in the thread header and includes every recipient from the original message.</li><li><strong>Forward</strong>: forward the entire thread to another recipient. Opens a compose drawer with the original message body and attachments preserved for editing before sending. The forwarded message header shows the original sender, date and subject.</li><li><strong>CC and BCC</strong>: CC and BCC fields are available on both compose and reply through an &quot;Add Cc/Bcc&quot; toggle. Addresses accept comma-separated lists or paste from clipboard. The inputs stay hidden until needed, matching the standard inbox pattern where most messages do not need them.</li><li><strong>Drafts</strong>: save partially written messages and come back to them later. Drafts are stored server-side and persist across browser sessions. Each draft carries a subject, recipient list and body. Drafts that are missing a subject show &quot;(no subject)&quot;, and drafts without a recipient show &quot;(no recipient)&quot;. A reply draft is indicated with a &quot;Reply&quot; chip in the thread list, so you can tell at a glance which thread you were mid-response on.</li><li><strong>Compose</strong>: drawer form with a unified identity picker that sets both mailbox and sender address in one control, customer picker (or freeform <code>To</code>), subject, body, CC/BCC fields and attachments. Bounced-recipient warning is shown before send.</li><li><strong>Send-from aliases</strong> — <code>info@</code>, <code>support@</code>, <code>sales@</code> are bidirectional aliases on the same mailbox. <code>noreply@</code> is send-only — selectable as From, but inbound mail to it is dropped on ingest.</li><li><strong>Attachments</strong> — upload before send (compose and reply both). Attachments on inbound mail are downloadable from the message; signed download URLs expire after a short TTL.</li><li><strong>Alias notice</strong> — when an inbound message arrives at an address that isn&#39;t yet a declared alias, the thread shows a soft notice with an &quot;Add as alias&quot; action.</li><li><strong>Linking</strong> — threads can be linked to a customer, project or invoice for cross-referencing.</li><li><strong>Catch-all fallback</strong> — mail to any local-part on the domain falls through to the default mailbox (<code>is_default = true</code>, one per domain). This means typos and undeclared aliases don&#39;t vanish silently.</li><li><strong>Audit log</strong> — outbound sends, mailbox changes and thread state changes are recorded in an audit table for the workspace. Currently API-only (no UI surface yet) — accessible to support staff for troubleshooting.</li><li><strong>HTML email rendering</strong>: HTML emails are rendered with their original styles intact inside a sandboxed iframe. The renderer strips scripts, forms and event handlers during sanitisation, and blocks remote images by default to protect your privacy. A notice bar appears when images are blocked, with a single-click &quot;Show images&quot; action that re-renders the message with images enabled. Text-only fallback displays the plain-text part when no HTML body is present.</li><li><strong>Star/unstar</strong>: mark important threads with a star for quick access. A star icon appears next to the status dot on starred threads in the thread list. The toolbar has a star button that toggles the state for the currently open thread. A &quot;Starred&quot; view in the left sidebar next to Open, Snoozed, Closed, Spam and Trash filters to only starred threads, backed by a partial database index for instant results.</li><li><strong>Soft-delete</strong>: threads can be moved to Trash instead of being permanently destroyed. A delete button (trash icon) appears in the toolbar for non-deleted threads. Once deleted, the button changes to a restore action that moves the thread back to <code>open</code>. The Trash filter appears in the left sidebar alongside Open, Snoozed, Closed and Spam, so you can review deleted threads before they are purged.</li><li><strong>Full-text search</strong>: a search bar above the thread list lets you search across all inbox messages by subject, body text, snippet, and sender. The search is powered by Postgres full-text search with weighted field ranking, so subject matches appear before body matches. Results are grouped by thread, with the best-matching message&#39;s snippet shown as a preview line. Supports quoted phrases, <code>OR</code>, and <code>-</code> exclusions. A 250 ms debounce keeps the UI responsive, and the loading spinner gives real-time feedback.</li></ul><h4 id="drafts" tabindex="-1">Drafts <a class="header-anchor" href="#drafts" aria-label="Permalink to &quot;Drafts&quot;">​</a></h4><p>The Drafts tab sits alongside the main thread list. Drafts are saved server-side, so they survive browser restarts and follow you across devices. When you start a new message or reply and close the compose drawer without sending, the content is automatically saved as a draft. You can also explicitly save a draft with the &quot;Save draft&quot; button. A draft chip shows &quot;Reply&quot; if the draft was started from a thread, or &quot;New&quot; if it is a fresh compose. Editing a draft opens the compose drawer pre-filled with the saved content. Deleting a draft requires a confirmation step.</p><p>The inbox uses your custom domain only after <code>quickEnableInbox</code> has run successfully and the apex MX records point at Cloudflare. Until then, the workspace can still send mail through the default delivery path described in <a href="/settings/email">Email Integration</a>, but it can&#39;t receive mail.</p><h4 id="live-polling" tabindex="-1">Live polling <a class="header-anchor" href="#live-polling" aria-label="Permalink to &quot;Live polling&quot;">​</a></h4><p>The inbox refreshes automatically while the tab is open. The thread list polls every 45 seconds for new mail, and the sidebar badge refreshes every 60 seconds. Both pauses when the tab is hidden, so no unnecessary network traffic in the background. The polling is silent: no loading spinners flash on background refreshes, and polls are skipped entirely during an active search to keep your results stable. A manual refresh button next to Compose lets you pull the latest state instantly when you are expecting something right now; it is disabled during the in-flight call to prevent request stacking.</p><h4 id="mark-as-unread" tabindex="-1">Mark as unread <a class="header-anchor" href="#mark-as-unread" aria-label="Permalink to &quot;Mark as unread&quot;">​</a></h4><p>You can mark any open thread as unread from the toolbar. Unlike earlier versions where the unread state was a local-only toggle that a refetch would discard, this is now persisted server-side. The sidebar badge counts unread messages, not threads with unread, and updates accordingly. The thread stays unread across page reloads, browser restarts, and devices until you open it again.</p><h3 id="sales" tabindex="-1">Sales <a class="header-anchor" href="#sales" aria-label="Permalink to &quot;Sales&quot;">​</a></h3><p>When you add buy buttons to pricing tiers or a product block on your public site, every completed payment creates a sale record. Track them at <strong>Money &gt; Sales</strong> (<code>/workspace/financial/money/sales</code>).</p><p>What the sales log shows:</p><ul><li>A chronologically ordered list of all purchases made through your site.</li><li>The payment processor (Mollie or Stripe Connect) and the payment status (<code>paid</code>, <code>pending</code>, <code>failed</code>, <code>expired</code>, <code>refunded</code>).</li><li>The originating section (which pricing tier or product block was purchased).</li><li>Customer email, amount paid, and currency.</li></ul><p>Sales records are created by the public checkout endpoint (<code>POST /public/sites/:slug/checkout</code>), which validates the section, creates a payment through the connected processor, and redirects the buyer to the hosted checkout page.</p><p>Paid sales show an action menu (three dots) with two options:</p><ul><li><strong>Regenerate invoice.</strong> Re-creates the linked invoice if it was lost or not generated during the purchase. Safe to run even if the invoice already exists.</li><li><strong>Refund.</strong> Returns the full amount to the customer through the original payment processor (Mollie or Stripe). A credit note is automatically created against the linked invoice for your bookkeeping. Only available for paid sales.</li></ul><h3 id="post-purchase-flow" tabindex="-1">Post-purchase flow <a class="header-anchor" href="#post-purchase-flow" aria-label="Permalink to &quot;Post-purchase flow&quot;">​</a></h3><p>When a payment completes, the platform automatically runs the following steps. Everything runs fire-and-forget: failures are logged and do not affect the payment status the buyer sees.</p><ol><li><strong>Invoice generated.</strong> An invoice is created from the sale, with the product name, price and the buyer&#39;s email. The price you set in the editor is the final customer price, VAT included. The invoice line splits this into an ex-VAT amount and the VAT rate you configured on the pricing tier or product block (default 21%). If the buyer&#39;s email matches an existing customer in your workspace, the invoice is linked to that customer. Otherwise a minimal customer record is created. The invoice is finalised immediately (status <code>sent</code>) since payment was already received.</li><li><strong>Payment recorded.</strong> A payment record is created on the invoice through the standard payment service. The payment method is set to the processor (Mollie or Stripe), and the reference includes the processor session ID for audit trails.</li><li><strong>Customer receipt.</strong> The buyer receives a confirmation email with the product name, amount and payment method. If an invoice was generated, the email includes a secure portal link to view and download the invoice PDF.</li><li><strong>Owner notified.</strong> You get an in-app notification and an email summary of the sale: product, amount, customer email, and a direct link to the invoice.</li></ol><p>The checkout-success and checkout-cancelled pages show the buyer a branded result screen that uses your site&#39;s design tokens (colors) so the page stays on-brand.</p><h2 id="inbox-tab-visibility" tabindex="-1">Inbox tab visibility <a class="header-anchor" href="#inbox-tab-visibility" aria-label="Permalink to &quot;Inbox tab visibility&quot;">​</a></h2><p>The Inbox tab appears in the sidebar and bottom navigation only when the workspace has an inbox-enabled domain configured. Having plan-level access is not enough on its own -- the workspace must complete the onboarding flow and enable inbox on at least one domain. This prevents the tab from appearing for users who have inbox access through their plan but read email elsewhere and would never want an empty second inbox.</p><p>On free plans that do not include inbox, the tab remains visible as an upgrade hint. But on paid plans, once a domain is wired through the setup wizard and the inbox is ready, the tab appears automatically.</p><h2 id="demo-website-claim" tabindex="-1">Demo website claim <a class="header-anchor" href="#demo-website-claim" aria-label="Permalink to &quot;Demo website claim&quot;">​</a></h2><p>When MyCompanyDesk builds a demo website for a prospect as part of the outreach program, the prospect receives a personalised claim link (via WhatsApp or email). The claim page at <code>/claim/&lt;slug&gt;</code> lets the prospect take ownership of the demo workspace with their own email address and password.</p><h3 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h3><ol><li>Sil or the outreach cron generates a demo workspace (<code>companies.is_demo = true</code>) with a 4-page trade-specific website (Home, Diensten, Over ons, Contact) built on top of every new workspace&#39;s default site foundation. Trade-aware overlays are then applied: the Home hero gets a trade tagline, the Diensten page receives a spotlight block with the trade&#39;s primary service and a services block with three trade-specific service cards, and the Contact page is populated with the prospect&#39;s phone number in the form intro and their address in the locations block.</li><li>The prospect receives a link like <code>https://app.mycompanydesk.com/claim/roofer-amsterdam</code>.</li><li>The claim page loads the demo by slug and shows the business name. If the demo workspace exists and is claimable, the prospect fills in their email and a password (minimum 8 characters, at least one letter and one digit).</li><li>On submit, the workspace is atomically transferred: the placeholder user is rewritten with the prospect&#39;s email and password, <code>is_demo</code> is flipped off, and the outreach row is updated with claim metadata.</li><li>The email is marked as verified on claim (the prospect already proved ownership of the contact method used for outreach). A welcome email is still sent so the address is in their inbox.</li><li>The prospect is redirected to the login page with a success message and can immediately sign in, edit their website, send invoices, and use the inbox.</li></ol><h3 id="safety-guarantees" tabindex="-1">Safety guarantees <a class="header-anchor" href="#safety-guarantees" aria-label="Permalink to &quot;Safety guarantees&quot;">​</a></h3><ul><li>Only <code>is_demo = true</code> workspaces can be claimed. Real customer sites are never claimable through this endpoint.</li><li>The email must not already belong to another user on the platform.</li><li>The claim is atomic (single database transaction), so partial transfers cannot leave a workspace in an inconsistent state.</li><li>Claim links are invalidated once the demo is claimed, preventing reuse.</li></ul><h2 id="sending-mail-vs-receiving-mail" tabindex="-1">Sending mail vs receiving mail <a class="header-anchor" href="#sending-mail-vs-receiving-mail" aria-label="Permalink to &quot;Sending mail vs receiving mail&quot;">​</a></h2><p>This bundle is the <strong>receiving</strong> side. Outgoing email — invoice delivery, reminders, quote sends — is handled by the broader email pipeline described in <a href="/settings/email">Email Integration</a>. The inbox is for receiving customer mail and composing replies; it does not route your automated invoice sends. Invoice delivery always respects your chosen delivery method under <a href="/settings/email">Email Integration</a> (Gmail, Outlook, or the built-in sender). The inbox domain&#39;s DKIM is used for outbound replies composed in the inbox, not for automated transactional mail.</p><h2 id="limits-and-gotchas" tabindex="-1">Limits and gotchas <a class="header-anchor" href="#limits-and-gotchas" aria-label="Permalink to &quot;Limits and gotchas&quot;">​</a></h2><ul><li><strong>One website per company.</strong> Adding a custom domain deactivates the workspace subdomain. Removing the domain doesn&#39;t auto-revive the slug — re-activate it manually if you want to fall back.</li><li><strong>CNAME mode has no email.</strong> Email routing requires a full Cloudflare zone, which only nameserver mode provides.</li><li><strong>The wizard refuses to overwrite an existing third-party MX.</strong> If your apex already points at Google Workspace or Microsoft 365, <code>quickEnableInbox</code> returns <code>apexMx.status = &#39;conflict&#39;</code> and you have to choose: migrate MX to Cloudflare, or stay on your existing provider and skip the bundled inbox.</li><li><strong>Reserved subdomains.</strong> <code>app</code>, <code>admin</code>, <code>api</code>, <code>www</code>, <code>mail</code>, <code>support</code>, <code>portal</code>, <code>dashboard</code> and a handful of others are blocked at the workspace-slug level.</li><li><strong>Pre-launch.</strong> The bundle is feature-gated by <code>custom_domains</code> and <code>public_business_page</code>. Workspaces without those flags see the upgrade prompt instead of the editor.</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/getting-started/company-setup">Setup wizard</a> — the magical onboarding that drives the bundled flow.</li><li><a href="/settings/email">Email Integration</a> — outgoing email, send-from identity picker, delivery tracking.</li><li><a href="/advanced/business-page">Site Builder</a> — the full editor guide.</li><li><a href="/settings/company">Company Settings</a> — the umbrella that hosts About / Look / Website / Address.</li><li><a href="/settings/billing">Billing &amp; Plans</a> — feature flags that gate the bundle.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("features/domains-website-inbox.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const domainsWebsiteInbox = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  domainsWebsiteInbox as default
};
